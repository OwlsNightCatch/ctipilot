# Findings summary (machine-readable)
- code: F1
  category: editorial-advisory
  section: systemic
  item: "tools/check_run.py (out of the four-file scope; traceable to the iteration-3 remediation the run record claims complete)"
  url_or_quote: "# warned per record (98 of them would drown the store report)."
  summary: "New comment added by this commit still carries the superseded 98 figure that iteration 3 corrected to 101 everywhere else (check_run.py line 2107, the report, the run record, CHANGELOG, cti-run.md). In this sentence the suppressed per-record count is neither 98 nor 101 but 50 — the number `--all` actually prints (`PASS run-clock: 50 pre-v3.32 record(s)`). Non-blocking advisory; no reader-facing artifact is affected."
