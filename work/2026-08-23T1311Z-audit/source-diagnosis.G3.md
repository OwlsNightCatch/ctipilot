# G3 audit — OT/ICS + broader research-blog source diagnosis
run_id: 2026-08-23T1311Z-audit — 14-day retrospective window (2026-08-09T13:15:57Z → 2026-08-23T13:11:00Z)

## The eleven named suspects — deliberate fetch-ladder attempt on each

### 1. dragos (sources.json id: `dragos`)
- **Transport that worked:** plain `WebFetch` on the listing page `https://www.dragos.com/blog/` — returns a fully dated post list (titles + exact dates), no bridge/jina needed.
- **Transport that failed:** `python3 tools/fetch_source.py url https://www.dragos.com/blog/feed/` → direct HTTP 404 (feed path does not exist); jina fallback failed on ALL 7 rotation keys with HTTP 402 "balance exhausted" (confirms the spawn brief's note that the jina pool may be credit-exhausted this run).
- **Diagnosis: NOT dark.** The store already carries two 2026-08-16 entries citing `dragos.com/blog/...` directly as `primary_source_url` (`weekly-w33-q2-ransomware-reports-dragos-checkpoint`, `weekly-w33-water-plc-lockout-status`) — the working recipe (WebFetch the listing, ignore the dead `/feed/` path) is already in active use by at least one sub-agent this window. **Recipe fix for sources.json:** drop the `/blog/feed/` guess from the notes entirely; the documented recipe should read "WebFetch `https://www.dragos.com/blog/` directly — dated listing, no feed exists."

### 2. nozomi-networks (sources.json id: `nozomi-networks`)
- **Transport that worked:** plain `WebFetch` on `https://www.nozominetworks.com/blog` — returns titles but genuinely **no inline dates on the listing** (confirmed, matches the existing note); drilling individual `/blog/<slug>` pages returns a reliable byline date.
- **Transport that failed:** `tools/fetch_source.py url https://www.nozominetworks.com/blog/feed` → direct HTTP 404 (this run; a prior run had recorded "empty parse" via jina — the feed path itself appears to not exist at all, not just malformed).
- **Diagnosis: genuinely quiet in-window, not unreachable.** Drilled 5 candidate posts from the top of the listing (by page position, which is NOT chronological — a July 23 post outranked June/July items): "Far from the Battlefield… APT28…" (2026-07-23), "Tengu… Mirai" (2026-07-27), "…FBI/EPA PLC Warning…" (2026-07-31), all pre-window. The single in-window post found by drilling the position-1 item was "NERC CIP-015 in Practice #7" (2026-08-18) — process/compliance content, not technical threat research (recaps a webinar series; no detection mechanics). **Zero mentions of `nozomi` anywhere in the 14-day prior-coverage index** — the source really has contributed nothing this window, but that is because its most recent *substantive* research post predates the window, not because the recipe is broken.

### 3. claroty-team82 (sources.json id: `claroty-team82`)
- **Transport that worked:** plain `WebFetch` on `https://claroty.com/team82/research` — returns 9 titles with NO dates on the listing (matches the existing note); drilling individual `/team82/research/<slug>` pages returns a reliable byline date and full body.
- **Diagnosis: RECOVERED a genuine, multi-week dark spell.** Zero mentions of `claroty` or `team82` anywhere in the 14-day prior-coverage index. Drilling the top-2 listing items surfaced **two substantive, previously uncovered OT vulnerability disclosures, both dated 2026-08-09** (right at/near the window's opening boundary — see the two `borderline: true` items in the findings YAML): Copeland XWEB Pro refrigeration-controller root RCE (23 CVEs) and Danfoss AK-SM 800A refrigeration-controller auth-bypass/RCE (3 CVEs). This confirms the standing note's own working recipe is sufficient — the gap was a coverage/attention gap (nobody drilled the listing), not a transport failure.

### 4. sans-ics (sources.json id: `sans-ics`)
- **Transport attempted:** plain `WebFetch` on the filtered URL `https://www.sans.org/blog/?focus-area=industrial-control-systems-ics`.
- **Failure mode confirmed:** the `?focus-area=` query parameter is silently ignored by whatever renders/summarises the page — the returned listing is the GENERAL SANS blog (AI, DFIR, leadership posts), not the ICS-filtered set the URL claims to request, even though the page reports "102 total articles" under that filter. This is a JS-rendered filter that WebFetch cannot exercise.
- **Diagnosis: recipe genuinely broken, unresolved this run.** No working alternative found in the time available (no ICS-specific RSS discovered). Flagged as an open recipe gap, not attempted via jina (low expected value — SANS-ICS cadence is already documented as sparse and non-primary, reliability B/aggregator-adjacent).

### 5. industrialcyber-co (sources.json id: `industrialcyber-co`)
- **Transport that worked:** `python3 tools/fetch_source.py feed https://industrialcyber.co/feed/ 15` — direct fetch, 9 items, full bodies with dates (RECIPE CONFIRMED again, matches the 2026-07-14 note).
- **Diagnosis: live, working, low direct-research yield but valuable as a discovery lead.** Most items are regulatory/certification/PR content (ISASecure/NSA HCSA scheme, NIST guides, CMMC news) — none independently qualify. One item, however, was the discovery lead that pivoted to a genuine finding: "Bitdefender traces SilkParasite cyberespionage campaign…" (2026-08-20 aggregator mention) → pivoted to the Bitdefender primary (see findings). Recommend keeping this source's role as a discovery/lead-generation feed rather than a primary.

### 6. withsecure-labs (sources.json id: `withsecure-labs`)
- **Transport that worked:** plain `WebFetch` on `https://www.withsecure.com/en/resources-hub/w-labs/` — page renders real content, not a JS shell.
- **Diagnosis: genuinely quiet in-window.** The newest dated item found by drilling (GREYVIBE Russia-nexus AI-enabled group) is dated 2026-05-28 — five weeks before the audit window even opens. No August content surfaced. Listing shows no month-level dates for its other four entries either, consistent with the existing note; low cadence is confirmed, not a transport problem.

### 7. 0patch-blog (sources.json id: `0patch-blog`)
- **Transport that worked — and a BETTER recipe than the one on file:** `python3 tools/fetch_source.py feed https://blog.0patch.com/feeds/posts/default 15` — the legacy Blogger Atom feed returns 15 full-body items with reliable dates and the new-domain (`0patch.com/blog/...`) article URLs, exactly as the existing note promised, but the **exact feed URL was not itself recorded** in the source's `rss_url` field (it is `null` in the slice) — a prior agent had to rediscover the path from memory/notes each time. **Recipe fix:** set `rss_url: "https://blog.0patch.com/feeds/posts/default"` in sources.json so future runs don't have to reconstruct it.
- **Diagnosis: live and productive.** Surfaced a genuine, previously uncovered CVE (Certighost, CVE-2026-54121 — AD CS elevation of privilege via client-DC referral spoofing, 2026-08-17) — see findings. A second in-window item (KerberLoss micropatch, 2026-08-16) covers a CVE (CVE-2026-25177) already fully covered via Semperis/Black Hat reporting in the prior 2026-08-09 weekly entry — not a new gap, just corroboration that 0patch tracks the same population of AD vulnerabilities Semperis's Black Hat talk covered.

### 8. tenable-research (sources.json id: `tenable-research`)
- **Transport that failed (confirmed again):** `python3 tools/fetch_source.py url https://www.tenable.com/blog` → returns a Drupal HTML shell with **zero rendered post links** in the static markup (JS-rendered listing, matches the existing "count=0" note).
- **Transport that WORKS — a genuinely new recipe, not previously documented anywhere in sources.json:** the shell HTML itself advertises `<link rel="alternate" type="application/rss+xml" title="Tenable Blog" href="https://feeds.feedburner.com/tenable/qaXL" />`. `python3 tools/fetch_source.py feed https://feeds.feedburner.com/tenable/qaXL 15` returns 10 full-body items with reliable dates, resolving the long-standing "blog.xml feed parses to count=0" gap outright. **Recipe fix: set `rss_url: "https://feeds.feedburner.com/tenable/qaXL"` and `fetch_method: rss` in sources.json for `tenable-research`.**
- **Diagnosis: recipe now fully fixed; high-value in-window content confirmed.** Most items duplicate stories already covered from other primaries (Siemens S7 PLC advisory, Oracle CSPU), but one — "The Agentic AI Threat Cluster: Seven Incidents, Three Actors…" (2026-08-14) — was the discovery lead for a major, entirely uncovered story (the Taiwan near-autonomous AI government intrusion; see findings item 1).

### 9. symantec-broadcom — NOT a tracked source in sources.json at all
- No record for `symantec-broadcom`, `symantec`, or `broadcom` exists in `sources/sources.json` under any id.
- **Transport that worked:** plain `WebFetch` on `https://www.security.com/threat-intelligence` (the modern URL — `symantec-enterprise-blogs.security.com/threat-intelligence` 301-redirects here) returns a clean, dated listing.
- **Diagnosis: reachable and already indirectly contributing, but untracked.** The 2026-08-16 entry `jewelbug-pdf-viewer-extension-native-messaging-webmail-hole` already cites `https://www.security.com/threat-intelligence/jewelbug-crypto-fraud-espionage` as its `primary_source_url` — meaning a prior sub-agent found this page (via search, not via a systematic listing sweep), but the source was never added to `sources.json` and so is never swept proactively. **Recommend adding as a new candidate source** (see `candidate_sources` in the findings YAML) — this closes the loop between "occasionally cited" and "systematically checked."

### 10. cybereason — NOT a tracked source in sources.json
- **Transport that worked:** plain `WebFetch` on `https://www.cybereason.com/blog` returns a clean, dated listing.
- **Diagnosis: reachable but genuinely DORMANT, not a fetch failure.** The newest post on the listing is dated **2026-02-05** — over six months before the window opens. This is not a recipe problem; Cybereason's public blog appears to have gone quiet mid-fiscal-year 2026 (consistent with widely reported corporate/ownership turbulence at the company this year). Recommend NOT adding as a tracked source until cadence resumes — a source contributing nothing for six straight months earns no rotation slot.

### 11. forescout-vedere — NOT a tracked source in sources.json
- **Transport that failed the WebFetch summariser:** `WebFetch` on `https://www.forescout.com/blog/` reported "JavaScript-rendered shell with no dated posts" — but this is a **summariser misdiagnosis, not a true JS-empty shell.**
- **Transport that worked:** `python3 tools/fetch_source.py url https://www.forescout.com/blog/` (raw HTML, Nitro-cached WordPress page) DOES contain real `<a href="https://www.forescout.com/blog/...">` links in the static markup — three were recovered by grepping for `blog/` hrefs, including "Exploiting Serial-to-Ethernet Converters in Critical Infrastructure." **Recipe fix if this source is ever tracked: bridge `url` fetch + href-grep for `/blog/` patterns, NOT plain WebFetch (whose summariser drops the real links present in the raw body) and NOT the jina reader (unnecessary — direct HTML already contains the links).**
- **Diagnosis: genuinely quiet in-window, not unreachable.** All three recovered article dates are pre-window (2026-04-21, 2025-04-29, and one undated). No in-window Forescout/Vedere Labs content found.

## Summary judgment for the deployment

Of the eleven named suspects, **the "entirely dark" diagnosis holds cleanly for exactly one — Claroty Team82** — which this run recovered (two substantive OT vulnerability disclosures, dated right at the window boundary). **Dragos was mischaracterized as dark**: it is live, working, and was already being fetched successfully by at least one sub-agent this window (two W33 entries cite it directly) — the standing note's stale "feed 404" observation from 2026-07-10 appears to have been read by later audits as "source broken" when the actual, already-documented fallback (WebFetch the listing directly) works fine and is in active use. **Nozomi, WithSecure, and Forescout are genuinely quiet this window** (their most recent substantive posts predate 2026-08-09) rather than unreachable — all three recipes work when tested. **SANS-ICS has a real, unresolved recipe defect** (the focus-area filter is inert under WebFetch). **0patch and Tenable had real, now-fixed recipe gaps** — both were previously reachable only by accident or not at all; this run pins working feed URLs for both. **Symantec/Broadcom and Cybereason are simply untracked** (not suspects for "darkness" — they were never on the list to begin with); the former is a live, valuable gap in `sources.json`, the latter is dormant and not worth tracking yet.
