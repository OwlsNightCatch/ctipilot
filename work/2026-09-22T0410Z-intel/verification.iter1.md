**Model:** Sonnet 5 (`claude-sonnet-5`)
**Timestamps:** started_at=2026-09-22T04:46:29Z · ended_at=2026-09-22T04:57:21Z · duration_seconds=652

## Verification report — 2026-09-22T0410Z-intel (iteration 1)

### Citation does not support the claim

**#1** `entries/2026-09-22/cve-2026-7273-zyxel-gs1900-red-heron-kev-exploited.md` — body claims "Red Heron, a Chinese-speaking, PRC-linked cluster **Acronis Threat Research Unit profiled on 2026-09-13** for rapid n-day weaponisation of a Gitea flaw, based on a shared command-and-control domain, malware family and other overlapping tradecraft ([GreyNoise, 2026-09-21](https://www.greynoise.io/blog/open-season-on-kapibala-attacker-steals-government-records-wordpress-exploitation))." Fetched the GreyNoise post in full: it names and links Acronis ("The adversary is the same or related to 'Red Heron' reported on by Acronis…") but never states a publication date for the Acronis report anywhere in the article. The "2026-09-13" date is true (confirmed by fetching `https://www.acronis.com/en/tru/posts/red-heron-exploits-gitea-n-day-flaw-in-multinational-campaign-exposing-new-linux-rootkit/`, whose page metadata and text give `date: "2026-09-13"`), but the entry never cites the Acronis post — the sole citation terminating that clause is GreyNoise, which does not carry the date. Classic adjacency violation (check 2d): a true fact spliced onto a citation that doesn't state it. Fix: either add the Acronis post as a source and cite it for the date, or drop the specific date from the GreyNoise-cited clause.

**#2** `entries/2026-09-22/cve-2026-13684-synology-dsm-unauth-file-read-write.md` — body states "Two further flaws in the same advisory need only a low-privileged authenticated session: CVE-2026-13673 … and CVE-2026-6205 (CVSS 8.1, CWE-73, external control of file name/path in the Upload API), **both also reaching arbitrary file read/write and denial-of-service**" ([Synology PSIRT, 2026-09-18](https://www.synology.com/en-global/security/advisory/Synology_SA_26_13)). Fetched the Synology advisory directly: its CVE-2026-6205 entry reads "allows remote authenticated users to **write** arbitrary files and conduct denial-of-service attacks" — no "read" capability is claimed. The NCSC-CH advisory (fetched via `ncsc-csh post 12960`) independently confirms the same asymmetry: "External control of file name in Upload API allows authenticated users **to write** arbitrary files and cause DoS." Both cited sources agree CVE-2026-6205 is write/DoS only, not read/write; the entry's "both … reaching arbitrary file read/write" overstates CVE-2026-6205's impact. (Confirming detail: the store's own `state/cves_seen.json` record for this same CVE, written during this run's Phase 2, correctly says "arbitrary file **write** and DoS" — the published body text disagrees with the pipeline's own internal bookkeeping.)

**#3 (low confidence)** `entries/2026-09-22/cve-2026-66804-windows-dangling-com-privesc.md` — body states "a COM object registration for the **CrossDevice.Streaming.Source class** is registered machine-wide under the classes root." Fetched the Project Zero post directly: it identifies the dangling registration as "the CrossDevice COM object with the CLSID `{E9F83CF2-E0C0-4CA7-AF01-E90C70BEF496}`" and separately states the missing DLL path is "`%PROGRAMDATA%\CrossDevice\CrossDevice.Streaming.Source.dll`." The source never names the *class* itself "CrossDevice.Streaming.Source" — that string is the DLL filename, not a stated class/ProgID name. Minor precision slip, does not change the technical substance.

### Unsupported / hallucinated facts

**#4** `entries/2026-09-22/cve-2026-13684-synology-dsm-unauth-file-read-write.md` — frontmatter `sourcing_note`: "Synology's own PSIRT advisory is the sole source of the technical facts; **NCSC Switzerland and CERT-FR relay** that same advisory rather than independently assessing it, so credibility reflects one assessor with several publishers." No CERT-FR / ANSSI-FR record appears anywhere in this entry's `sources[]` (only Synology PSIRT, NCSC Switzerland, heise online), nor in `evidence[]`, nor as an inline citation in the body. The run record's `sources_changed[]` shows `anssi-fr` was fetched this run ("fetched and used (corroborated Synology DSM entry)"), so a French source was apparently used in research, but it never made it into the published entry as a citable reference — the sourcing_note asserts a relaying party the reader has no way to check. Either add the ANSSI-FR/CERT-FR URL as a source, or remove "and CERT-FR" from the note.

**#5** `entries/2026-09-22/cve-2026-13684-synology-dsm-unauth-file-read-write.md` — `cves[]` sets `type: rce` for all four CVEs (CVE-2026-13684, CVE-2026-13639, CVE-2026-13673, CVE-2026-6205), and the entry's top-level `tags[]` includes `rce`. Fetched both cited primary sources: Synology's own advisory text for every one of these four CVEs reads only "allows remote attackers/authenticated users to read or write arbitrary files and conduct denial-of-service attacks" (or write-only for CVE-2026-6205) — no code-execution capability is stated anywhere. NCSC-CH's advisory likewise frames all four purely as file read/write/DoS. The entry's own body and headline never claim RCE either ("two unauthenticated CVSS 9.8 flaws … allow remote arbitrary file read/write and denial-of-service"). `site/taxonomy.yaml` offers `path-traversal` and `logic-flaw` as closer-fitting `cve_types` values; `rce` is not supported by any cited source and contradicts the entry's own careful body framing.

**#6** `entries/2026-09-22/cve-2026-13684-synology-dsm-unauth-file-read-write.md` — `evidence[]` record for the heise quote is structurally malformed against the documented translated-quote schema (`docs/pipeline.md` § evidence example): `quote: "Die knappe Beschreibung der Lücken auf der Synology-Hinweisseite dürfte Ransomware-Autoren…"` (raw German) with the English translation buried parenthetically inside the `publisher:` field ("heise online (translated from German: \"The terse description…\")") instead of the required shape — `quote:` carrying the **English** translation marked "(translated from German)", with a separate `original:` field carrying the verbatim German text. No `original:` field exists on this record at all. Per the org-profile instructions this is exactly the "missing `original:` field on a translated quote" case (F4), and if the frontmatter `evidence[]` is ever rendered reader-facing as written, it surfaces untranslated German. (The body text itself does this correctly — "the terse description of the flaws … is likely to quickly draw ransomware authors and other criminals, since exploitation is, according to Synology, very easy" (translated from German) — only the frontmatter record is malformed.)

**#7** `runs/2026-09-22/2026-09-22T0410Z-intel.md` — verification notes state the deep-dive rotation check as: "Category `network-stack-rce` has not appeared in the prior 7 days' deep dives (`web-app-rce` ×2, `firewall-vpn-rce` ×1, `supply-chain` ×1, `apt-campaign` ×1, `annual-report` ×1 over the last 30 days) — no rotation demotion applies." Enumerated every published entry with `deep_dive: true` and its `discovered_at` against the run date (2026-09-22): the actual 30-day breakdown is `annual-report` ×2, `identity-infra` ×2, `web-app-rce` ×2, `apt-campaign` ×2, `windows-lpe` ×1, `cloud-saas` ×1, `other` ×1, `firewall-vpn-rce` ×1, `supply-chain` ×1 (13 deep dives total from `entries/2026-08-23/btr-sys-defender-remediation-driver-kernel-primitive.md` through `entries/2026-09-17/mandiant-ai-risk-resilience-report-2026.md`). The run record's own stated breakdown undercounts `apt-campaign` (says ×1, is ×2) and `annual-report` (says ×1, is ×2), and omits `identity-infra` ×2, `windows-lpe` ×1, `cloud-saas` ×1 and `other` ×1 entirely. The bottom-line conclusion (network-stack-rce hasn't appeared, no demotion needed) still holds against the correct data, but the published run-record notes misstate the supporting counts — this is reader-facing text (run records publish) and should be corrected to the actual tally.

### Missed angles

**#8 (low confidence)** `entries/2026-09-22/cve-2026-7273-zyxel-gs1900-red-heron-kev-exploited.md` newly registers `actor:red-heron` and links it (via a hedged `exploits` relation) back to the actor's earlier Gitea CVE-2026-60004 exploitation, which the store already covers as its own entry (`entries/2026-08-30/cve-2026-60004-gitea-diffpatch-git-hook-rce-kev.md`). That older entry has no changelog record noting the newly-established Red Heron attribution, and the new Zyxel entry's `references[]` is empty rather than pointing back to the Gitea entry. Not a dedup violation (different CVE, genuinely distinct finding), but the actor link this run established is a natural candidate for a short `internal`/`update` changelog record on the older entry, or at minimum a `references[]` pointer from the new entry — worth a look on a future pass. Suggested angle: re-check whether Acronis's or GreyNoise's Gitea-exploitation-in-July detail materially updates the CVE-2026-60004 entry's own actor attribution (it currently carries none).

### Org-triage line missing / inconsistent

**#9 (low confidence)** `entries/2026-09-22/cve-2026-13684-synology-dsm-unauth-file-read-write.md` — priority calibration. `priority: high` is applied to a pair of CVEs where (a) NVD's own SSVC assessment records `exploitation: none` as of 2026-09-18 (confirmed via a fresh NVD API lookup this iteration), (b) no PoC is reported by any cited source, and (c) — per finding #5 above — the vulnerabilities are not actually RCE, only arbitrary file read/write and DoS. Per whole-run check 11's PD-11 gate ("actively exploited, imminent mass exploitation, pre-auth RCE on an exposed edge with public PoC, another out-of-band response … high CVSS alone" does not qualify), this entry's urgency case rests entirely on heise's editorial framing ("likely to quickly draw ransomware authors … exploitation is, according to Synology, very easy") plus the vendor's "no mitigation" line, rather than any of the four enumerated qualifying conditions. There is a reasonable counter-argument (unauthenticated arbitrary file read/write with no mitigation on commonly WAN-exposed NAS boxes is real exposure-driven urgency even without confirmed exploitation), so this is flagged low-confidence for the main agent's judgment rather than as a clear-cut miscalibration.

### Verdict

`NEEDS_FIXES (truth: 7, editorial: 2, advisory: 0)`

Findings #1–#7 are truth-class (F3 ×3 including the low-confidence #3, F4 ×4 including the run-record deep-dive-count error). Findings #8–#9 are editorial-class (F10, F16), both marked low confidence. All four new entries' CVE/CVSS/EPSS transcriptions were checked directly against NVD and FIRST.org and matched exactly; all four entries' `techniques[]` ids resolve to active (non-deprecated, non-revoked) entries in the pinned ATT&CK dataset; the Plugin4Shell entry's every checkable claim (version numbers, timeline dates, vendor-status table, GitLab mention) was independently confirmed against AIR Security's raw HTML timeline table and against heise's corroborating coverage; the Windows COM entry's every technical claim and both evidence quotes were verified verbatim against Project Zero's post and MSRC's page (fetched via jina after the JS-rendered SPA blocked direct extraction); no dedup violations were found against `prior_coverage.json` (68 records) or `state/cves_seen.json`; the two S2 borderline drops (VBS/NDB Kaspersky-investigation closure, Google Gemini breach story) were independently spot-checked via web search and both hold up as reasonable drops on the stated grounds; no IOCs, no watchlist/org-triage violations, no classification-vocabulary violations were found; actions[] on all four entries are concrete and finding-specific (or empty), no padding.

### Findings summary (machine-readable)
