**Model:** Opus 5 (`claude-opus-5`)
**Timestamps:** started_at=2026-08-19T05:19:33Z · ended_at=2026-08-19T05:45:29Z · duration_seconds=1556

## Verification report — 2026-08-19T0410Z-intel (iteration 1)

Read cold. Eleven new entries plus the run record, end to end, frontmatter and body. All 22 distinct inline URLs exercised. Every evidence quote re-checked as a literal substring against a page body I read this iteration (saved `raw/` bodies where the run kept them, fresh fetches otherwise). Independent re-derivations performed on the two exploitation-status flips (KEV feed, EUVD API, MSRC API), the Keycloak errata structure (Red Hat security-data API plus all four errata pages), the GitLab release history, and the Medusa advisory as carried by three outlets.

**Transport ladder honoured.** The jina reader pool is credit-exhausted (confirmed live: every key returns HTTP 402), so `WebFetch` and `tools/fetch_source.py url` were the transports. One cited page could not be rendered by any available rung — the DataBreaches.net Bits of Gold article (direct 403, reader 402). That is not a finding: the entry discloses it explicitly, and I verified the quoted substance against the publisher's own feed, whose item is dated `Mon, 17 Aug 2026 13:19:23 +0000` and reads "a hacker gained unauthorized access to a third-party data analytics network and gained access to customers' names, national ID numbers, emails" — matching the entry, including that the company names the platform class and not the product. Wordfence and cisa.gov HTML behaved exactly as the spawn message described.

### Citation does not support the claim

**F1 — Medusa entry: the agencies do not say Medusa buys its exploits.** Title: "the agencies say the group buys its exploits rather than finding them". Headline: "by purchasing access to the ones others find". Body: "the pre-disclosure window comes from purchased access". The cited healthsystemCIO page says the opposite in terms: *"Still, the FBI found no indication the group develops its own zero-day or N-day vulnerabilities. Advance access comes instead from sources the agencies could not identify."* The advisory language both other outlets quote is "preferring instead to obtain advanced access to exploits from unknown sources" — obtained from unknown sources, not bought. The $100–$1,000,000 broker economics in the same entry is payment for initial **access to victim networks**, a different transaction from exploit acquisition; the entry conflates them. The registry summary for `malware:medusa` gets this right ("developing no zero-day or N-day flaws of their own, buying access from initial-access brokers"), so only the entry needs correcting.

**F2 — Medusa entry: GoAnywhere is cited to the one source that does not mention it.** "Two products from the group's historically exploited list are named — Fortra's GoAnywhere managed file transfer and BeyondTrust's remote-support software, the latter's February 2026 disclosure offered as the worked example … ([healthsystemCIO, 2026-08-18])". "GoAnywhere" occurs zero times in the healthsystemCIO page I fetched and twice in CyberScoop ("such as Fortra GoAnywhere and BeyondTrust flaws"). healthsystemCIO does carry the BeyondTrust half ("the agencies cite the February 2026 BeyondTrust disclosure as one example"). Split the citation.

**F3 — PurpleDelta entry: a hedge dropped (minor).** "credentials and identities are bought from infostealer-log channels" is asserted; Insikt writes *"PurpleDelta was observed across multiple Telegram channels that sell infostealer logs, suggesting it may be purchasing and using stolen credentials"*. The identity-buying half is observed fact ("buying US and Ukrainian identities and freelance accounts"); the infostealer-credential purchase is Insikt's inference. The entry's own sourcing note makes hedge fidelity its stated method, so this is inconsistent with its own standard.

### Unsupported / hallucinated facts

**F4 — GitLab entry: "every release line since mid-2026" is wrong by a year.** GitLab 18.2 shipped **2025-07-17**, verbatim from GitLab's own release notes fetched this iteration (`https://docs.gitlab.com/releases/18/gitlab-18-2-released/`): "On July 17, 2025, GitLab 18.2 was released with the following features." Neither cited source dates 18.2, so the gloss is uncited as well as wrong, and it *understates* the exposure window — which is the direction that matters for "which of my instances are affected". It is also internally inconsistent: the entry's own affected list spans 18.2–18.11 plus 19.0/19.1/19.2, about fourteen monthly release lines, which cannot fit between mid-2026 and 2026-08-19.

**F5 — Medusa entry: "technology" is not a sourced victim sector.** Summary and body both read "across healthcare, education, legal, insurance, technology and manufacturing". The only sector list any cited source publishes is healthsystemCIO's: "That figure spans every sector the agencies track, including medical, education, legal, insurance and manufacturing." "technology" occurs zero times in CyberScoop and healthsystemCIO and, in The Record, only in site navigation and RSS payloads. Already propagated into the `malware:medusa` registry summary — fix both. (The frontmatter `sectors[]` tag is an org-relevance label and is not the defect.)

**F6 — Medusa entry: `actor:storm-1175` is a mislink that will corrupt that actor's timeline.** The body never mentions Storm-1175. No cited source ties the advisory to it; CyberScoop's only mention is an aside — "Earlier this year, Microsoft detailed how a group it dubbed Storm-1175 was making use of Medusa ransomware" — one tracked cluster among a RaaS affiliate population. And the store's own registry contradicts the link: `actor:storm-1175` records "from 2 August 2026 Microsoft observed it deploying a new strain, StormEncryptor", and `malware:stormencryptor` calls that "the actor's departure from the Medusa ransomware it had used previously". So a 2026-08-18 Medusa-ecosystem advisory would be filed against an actor the store says left Medusa on 2026-08-02. Drop the actor key and keep `malware:medusa`, or register the Medusa operation as its own actor.

### Analytical-link-as-fact

**F7 — Deep dive: "the campaign's leak-site batch already includes a Swiss and a Dutch organisation" is unsourced and reverses a caveat the store already published.** This is the sentence that carries the deep dive's constituency relevance, and it is cited to BleepingComputer. I fetched and extracted that article in full: "Swiss", "Dutch", "Switzerland" and "Netherlands" appear **zero** times. Same zero count across the ReliaQuest primary (`raw/reliaquest.txt`), the entry's only other source. Prior coverage is explicit in the other direction — `2026-08-13/clop-leak-site-names-44-victims-swiss-dutch-listings`: *"No named victim has confirmed a compromise, and no source links the named batch to the campaign."* The weekly `2026-08-16/weekly-w33-clop-windchill-status` records only that a vendor "assessed a possible relationship with this campaign" for a **different**, earlier batch of 42 *masked* listings, "while stating leak-site information alone cannot establish the access route for any listed organisation". The Swiss and Dutch names sit in the 44-listing *named* batch — precisely the batch prior coverage says is unlinked. The entry's own sourcing note even concedes the counts differ and "neither is a count of confirmed victims", then the body upgrades a hedged possible-relationship to "the campaign's leak-site batch". The run record repeats the inflation ("a campaign that already has a Swiss victim"). Restate as prior coverage does, and correct the run-record line.

**F8 — SharePoint entry: the title asserts the attribution the entry itself disclaims twice.** Title: "the SharePoint authentication bypass **that already breached Swiss federal and cantonal government** is now on CISA's exploited catalogue". Sourcing note: "linked here as estate context, not as attributed exploitation of this CVE — **no source ties either intrusion to this identifier**." Body: "neither publicly tied to this identifier by any source." Prior coverage goes further — `2026-08-13/sharepoint-cve-2026-55040-jwt-forgery-exploited-root-cause` says the two Swiss intrusions came "from a different set of flaws". The relative clause states the causation as settled, and the title is what renders at the top of the brief and drives the notification.

**F9 — Both KEV flips: ENISA is asserted to corroborate "independently"; the run's own evidence indicates derivation.** Claims: "ENISA's EU Vulnerability Database independently records the same exploitation date"; "two independent cataloguing authorities recording it on the same date"; "rests on CISA's catalog … and, independently, ENISA's EU Vulnerability Database". No cited source states independence, and the saved raw data is more consistent with ingestion of the KEV release:

- `raw/kev.json`: catalogue 2026.08.18, `dateReleased` 2026-08-18T16:52:08Z, adding exactly four CVEs — CVE-2026-33824, CVE-2026-59310, CVE-2026-55040, CVE-2026-65400.
- `raw/euvd-exploited.json`: all four of those records carry `dateUpdated` "Aug 18, 2026, 7:58:23 PM" / ":24 PM" — one batch write inside a single second, about three hours after the KEV release.
- Both Microsoft records' `exploitedSince` is "Aug 18, 2026, 12:00:00 AM" — a midnight date-copy equal to the KEV `dateAdded`, not a telemetry timestamp.
- The EUVD record for CVE-2026-55040 carries, in its nested vulnerability references, `https://www.cisa.gov/known-exploited-vulnerabilities-catalog?field_cve=CVE-2026-55040` — a pointer to the KEV catalogue itself.
- Both EUVD records still publish CVSS temporal metric `E:U` (Exploit Unproven) and contain no exploitation evidence, telemetry, actor or campaign — which the entries themselves concede.

Scoped deliberately: I am **not** claiming EUVD never has independent inputs — the vCenter record in the same batch carries a different `exploitedSince` (Aug 10). The finding is that for *these two* records the entries cannot assert independence. Drop "independently" and "two independent cataloguing authorities"; state that ENISA carries the same date and that its flag for these records appears to derive from the CISA catalogue. This also unwinds the credibility rating (F15).

### Quantifier without source

**F10 — Both WordPress entries: a negative finding attributed to Wordfence that Wordfence never published.** Forminator: "Wordfence reports no observed exploitation and no blocked attempts". User Profile Builder: "Wordfence reports no observed exploitation". Neither saved syndicated body contains "no observed", "in the wild", or any statement about observed exploitation or blocked-attempt counts in either direction. What the posts do carry is a product-protection claim the entries correctly excluded — Forminator: "All Wordfence users … are protected against any exploits targeting this vulnerability by the Wordfence firewall's built-in Malicious File Upload protection"; UPB: "Wordfence Premium, Wordfence Care, and Wordfence Response users received a firewall rule to protect against any exploits targeting this vulnerability on July 15, 2026." Absence of a statement is not a reported absence, and "no blocked attempts" is arguably contradicted by the firewall framing. Use the sourced formulation the summaries already have: no cited source reports exploitation, and NCSC-CH records the bundle's status as unknown.

**F11 — User Profile Builder entry: "a two-day vendor turnaround" (minor).** Source: "We provided full disclosure details to the Cozmoslabs team … on July 15, 2026. The developer acknowledged the report on July 16, 2026, and released the fully patched version **on the same day**." Vendor time is one day, and the source's own emphasis is same-day-as-acknowledgement; the entry's two days measures from Wordfence's 14 July intake, which is not vendor time. The same sentence renders the 15 July step as "validated on 2026-07-15" where the source says Wordfence provided disclosure details to the vendor that day.

### Strengthen primary source

**F12 — Metabase entry: `role: primary` is a commercial incident tracker relaying the vendor's operational content.** VenariX is right for the delta — the nine-victim count is genuinely its own work — but the load-bearing operational content is Metabase's and arrives second-hand: the two-request compromise indicator that drives action #2, and the entire remediation set. VenariX names the vendor lead itself ("GitHub Security Advisory GHSA-vwf4-m7j8-wcjf describes the issue as a critical, CVSS 10 SQL injection vulnerability in Metabase's password-reset functionality"; "According to the vendor advisory"). Add Metabase's own advisory as a corroborating record for the detection pattern and remediation guidance. I am deliberately **not** suggesting a URL: I tried `https://github.com/advisories/GHSA-vwf4-m7j8-wcjf` this iteration and the direct bridge returned an 86-byte empty body, so I have not confirmed a fetchable page and the run must verify before citing.

### Needs more research

**F13 — Keycloak entry: the package-state table it claims to have read carries a second affected product, with no fix.** From Red Hat's own security data (`https://access.redhat.com/hydra/rest/securitydata/cve/CVE-2026-18963.json`, fetched this iteration), `package_state` is: **"Red Hat JBoss Enterprise Application Platform Expansion Pack / keycloak-services / fix_state: Affected"** and "Red Hat Single Sign-On 7 / keycloak-services / fix_state: Not affected". A JBoss EAP Expansion Pack estate therefore carries this critical pre-auth account-takeover component with no erratum — a materially different position from "upgrade to 26.4.15 or 26.6.6", and it belongs in an entry whose sourcing note says the boundaries came from that very table.

*Explicitly cleared, because the operator asked for an independent re-derivation:* the **two-stream, four-errata claim is correct in every particular.** I fetched all four errata pages. RHSA-2026:56520 = "Critical: Red Hat build of Keycloak 26.4.15 Security Update", "New Red Hat build of Keycloak 26.4.15 **packages**", CVEs list = CVE-2026-18963 only. RHSA-2026:56519 = "26.4.15 **Images** Security Update", "New images … for Red Hat build of Keycloak 26.4.15 and … 26.4.15 Operator". RHSA-2026:56523 = "26.6.6 Security Update", packages. RHSA-2026:56524 = the 26.6.6 images/operator counterpart. Errata release dates 2026-08-18. The entry's mapping, the 26.4.15/26.6.6 versions, the package-vs-image split and the "updates only the package and keeps its existing image is not fixed" warning are all exactly right. Note for future iterations: Red Hat's `affected_release` array and its `?advisory=` query index both omit RHSA-2026:56520 (a text-only advisory), so an API-only check produces a false "fabricated advisory ID" signal — the errata page is authoritative. The entry also correctly makes **no claim in either direction** about upstream community Keycloak; Red Hat's page speaks only for its own build, and the one adjacent Red Hat statement in the data ("Red Hat Single Sign-On 7 — Not affected") is about a Red Hat product, not upstream. Quotes, CVSS 9.1, the vector, Critical rating and root-cause sentence all verify verbatim.

### Missed angles

**F14 — four sibling Keycloak CVEs in the same cited erratum, none in the store.** RHSA-2026:56523 closes five CVEs, not one: "FGAP v2 role groups endpoint discloses hidden group metadata without group view permission (CVE-2026-14613); Predictable account-linking hash enables account takeover via malicious OIDC client (CVE-2026-15571); Vault-resolved rotated client secrets leaked via Admin REST API (CVE-2026-17048); Unauthenticated account takeover via reset-credentials flow bypass (CVE-2026-18963); Privilege escalation via Time-of-Check to Time-of-Use (TOCTOU) vulnerability (CVE-2026-9796)". I checked `state/cves_seen.json`: CVE-2026-14613, CVE-2026-15571, CVE-2026-17048 and CVE-2026-9796 are all **NOT IN STORE**. Two are squarely on the entry's own theme — a second account takeover (via a malicious OIDC client) and a client-secret leak through the Admin REST API, on the component that fronts SSO for this constituency. Red Hat's API dates CVE-2026-15571 at `public_date` 2026-08-18T19:41:18Z, in window. The same data also shows an asymmetry worth a sentence: RHSA-2026:56520 (26.4.15) closes CVE-2026-18963 alone, so the 26.4 and 26.6 upgrade decisions are not equivalent. A paragraph in the existing entry closes this; no separate entry needed. Re-read with `python3 tools/fetch_source.py url 'https://access.redhat.com/errata/RHSA-2026:56523' --direct`.

### Classification missing / inconsistent

**F15 — `credibility: 1` on three entries the corroboration does not support.** Affected: `cve-2026-33824-ikeext-kev-confirmed-exploited`, `cve-2026-55040-sharepoint-kev-exploitation-confirmed`, `metabase-downstream-victims-nine-credential-rotation`.

- The two KEV flips: with the independence claim withdrawn (F9), the exploitation determination rests on one authority (CISA KEV), the third cited source (Microsoft) contradicts it, and both EUVD records still carry `E:U`. An uncorroborated, vendor-contradicted determination is credibility 2. Reliability A is correct — CISA KEV is on the national-CERT carve-out list and `sources/sources.json` rates it A.
- Metabase: reliability C is well argued, but the two central claims — the count of nine, and the placement of each newly named organisation in this campaign — are VenariX's alone, and the entry says so ("that association is VenariX's assessment, not a statement by the named organisations"). The DataBreaches corroboration covers only that Bits of Gold was breached via "a third-party data analytics network", explicitly not that it was Metabase. Credibility 2.

The run record already articulates the correct test at line 253 — "the several publishers relay one assessor, so both sets of entries carry a credibility of 2 rather than 1" — and applies it to the Wordfence and Medusa entries but not to these three. For contrast, the Medusa entry's **B/1 is defensible and should be left alone**: two editorially independent journalists each read and quoted the same primary. No reliability letter is out of line with `sources/sources.json` anywhere in the run.

### Editorial / less-is-more flags (advisory)

**F16 — IKEEXT entry: unexplained actor entity on a confirmed-exploitation flip.** `entities: [actor:knaithe-knyuan]` against a body that says "neither names an actor, so nothing here supports an attribution". The link is defensible continuity (prior entry 2026-08-10 carries it because this CVE was one of four that campaign reached), but on an entry whose headline event is the exploitation flip it invites a downstream timeline to read the flip as that actor's activity. The sibling SharePoint entry solves the identical problem with one line. Cheap to add; acceptable to leave.

**F17 — run record: "Phase 2" in `sub_agents.F1.note`.** Workflow-internal language in a published file. It sits in telemetry frontmatter rather than the verification-notes body, so it is outside the strict letter of the style rule, but it renders on the ops surface. "after the surfacing passes returned" would close it. Acceptable to leave.

---

## What I checked and found clean

Recorded so the confirmation pass can spend its budget elsewhere.

**Quote integrity — no splices, no re-hedging, no ellipsis.** Every `evidence[]` quote in all eleven entries is a contiguous verbatim substring of a page I read this iteration. Verified by literal substring match: ReliaQuest ×10 (incl. the four `evidence[]` and six inline), Check Point ×7, Insikt ×6, Wordfence-via-mirror ×4 + ×4, The Record ×5, CyberScoop ×2, VenariX ×3, BleepingComputer ×2, CISA KEV ×2, MSRC ×3, GitLab ×2, Red Hat ×2. The Wordfence quotes all sit **inside** the syndicated body, not in malware.news's interleaved marketing — I checked positions against the marketing blocks specifically (e.g. the Forminator precondition quote lands at offset 6379, immediately before the "IMBT Course" affiliate block at ~6440). The one body quote with a typographic difference is ReliaQuest's "its queries run under the application's existing database identity…", where a curly apostrophe was normalised to straight — not a defect, and it is an inline quote rather than an `evidence[]` record. The Philips statement in the deep dive reproduces the source's zero-width characters exactly.

**Deep-dive attribution hedge and IOC hygiene.** ReliaQuest's "This activity was highly likely conducted by the Clop extortion group" is verbatim, and the hedge is preserved in the title ("attributing it highly likely"), summary, body and sourcing note — never upgraded. The three attribution bases the sourcing note claims are exactly the three the report lists (extortion-email addresses matching the live leak site; the `X-windchill-req` header previously linked to Clop in external reporting; TTP alignment with mass-exploitation-then-custom-web-shell). The "moderate confidence" copycat expectation and the "high confidence" expansion assessment are both correctly graded. **No IOC leaked anywhere in the run**: I regex-swept all eleven entries for 32–64-char hex, IPv4, and defanged indicators — zero hits, despite ReliaQuest publishing a hash-and-address table. The class names, header name and `flst.txt`/`ieStructProperties.txt` filenames carried are behavioural artefacts from ReliaQuest's own hunt guidance, not banned indicator classes. Vendor product-efficacy sections were excluded everywhere I checked them: ReliaQuest's GreyMatter DRP paragraph, Check Point's "Protections" section, both Wordfence firewall claims, the bug-bounty amounts ($2,048 and $975), BleepingComputer's trailing "37% blocked / 338 million simulations" promo, and the $10M State Department reward.

**StopAndProtect numbers all verify** — and my first pass mis-flagged them before I checked the source's formatting. "close to 2,000 compromised WordPress domains" ✓, "approximately 31,000 screenshots" from "mid-May to the end of July 2026" ✓, "more than 700 archives" over the same window ✓ (entry softens to "roughly 700"), "Statistics as of 24/07/2026 – more than 6000 unique IP addresses" ✓ with the US/RU/IN ordering matching the published table (1852/630/630) ✓, and the partial-data caveats ✓ ("some of the logs belong to various sandboxes and researchers' machines"; "the ClickFix log file was reset a few times"). `malware:silentencryptor` is grounded — "SilentEncryptor is the ransomware component" — and the per-file key derivation the entry describes matches the report. No name collision with the store's `malware:stormencryptor` (distinct names, distinct keys, distinct actors).

**The SharePoint entry should exist.** I challenged it as instructed. The covered entry `2026-08-13/…-jwt-forgery-exploited-root-cause` carries `status: [poc-public, patch-available]` with a sourcing note that explains the choice ("Exploitation status in this entry's cves[] record stays poc-public rather than exploited for exactly that reason" — Defused observing its own honeypots, not vendor- or catalogue-confirmed). The KEV listing is therefore a genuine machine-readable state change, not bookkeeping, and it carries a 2026-08-21 federal remediation deadline. Keep it. Fix the title (F8).

**Both KEV additions the run dropped were correctly dropped.** The run record claims CVE-2026-59310 and CVE-2026-65400 are already carried as exploited. Verified against `state/cves_seen.json`: the vCenter record reads "confirmed actively exploited from 2026-08-03, 361 victim IP addresses across 47 countries" and the macOS record "records active abuse observed on multiple systems with port 5900 reachable from the internet". Bookkeeping, correctly shipped as nothing.

**Priority calibration holds; I could not argue any `high` down.** Seven `high` in this window is a property of the window. The two KEV flips are confirmed-exploited pre-auth paths (EPSS 55.85 on the IKEEXT one, a three-day federal deadline on both). GitLab clears the vulnerability bar on the explicit "another out-of-band response" ground — the vendor broke its own twice-monthly cadence, and the flaw is pre-auth with integrity-and-availability impact on self-managed infrastructure. Keycloak is a pre-auth path to administrator on an internet-facing identity provider. Metabase is an exploited CVSS 10.0 with nine downstream breaches and a credential-rotation gap that survives patching. The Clop deep dive is an active mass-extortion campaign with a newly published mechanism. Forminator sits closest to the line — no exploitation, and a form-level precondition — but pre-auth upload-to-RCE on 600,000 installs two days after the root cause went public, in a constituency with in-window WordPress-compromise history, earns it; and the run's differentiation from the `notable` sibling (estate size, precondition class) is coherent rather than arbitrary. No `critical` is correct: both exploitation flips have patches months old and neither carries a mass-exploitation report.

**All four borderline drops upheld; I tried to overturn each.** The Entra directory-theft brief clears none of the four higher grounds for an out-of-nexus breach (unverified vector, no platform vulnerability, generic password-spray/MFA-fatigue guidance). The Mandiant agentic harness is the vendor-capability-announcement class check 7 exists to catch. The Zurich court reporting is the strongest candidate — it has a home-region nexus, which lowers the relevance bar — but day-one procedural detail (admissibility submissions, a denial, an allegation about a principal said to have died in 2022) changes no control, and queueing the 2026-09-10 verdict is the right call. The two Royal Elementor flaws I verified independently against the NCSC-CH advisory text: "Royal Elementor flaws require Contributor-level access or higher" — post-auth SSRF at 8.8 and stored XSS at 6.4 with no exploitation are routine patch-cycle items. The three SOHO-networking CVEs (third-party-assigned scores, no vendor advisory, EPSS 0, no constituency presence) likewise.

**Coverage otherwise looks complete.** The only in-window gap I can name and evidence is F14 (the Keycloak sibling CVEs, surfaced from the run's own primary). The unreachable-source list in the run record is accurate and each entry has a mitigation or an honest "none available"; I re-confirmed cisa.gov HTML 403, the reader pool at 402, and that the KEV JSON feed path works. Nothing in the fetch-failure ledger points at a story the window should have carried.

**Mechanics.** All 22 inline URLs resolve and land on specific articles, advisories or per-record API paths — no homepage, listing index, news category, or NVD/MITRE/cve.org per-CVE page anywhere. All four `update_of` targets exist on disk and are the same story with a genuine delta. All twelve referenced registry keys resolve, none tombstoned. CERT-FR CERTFR-2026-AVI-1037 verifies as a GitLab advisory dated 18 août 2026 sourced to "Bulletin de sécurité GitLab du 17 août 2026", with matching affected ranges. The Hacker News article does cover the User Profile Builder flaw as well as Forminator (CVE-2026-15826, 40,000, 3.16.5 and `absint` all present), so its citation on that entry is legitimate — I checked because I suspected otherwise. `org_triage` is null on all eleven, `watchlist_hit` false on all eleven, no `watchlist` tag: correct for a profile with neither configured. No `techniques[]` is empty. English throughout; no vanity metrics; the only "spawn" occurrences are legitimate process-lineage prose.

**Action-item discipline is strong — no F-code raised.** Zero to two actions per entry, none above the ~3 bar. Every action is concrete and derived from its own entry's mechanics (verify the April CU on IKEv2 responders; the exact three SharePoint build numbers; the four Keycloak stream/image targets; the Forminator File-Upload-plus-Select form pairing; `wp-content/mu-plugins` enumeration; the Windchill codebase JSP review against ReliaQuest's named classes; the two-request Metabase log pattern). No generic advice, no body restatement, no hedged non-tasks. `actions: []` on the Medusa advisory entry is the right call. I cross-checked the four update entries against their prior targets' `actions[]` for duplication and found none — the Metabase pair in particular is a scanner sweep then a credential rotation, genuinely different work.

**Contradictions are already surfaced, so no surface-contradiction finding.** The deep dive's sourcing note carries the 43-vs-44 victim-count divergence and states neither is a count of confirmed victims; both KEV entries carry the Microsoft-versus-catalogue disagreement in the body and treat it as the operationally useful part. That is the right handling.

### Verdict

**NEEDS_FIXES (truth: 11, editorial: 4, advisory: 2)**

The run is well built — sourcing notes that argue their own weaknesses, quote discipline that holds under literal substring checking, no IOC leakage, correct rejection of every borderline item, and a Keycloak errata read that I could not fault after re-deriving it from four separate vendor endpoints. The defects cluster in one place: **claims about relationships between things, made in titles and framing sentences rather than in the carefully-sourced bodies.** F7 and F8 are the two that must not ship — the deep dive's Swiss/Dutch sentence reverses a caveat this store published twice, and the SharePoint title asserts an attribution the same entry disclaims in two other places. F9 and F15 travel together: withdraw the independence assertion and the credibility-1 ratings follow. F4, F5, F6 and F10 are discrete factual corrections. F1 and F2 are the Medusa entry's framing versus its own sources. Nothing here requires dropping an entry or restructuring the window.

### Findings summary (machine-readable)

See `work/2026-08-19T0410Z-intel/verification.iter1.findings.yaml` (identical payload, clean parse target).
