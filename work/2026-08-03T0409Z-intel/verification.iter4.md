**Model:** Sonnet 5 (`claude-sonnet-5`)
**Timestamps:** started_at=2026-08-03T05:45:23Z · ended_at=2026-08-03T05:55:17Z · duration_seconds=594

## Verification report — 2026-08-03T0409Z-intel (iteration 4)

Cold read of the three published entries and the run record, independent of iterations 1–3. Re-verified all 32 Bouncy Castle CVE records against NVD live (all 32 CVSS scores/vectors match exactly, including the four records edited in iteration 3), re-fetched all six Gladinet VulnCheck advisories (all six CVE ids/scores/version ranges/mechanics match), re-fetched the N-able blog, the N-able status page and the Huntress post (including its 3 August update) live, and re-confirmed the CISA KEV catalog directly for the "three earlier CentreStack CVEs in the exploited catalog" claim (confirmed: exactly three KEV entries name CentreStack — CVE-2025-30406, CVE-2025-11371, CVE-2025-14611 — the fourth Gladinet KEV entry, CVE-2025-12480, is scoped to Triofox only). Also re-confirmed the previously-corrected maintainer advisory-index mixup (CVE-2026-58062 and CVE-2026-58063 each now show their own content) and the release-notes citations for the strict-DigestInfo property and the OCSP/DH/name-constraint CVE one-liners (a direct fetch reached content that WebFetch's own summariser truncated on first pass — no defect, just a fetch-ladder note for future iterations: this raw.githubusercontent.com "main"-branch URL is long enough that WebFetch may need a second, more targeted prompt or a direct curl to reach content past the fold).

Two new truth findings, both in the N-able entry, neither found by iterations 1–3.

### Citation does not support the claim

- **F3** — entry: `2026-08-03/cve-2026-18577-n-able-n-central-auth-bypass-exploited`. Sentence: "N-able first addressed the issue in 2026.2 and, on 1 August, told customers still on older builds to move to 2026.3 as an immediate protective measure — its initial advisory tied the exploitation to CVE-2026-18556, while the hotfix that followed pointed at CVE-2026-18577 ([Huntress, 2026-08-03](https://www.huntress.com/blog/n-able-vulnerability-exploitation))." The single trailing citation is Huntress. Live-fetched the Huntress post and searched it specifically: it never mentions "2026.2" at all, and its only upgrade guidance is to the 2026.3.1.7 hotfix — it does not state that N-able recommended upgrading to 2026.3 (without the hotfix suffix) at any point. Huntress supports only the sentence's final clause (the CVE-2026-18556/CVE-2026-18577 attribution shift), which its text does state verbatim: "N-able's initial security advisory linked this critical vulnerability to CVE-2026-18556; while the subsequent hotfix pointed to CVE-2026-18577." The "2026.2" and "move to 2026.3" facts are true and are stated by N-able's own blog — live-fetched and confirmed: "We had addressed this issue in later builds and were recommending that customers on older versions upgrade to version 2026.3 as an immediate protective measure," and separately "which was fixed in 2026.2" — but that source (N-able, cited earlier in the same paragraph for a different sentence) is not the one attached to this sentence. This is the adjacency-check shape the org profile specifically calls out as the dominant residual defect class: a true fact cited to a co-cited source that does not carry it. Remediation: reattach the N-able citation to the 2026.2/upgrade-to-2026.3 clause (it is already cited earlier in the same paragraph, so this is a citation-placement fix, not a missing-source problem), keeping the Huntress citation only on the CVE-attribution clause it actually supports.

### Unsupported / hallucinated facts

- **F4** — entry: `2026-08-03/cve-2026-18577-n-able-n-central-auth-bypass-exploited`. Three places in the entry name a specific service identifier: body paragraph 3 ("in service-installation telemetry the durable artifact is an unexpected `Cloudflared` service"), the Triage section ("A Cloudflared service registration is the sharper signal, because a remote-support session that legitimately used Take Control has no reason to leave a tunnel behind"), and action item 2 ("check the endpoints those sessions touched for an unexpected Cloudflared service registration"). Live-fetched both the N-able blog and the Huntress post and searched each specifically for the string "Cloudflared" (the actual Cloudflare Tunnel client binary/service name). Neither source contains it. N-able's own account, verbatim: "the attackers registered a new service for a CloudFlare tunnel, enabling persistence into an environment after access to the N‑central server was revoked" — it names no specific service identifier. Huntress's account, verbatim: "deploy Cloudflare-based tunnels for persistence" — same, no specific service name. Both sources describe a generic "service for a Cloudflare tunnel" without ever naming it "Cloudflared." The entry presents "Cloudflared" in backtick code formatting three times, including as the detection artifact a reader is told to hunt for by name and as the discriminator that decides a Triage call — an operator who searches literally for a service named "Cloudflared" and finds a different name (the actual Cloudflare Tunnel client can run under an arbitrary/renamed service name, and neither cited source rules that out) could wrongly conclude the host is clean. This is invented specificity beyond what either cited source states, in the load-bearing detection/triage/action-item surfaces of the entry. Remediation: replace "Cloudflared" with the sources' own generic phrasing ("an unexpected service registered for a Cloudflare/CloudFlare tunnel" or similar) in all three locations, or attribute the specific name to a source that actually states it if one exists and cite it.

### Verdict

**NEEDS_FIXES (truth: 2, editorial: 0, advisory: 0)**

Both findings are in the N-able entry only. The Bouncy Castle entry (all 32 CVE records re-verified against NVD live, including the four iteration-3-edited records, all exact matches; FIPS build numbers and BC-LTS applicability re-confirmed against the per-CVE wiki pages; the maintainer's advisory-index mixup correction re-confirmed live) and the Gladinet entry (all six CVE records re-verified against their VulnCheck advisories live — scores, vectors, ranges, mechanics all match; the three-KEV-entry count re-verified directly against the CISA KEV catalog; the out-of-window publication's honesty controls — real `event_date`, sourcing-note disclosure, body opening disclosure — all hold; the sourcing_note's scores-are-displayed correction from iteration 3 confirmed accurate) show no new defects. The `critical` rating on the N-able entry remains defensible on all three elements (newly disclosed, actively exploited, time-critical). Both `single-source` gradings (Bouncy Castle, Gladinet) and their Admiralty classification codes (A/2 and B/2 respectively, matching `sources/sources.json`'s own B rating for VulnCheck) check out; the N-able entry's `multi-source`/A/1 grading is consistent with its independent Huntress corroboration. No missed angles: cross-checked the run's own research telemetry (`findings.S1–S4.yaml`) against the run record's Published/Borderline-drops sections and found full accounting — nothing surfaced by research was silently dropped, and the CISA KEV "no addition since 2026-07-29" quiet-window claim re-confirmed directly against the live KEV feed.

### Findings summary (machine-readable)

```yaml
- code: F3
  category: claim-not-supported
  section: operational
  item: "CVE-2026-18556 / CVE-2026-18577 — N-able N-central: unauthenticated admin access to the RMM console, exploited in the wild, and the day-one fix was itself bypassable"
  url_or_quote: "N-able first addressed the issue in 2026.2 and, on 1 August, told customers still on older builds to move to 2026.3 as an immediate protective measure — its initial advisory tied the exploitation to CVE-2026-18556, while the hotfix that followed pointed at CVE-2026-18577 ([Huntress, 2026-08-03])"
  summary: "The trailing Huntress citation supports only the CVE-attribution clause (confirmed verbatim on the live page); Huntress never mentions '2026.2' and never recommends upgrading to plain 2026.3. Those facts are true and stated by N-able's own blog (cited earlier in the same paragraph for a different sentence: 'We had addressed this issue in later builds and were recommending that customers on older versions upgrade to version 2026.3 as an immediate protective measure' / 'which was fixed in 2026.2') but the citation attached to this sentence is the wrong co-cited source."
- code: F4
  category: hallucinated-fact
  section: operational
  item: "CVE-2026-18556 / CVE-2026-18577 — N-able N-central: unauthenticated admin access to the RMM console, exploited in the wild, and the day-one fix was itself bypassable"
  url_or_quote: "in service-installation telemetry the durable artifact is an unexpected `Cloudflared` service"
  summary: "Live-searched both cited sources (N-able blog, Huntress post) for the string 'Cloudflared'; absent from both. N-able says only 'the attackers registered a new service for a CloudFlare tunnel'; Huntress says only 'deploy Cloudflare-based tunnels for persistence'. Neither names the service 'Cloudflared'. The invented specific name appears three times: body detection paragraph, the Triage discriminator, and action item 2 — all load-bearing hunt/triage guidance."
```
