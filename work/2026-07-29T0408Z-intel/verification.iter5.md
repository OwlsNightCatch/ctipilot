**Model:** Opus 5 (`claude-opus-5`)
**Timestamps:** started_at=2026-07-29T06:29:33Z · ended_at=2026-07-29T06:40:07Z · duration_seconds=634
**Self-telemetry:** urls_checked=14 · webfetch_calls=5 · bridge_fetches=7 · websearch_calls=0

## Verification report — 2026-07-29T0408Z-intel (iteration 5)

Read cold. All eleven entries read end-to-end (frontmatter + body), plus the run record, `entities/registry.yaml`, `state/cves_seen.json`, `sources/sources.json` and the deep-dive category of every entry in the trailing 30 days. Per the spawn brief the pass was weighted toward (a) the run record's statements about itself and (b) remediation debt from the 31 edits already applied; entry-level facts earlier iterations confirmed were not re-litigated except where a figure or a late edit made them doubtful.

### What was independently re-verified sound this pass

- **Deep-dive rotation (iteration 4's single fix).** Recounted from every entry's own `deep_dive_category`. The 30-day window (2026-06-29 → 2026-07-28) holds exactly **13** deep dives, none of them `annual-report` — so "none of the 13 deep dives in the prior 30 days was an annual-report treatment" holds. The last six are 2026-07-18 `firewall-vpn-rce`, 07-19 `other`, 07-20 `network-stack-rce`, 07-21 `identity-infra`, 07-24 `apt-campaign`, 07-25 `apt-campaign` — so "the six picks in the trailing week were firewall-vpn-rce, other, network-stack-rce, identity-infra and apt-campaign twice" is exact, and `apt-campaign` twice is correct. No deep dive on 26, 27 or 28 July. **Iteration 4's fix is correct and introduced no new debt.**
- **KEV claims.** Full catalog pulled via `tools/fetch_source.py cisa-kev` (1655 entries). Exactly five Langflow records — CVE-2026-0770 (2026-07-21), CVE-2026-55255 (2026-07-07), CVE-2025-34291 (2026-05-21), CVE-2026-33017 (2026-03-25), CVE-2025-3248 (2025-05-05) — matching the Langflow entry's sourcing note verbatim and in full. CVE-2026-0769 absent, CVE-2026-5027 absent, CVE-2026-16232 present. The load-bearing claim of that entry is verified.
- **Per-CVE authority checks** via `cveawg.mitre.org`: CVE-2026-59243 → `CVE_RECORD_DNE` (so `cvss: null` is right and the sourcing note exact); CVE-2026-63077 → JetBrains CNA, `datePublished 2026-07-27T16:44:32Z`, CVSS 9.8 with AV:N/AC:L/PR:N/UI:N, affected `0` to `<2026.1.3, 2025.11.7` with `defaultStatus: unaffected` — the entry's `affected` prose matches the structured block clause for clause; CISA-ADP layer dated **2026-07-28** with `Exploitation: none`, exactly as the sourcing note states. CVE-2026-0769 → ZDI CNA, `datePublished 2026-01-23` (matching the cited source date), CVSS 9.8, sole affected version 1.3.2 with `defaultStatus: unknown`; CISA-ADP layer dated **2026-02-26** with `Exploitation: none`, exactly as the sourcing note states.
- **Iteration 3's Mirage Kitten tool-conflation fix.** Kaspersky re-fetched. BridgeHead alone carries the SOCKS5 tunnel proxy, the HTTP 407 scheme query via `WinHttpQueryAuthSchemes`, Negotiate-before-NTLM order, null-credential SSO, the 60-second-capped backoff and the 3-character username gate. ArcBridge is two commands (OPEN, DNS) with a config block of host, port, retry, SSL flag and implant id, and the page claims none of BridgeHead's mechanics for it. `AppVShNotify.exe` is described as **legitimate**, never signed. The command table lists 16 ids (1, 3, 17, 20, 25, 27, 30, 36, 43, 56, 62, 69, 70, 75, 90, 93) — the entry's "16 numeric commands" and the run record's correction are both right. The alias sentence, TWOSTROKE/GTIG, Retrograde↔MiniFast/MiniUpdate, NetSetup.log and the victim list all check out. **No orphaned citation or broken wording survived that rewrite**, and the T1016 swap (iteration 3 F7) is supported by the surviving NetSetup.log text.
- **Iteration 1's Minnesota citation split.** Cybersecurity Dive re-fetched raw (263 KB). "impacted certain automated controls" / "no major impact to drinking and wastewater treatment operations" are on that page, cited to that page. The sourcing note's `"no usage changes requested"` framing is supported verbatim: "There is no evidence that local officials have instructed residents to modify their usage of drinking water, MNIT said." The Cybersecurity Dive `evidence[]` quote ("The two-day attack comes days after federal officials warned of state-linked threat groups targeting a wider set of industrial devices.") is a **contiguous verbatim substring** of the page — it is the article's own dek/meta-description line; the prose paraphrase elsewhere on the page is a different sentence and does not make the quote non-verbatim. Checked because the summariser's prose rendering disagreed with the quote; the raw grep settles it.
- **VulnCheck evidence quote** re-fetched raw: "With LangFlow, we've seen attackers gain initial access using exploits targeting both CVE-2026-0769 and CVE-2026-5027, harvest credentials, likely for services such as OpenAI and Claude, deploy cryptominers, and attempt lateral movement. Neither of these vulnerabilities have been added to CISA KEV." — contiguous and verbatim, both sentences.
- **`poc-public` on the Check Point update.** Rapid7's page links its PoC at `https://github.com/sfewer-r7/CVE-2026-16232`, so the tag and CVE status are supported, not inferred. Take 146 → Take 158, the `gen-sso-token` special case, TCP 18190/19009, and the `CN=siclocal` loopback narrowing are all on the page as the entry states. The 9.1-is-NVD / 9.3-is-PSIRT provenance matches the 2026-07-23 entry it updates.
- **Talos deep-dive figures**, all against the source: 65% vs 35% authentication abuse; 42% vs 18% insufficient logging; 31% exposed/unpatched infrastructure (third-ranked); almost 15% outbound-email thresholds; 6,600+ messages from one mailbox; 80+ ARToken API endpoints; 90 days retention; ~3 days dwell; healthcare 17%, public administration and manufacturing 14% each. No inflated or invented quantifier found.
- **Sophos STAC4749 figures**: all-IP-law legal victims, 90 s–20 min call durations, 1,000 concurrent connections, RemSupp-preference-from-April with the blocklist rationale, `msconfig`/RDP, the MuddyWater non-link with Rapid7 as the false-flag proposer, Chaos-from-BlackSuit(Royal), the `.top` domains, PyArmor/GitHub-hosted AES key, Realtek/WinAudio/SecurityHealth/OneDriveUpdate, <17 hours. The entry's "94%" is the exact sum of the source's Canada 50% + US 44% (which the source itself rounds to "nearly 95%") — arithmetically faithful, not a defect.
- **Registry and state.** All eleven `entities_added` keys resolve in `entities/registry.yaml`, including the renamed `incident:uvvg-arad-cyberattack-2026-07` (iteration 1 F14 / iteration 2 F4 both hold). `tool:talos-artoken-eviltokens-bec-panel` resolves. `state/cves_seen.json` carries all eight of this run's CVEs with correct `first_seen`, and CVE-2026-5027 → 2026-06-11, CVE-2026-0770 → 2026-07-22, CVE-2026-26980 → 2026-05-25, matching every prior-coverage date the run record and entries assert.
- **Run-record self-claims otherwise verified**: 15 essential-tier sources in `sources/sources.json`, and all 15 attempted across the two relevant slices with none in `fetch_failures` — "No miss" holds; the "seventeen major research labs" count is exact; "sixteen targeted German- and French-language searches" matches the recorded 16 websearch calls on that slice; the already-tracked French-language Swiss daily is `dcod-ch`, status candidate, `last_successful_fetch: 2026-07-22`, note recording it as proposed on 2026-07-22 — the claim is exact; 11 entries = 5 vulnerability + 2 threat + 1 research + 2 incident + 1 periodic report, matching the composition sentence; `entries_updated: 1` matches the single `update_of`; no entry is `critical`, matching "No entry reached the critical bar"; six borderline-drop bullets match "six candidates were dropped at the gate".
- **Style discipline**: zero hashes, zero IPs, zero attacker domains, zero rule code across all eleven entries and the run record. Workflow-internal language appears only in frontmatter telemetry keys (the documented machine contract); the published notes body is clean of it. English throughout. No `watchlist_hit: true`, no `watchlist` tag, no `org_triage` block on any entry — correct for this profile. Every entry carries a valid Admiralty `classification` block, and each letter/number is consistent with its sourcing (A only where a first-party vendor/PSIRT is the primary; credibility 1 only on the two genuinely corroborated entries).
- **Coverage completeness**: I looked for an in-window gap and did not find one I can name a plausible source for. The sekoia / ransom-isac / apple-security failures are each documented with a mitigation, and the Apple recovery through two national CERTs is a defensible reason not to carry that batch. No F10.
- **Priority calibration**: the four `high` vulnerability entries each clear the beyond-patch-cycle bar (all-versions pre-auth RCE on a build server; unpatched V7 family with public exploit; observed exploitation with no fix and no KEV entry; shipped-default full auth bypass), the Check Point update is `high` on active exploitation plus a default-configuration finding, Minnesota and STAC4749 are `high` on sector nexus and a single-shift containment window, and the four `notable` items are correctly below the TL;DR line. Nothing is mis-tiered in either direction; no F16.
- **Iteration 4's `priv-esc` advisory**: I agree with leaving it. The technique crosses an account boundary the attacker holds no credentials for, the entry says so explicitly in its own sourcing note, and the `vulnerabilities` tag — the one that would actually have contradicted the body — is correctly absent. Settled.

### Unsupported / hallucinated facts

**F1 — run record, § Single-source items and carve-outs: the single-source count is one short, and the omitted entry is disclosed nowhere in the section.**

The record states, verbatim:

> Four entries publish as single-source original research with no second lab reporting: the Mirage Kitten toolset (Kaspersky), the STAC4749 cluster (Sophos, which notes its custom-malware chain has not been reported elsewhere), the Check Point root-cause analysis (Rapid7), and the Talos quarterly report. Each carries a sourcing note explaining what rests on the single source.

`entries/2026-07-29/legacyhive-offline-registry-hive-profile-hijack-no-fix.md` carries `verification: single-source`, exactly one `sources[]` record (LevelBlue SpiderLabs, role primary), and a sourcing note opening:

> Single-source: LevelBlue SpiderLabs' own analysis and independent reproduction of a publicly released proof-of-concept.

That is the same class the sentence enumerates — single-source original research with no second lab reporting — so the count is **five**, not four. Enumerated against the artefacts, seven of the eleven entries carry a `single-source*` verification value (check-point `single-source`, teamcity `single-source`, legacyhive `single-source`, mirage-kitten `single-source`, stac4749 `single-source`, talos `single-source`, uvvg `single-source-victim`); the section accounts for six of them. Grepping the whole run record for LegacyHive returns three hits — the registry key in `entities_added`, the iteration-3 F9 finding record, and the recency-disclosure bullet — and none of them discloses that the entry rests on one source. A reader auditing the run's single-source exposure from the run record therefore gets an incomplete answer, which is the exact purpose that section serves.

Remediation: change "Four entries" to "Five entries" and add the LegacyHive analysis (LevelBlue SpiderLabs) to the enumeration. Nothing in the entry itself needs to change — its `verification` value and sourcing note are already correct.

**F2 — `sources/sources.json`, record `siemens-productcert-csaf`: the note still carries the count iteration 3 corrected in the run record, so the two artefacts of this run now contradict each other.**

The source record's `notes` field reads, verbatim:

> Added 2026-07-29 as this run's one new candidate: three published entries in the trailing 30 days cite cert-portal.siemens.com, and the pipeline currently reaches Siemens advisories only through CISA's ICS republication …

I recounted independently: exactly two **prior** published entries cite that host — `entries/2026-07-10/siemens-sicam-8-ssa-229470-firmware-signing-bypass.md` (`cert-portal.siemens.com/productcert/html/ssa-229470.html`) and `entries/2026-07-18/siemens-ruggedcom-rox-ii-unit42-three-cve-chain.md` (`cert-portal.siemens.com/productcert/html/ssa-081142.html`) — the third citation being this run's own Desigo CC entry. That is precisely iteration 3's F3, whose remediation is recorded as "Corrected in both the source-change record and the notes body, naming the two prior entries explicitly." The run record now reads:

> Added on evidence rather than speculation: two prior entries in the trailing 30 days already cite cert-portal.siemens.com — the SICAM 8 firmware-signing advisory of 2026-07-10 and the RUGGEDCOM ROX II chain of 2026-07-18 — with this run's own Desigo CC entry making a third

The fix reached the run record's `sources_changed` block and its notes body but not the `sources/sources.json` record, which is the artefact that outlives the run and is the one a future run reads when deciding whether to promote the candidate. It still states the addition rationale in the wording iteration 3 ruled defective, and it now disagrees with the run record of the same fire on the same figure.

Remediation: reword the `notes` field to match the corrected run-record statement — two prior published entries in the trailing 30 days, plus this run's own Desigo CC entry as a third. The addition rationale is unaffected: it rests on the two-week latency on SSA-734552/SSA-814963, which is verified and unchanged.

### Editorial / less-is-more flags (advisory)

**F3 — run record, `fetch_failures[sekoia].mitigation_applied`: the mitigation sentence claims a full sweep of a set that contains the failed source.**

> The remainder of the research slice was swept in full, including the seventeen major research labs, and no in-window item surfaced anywhere that pointed at a Sekoia publication.

The count of seventeen is exact and correct (mandiant-gtig, google-tag, talos, unit42, checkpoint-research, kaspersky-securelist, proofpoint, trendmicro-research, crowdstrike, recordedfuture-insikt, group-ib, zscaler-threatlabz, rapid7-research, sophos-xops, volexity, sekoia, malwarebytes — verified against the S3 attempted list), but sekoia is one of the seventeen, so "the remainder … including the seventeen major research labs" asserts a complete sweep of a set whose one unswept member is the subject of the record. "sixteen of the seventeen" would be exact. The neighbouring `sources_changed` line ("the majority of the seventeen major research labs") is already correct. Advisory — the substantive mitigation claim (no in-window item pointed at a Sekoia publication) is unaffected and I found nothing to contradict it.

**F4 — `2026-07-29/stac4749-teams-vishing-certificate-pinned-golang-chaos`: RemSupp missing from `affected_products`.**

> affected_products: ["Microsoft Teams", "Microsoft Quick Assist", "AnyDesk", "DWAgent"]

RemSupp is the tool the entry singles out as the operationally novel substitution ("from April 2026 RemSupp by preference") and the one its own second action item instructs the reader to add to application control ("Add RemSupp to the remote-support tooling your application control blocks or alerts on"), yet it is the only abused remote-support tool absent from the list while the three less-emphasised ones are present. Sophos names it explicitly ("deployed the cloud-based RemSupp remote monitoring and management (RMM) tool as a fallback … activity since April suggests the threat actors began to prefer RemSupp"), so it satisfies the "a product the cited sources actually name" test. An automated consumer matching an alert against `affected_products` would miss the tool the entry itself prioritises. Advisory — the body and the action item both carry the name, so a human reader loses nothing.

### Verdict

NEEDS_FIXES (truth: 2, editorial: 0, advisory: 2)

Both truth findings are single-line corrections to statements the run makes **about itself**, not to any entry's technical content — consistent with the pattern the spawn brief flagged, and F2 in particular is unremediated debt from iteration 3's own fix reaching two of the three places the claim lives. Entry-level truth is stable: every URL I checked resolved to a specific advisory or article, every quantifier I checked traced to its source, every evidence quote I raw-grepped was contiguous and verbatim, and the two heavy late rewrites (Mirage Kitten's tool split, Minnesota's citation split) left no orphaned citations or weakened arguments. Coverage looks complete — I could not name an in-window omission with a plausible source behind it. If the two truth findings are applied, I would defend this run in full to a hostile Tier 2/3 reader.

### Findings summary (machine-readable)

```yaml
# Findings summary (machine-readable)
- code: F1
  category: hallucinated-fact
  section: run-record
  item: "runs/2026-07-29/2026-07-29T0408Z-intel — § Single-source items and carve-outs"
  url_or_quote: "Four entries publish as single-source original research with no second lab reporting: the Mirage Kitten toolset (Kaspersky), the STAC4749 cluster (Sophos, which notes its custom-malware chain has not been reported elsewhere), the Check Point root-cause analysis (Rapid7), and the Talos quarterly report."
  summary: "Undercount by one. entries/2026-07-29/legacyhive-offline-registry-hive-profile-hijack-no-fix.md carries verification: single-source with a single source (LevelBlue SpiderLabs) and its own sourcing note reads 'Single-source: LevelBlue SpiderLabs' own analysis and independent reproduction of a publicly released proof-of-concept' — it is a fifth entry of exactly the class the sentence enumerates, and it appears nowhere in that section. Seven of the eleven entries carry a single-source* verification value (check-point, teamcity, legacyhive, mirage-kitten, stac4749, talos, uvvg); the section accounts for six. Fix: change 'Four' to 'Five' and name the LegacyHive entry with LevelBlue as its single source, so the run-record single-source disclosure covers every single-source entry."
- code: F2
  category: hallucinated-fact
  section: state-files
  item: "sources/sources.json — record id: siemens-productcert-csaf (notes field)"
  url_or_quote: "Added 2026-07-29 as this run's one new candidate: three published entries in the trailing 30 days cite cert-portal.siemens.com"
  summary: "Unremediated debt from iteration 3's F3. That iteration established, and the run record was corrected to state, that only TWO prior published entries cite cert-portal.siemens.com — verified independently this pass: entries/2026-07-10/siemens-sicam-8-ssa-229470-firmware-signing-bypass.md (ssa-229470.html) and entries/2026-07-18/siemens-ruggedcom-rox-ii-unit42-three-cve-chain.md (ssa-081142.html); the third citation is this run's own Desigo CC entry. The run record's sources_changed record now reads 'two prior entries in the trailing 30 days already cite cert-portal.siemens.com ... with this run's own Desigo CC entry making a third', so the permanent source record and the run record of the same run contradict each other on the same figure, and the source record — which outlives the run — repeats the exact wording iteration 3 ruled defective. Fix: reword the notes field to match the corrected run-record statement (two prior entries plus this run's own)."
- code: F3
  category: editorial-advisory
  section: run-record
  item: "runs/2026-07-29/2026-07-29T0408Z-intel — fetch_failures[sekoia].mitigation_applied"
  url_or_quote: "The remainder of the research slice was swept in full, including the seventeen major research labs, and no in-window item surfaced anywhere that pointed at a Sekoia publication."
  summary: "Advisory only. Sekoia is itself one of the seventeen major research labs in the S3 attempted set (mandiant-gtig, google-tag, talos, unit42, checkpoint-research, kaspersky-securelist, proofpoint, trendmicro-research, crowdstrike, recordedfuture-insikt, group-ib, zscaler-threatlabz, rapid7-research, sophos-xops, volexity, sekoia, malwarebytes — the count of 17 is exact and correct), so 'the remainder ... including the seventeen major research labs' claims a full sweep of a set that includes the source that failed. sixteen of the seventeen would be exact. The neighbouring sources_changed line ('the majority of the seventeen major research labs') is already correct."
- code: F4
  category: editorial-advisory
  section: active-threats
  item: "2026-07-29/stac4749-teams-vishing-certificate-pinned-golang-chaos — affected_products"
  url_or_quote: "affected_products: [\"Microsoft Teams\", \"Microsoft Quick Assist\", \"AnyDesk\", \"DWAgent\"]"
  summary: "Advisory only. RemSupp is the abused remote-support tool the entry treats as the operationally novel one ('from April 2026 RemSupp by preference') and the one its second action item tells the reader to block ('Add RemSupp to the remote-support tooling your application control blocks or alerts on'), yet it is the only such tool absent from affected_products while the three lesser ones are present. Sophos names it verbatim ('deployed the cloud-based RemSupp remote monitoring and management (RMM) tool as a fallback ... activity since April suggests the threat actors began to prefer RemSupp'), so it is a product the cited source names. An automated consumer matching an alert on affected_products would miss the tool the entry itself prioritises."
```
