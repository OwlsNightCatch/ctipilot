**Model:** Opus 5 (`claude-opus-5`)
**Timestamps:** started_at=2026-08-24T09:33:33Z · ended_at=2026-08-24T09:54:39Z · duration_seconds=1266
**Self-telemetry:** urls_checked=14 · webfetch_calls=0 · bridge_fetches=10 · direct_curl_fetches=14

## Verification report — 2026-08-24T0410Z-intel (iteration 1)

Read cold: all nine entries end-to-end (frontmatter + body), the run record, `work/2026-08-24T0410Z-intel/prior_coverage.json` (129 records), `entities/registry.yaml`, `sources/sources.json`, `attack/enterprise-attack.json` (v19.2 pin), `work/.../quotes.json`, `work/.../qcheck.py`, `work/.../sources_changed.json`, and the two entries this run updates (2026-08-18 ShieldBreak, 2026-08-19 Keycloak).

**Transport work performed this iteration.** All ten cited URLs re-fetched independently (10/10 HTTP 200, none a homepage/index/landing, all carry article slugs, none an NVD/MITRE/cve.org per-CVE page). The BACS PDF was re-fetched binary (MD5 `f085ff2341c9854023f890950b7f67fb`, byte-identical to the run's copy) and its text re-extracted locally with pdfminer under a stubbed `cryptography` import chain (77,075 chars). The Red Hat product states were re-derived from two independent structured surfaces the run did not cite — the Hydra security-data API and the CSAF/VEX document — because the portal page is client-rendered and carries no product table in HTML. Publication dates were re-read from JSON-LD `datePublished` / `article:published_time` / visible bylines on every source.

**Quote verbatimness (attack target 1) — independently re-derived, all 22 pass.** I re-stripped every re-fetched page and literal-substring-checked all 19 English `evidence[]` quotes plus the 3 German BACS fragments under NFKC + typographic normalisation. All 22 are contiguous verbatim substrings. Three (`truffle` "The two sets do not overlap, so 768 live keys give full control of a company AWS account.", `socradar` "However, this method is less stealthy…", `socradar` "This flow ensures that only a single 4 KB page…") fail a naive tag-to-space strip and pass a tag-to-empty-string strip — i.e. they are contiguous in rendered text with only inline `<strong>`/`<code>` markup intervening. **Not defects.** The German fragments carry no hyphenation or line-break artifact and match as printed. The Rapid7 quotes reproduce the page's own typographic damage (`quarter- double`, the en-dash CVSS range) correctly.

**Attack target 2 (ShieldBreak↔CVE linkage) — judged honest; no finding.** LevelBlue names no CVE (confirmed: 0 matches for `(?i)cve[- ]?\d{4}` across the 27.5 KB stripped body). But the linkage is *identifier resolution on a vendor-assigned codename*, not an analytical leap: Microsoft's own advisory names "ShieldBreak" for CVE-2026-69414 and NCSC-CH states "ShieldBreak is tracked as CVE-2026-69414 by Microsoft" — both carried and quoted in the entry this one updates. The `sourcing_note` states the caveat explicitly and `sourcing_note` renders reader-visible (`site/build.py:3852`). No F13.

**Attack target 6 (Keycloak correction) — verified correct against Red Hat's own record.** `access.redhat.com/hydra/rest/securitydata/cve/CVE-2026-18963.json`: exactly 11 `affected_release` rows (RHSA-2026:56519/56520/56523/56524 across Keycloak 26.4 / 26.4.15 / 26.6 / 26.6.6, `keycloak-services` + operator + RHEL9 + OpenShift images) and exactly 2 `package_state` rows, both `Not affected` (JBoss EAP Expansion Pack, Red Hat Single Sign-On 7). `security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-18963.json` confirms the justifications: `flags[]` = `component_not_present` → Expansion Pack, `vulnerable_code_not_present` → Single Sign-On 7. CVSS 9.1 AV:N/AC:L/PR:N/UI:N, `verified` ✓. The interim mitigation is verbatim Red Hat ("disabling the \"Forgot password\" functionality across all realms … Realm settings → Login → Forgot password → Off"). The original entry did make the claim being corrected (2026-08-19 line 96), so the correction is warranted. The "26.6.6 closes several on the same identity surface" claim also checks out: RHSA-2026:56520 lists 1 CVE, RHSA-2026:56523 lists 5 (CVE-2026-9796, -14613, -15571, -17048, -18963). Clean.

**Attack target 5 (priority calibration) — defensible as set; no finding.** No `critical` is correct: nothing in this window is act-to-the-hour. `high` holds on all three — BACS (home-region national authority, day-of publication, public sector now the largest reporting sector, full European energy-sabotage anatomy); ShieldBreak (no fix, PoC public, "Exploitation More Likely", baseline endpoint control, and the delta is the *first available detection*, which is a genuine change of what a team can do today); SynkLoader (the tenant-level control is a same-day action, and BACS independently places the identical email-bombing→Teams-help-desk chain in Swiss caseload, which is exactly the argument the entry makes for raising it above one vendor's incident). Nothing at `notable` clears the critical bar, and nothing at `notable` is being under-alerted.

**Attack target 7 (marginality / completeness).** Nothing in the nine is marginal enough to drop. Every `vulnerability` entry demands action beyond the patch cycle (ShieldBreak: no fix, detection is the only control; Keycloak: a correction that removes a deferral excuse on a CVSS 9.1 pre-auth account takeover). The weakest inclusion is the Rapid7 quarterly — a report-promotion post for a gated report, single-source vendor telemetry, no regional nexus — but it earns its place on a method argument (reachability discriminates better than severity) and is unusually honest about its own limits, including an explicit paragraph on what the report does *not* support. Not a drop. The borderline-drop list hides nothing: Berlin Landesnetz (no mappable attacker behaviour — correct hold), the Cisco Secure Workload / Crosswork critical tail and CVE-2026-16242 (both genuinely relevant, both correctly recorded as backlog rather than forced through recency), and the leak-site-only listings correctly held on the fake-news guard. See § Missed angles for the one coverage observation.

**S3 termination — disclosure judged adequate.** The gap is stated three times (frontmatter note, § Coverage gaps, the coverage-gaps line), names the domain, names the reason it was not respawned, and states plainly what would have been missed ("genuinely new research published on 2026-08-22 to 2026-08-24 by a lab outside those six primaries would not have been seen"). No entry in the run leans on a claim that a research sweep would have been needed to support: six of the nine ARE research-domain material worked from primaries the main agent re-fetched itself, and I independently re-fetched and re-read all ten. Adequate.

**Also checked clean, no findings:** zero IOCs (the only hash/IP-shaped matches are `127.0.0.1` inside the ShieldBreak UNC redirect path, which is part of the technique, not an indicator); no workflow-internal language in any entry (the `spawn` hits in the FTP and SilkParasite entries are the ordinary process sense — and the run record's use is settled practice, 20 occurrences in the previous weekly, so not flagged); English throughout with German quotes glossed; all `event_date` values are the primaries' own publication dates (ReliaQuest 08-22 vs post 08-23 and Keycloak 08-18 vs `public_date` 08-17 are 1-day, inside tolerance); every citation date matches its source's `datePublished`; all backlog `event_date` values (08-18 to 08-21) are honestly stated and none is presented as fresher than it is; all ATT&CK ids across all nine entries resolve active in the v19.2 pin (T1574.001 correctly used where LevelBlue's own table says the revoked T1574.002); the four dedup advisories are correctly reasoned; `actions[]` is disciplined at 3 across 9 entries with no generic, hedged, duplicated or padded item (**no F18**); the SilkParasite `relations[]` edges are typed, sourced and correct, and `incident:reliaquest-shinyhunters-vishing-claim-2026-08` correctly carries NO edge to `actor:shinyhunters`; no `org_triage` block, no `watchlist_hit: true`, no `watchlist` tag anywhere (**no F16**); every source change in `sources_changed.json` landed in `sources/sources.json`, and the one-new-candidate rule holds (`truffle-security` this run; `expel` was added by the 0110Z weekly).

---

### Citation does not support the claim

**F3-a — ReliaQuest: an industry-playbook element asserted as an observed event in this incident, with an invented Triage sequence built on it.**
Entry: `2026-08-24/reliaquest-vishing-mfa-push-device-trust-contained`
Claim: *"ReliaQuest also records that the attacker tried to enrol a new authenticator — the standard move for turning a borrowed session into durable access"*, and the Triage line: *"whereas this one followed immediately after a single push approval from a session already being denied. Treat an enrolment attempt inside such a session as an incident, not a user-support ticket."*
Source fetched this iteration: `https://reliaquest.com/blog/threat-spotlight-social-engineering-attempt-against-reliaquest-what-we-found`. What it actually says: enrolment appears **only** as an element of the industry-wide playbook, not as an observed step — *"This playbook is consistent with what we are seeing across the industry: an impersonation call, a throwaway lookalike domain registered and burned within the hour, a harvesting page behind a content delivery network, MFA push abuse, and a rapid attempt to enroll a new authenticator."* The incident narrative says the opposite about durable access: *"no persistence was established."* No cited text supports an enrolment attempt in this incident, and none supports the sequencing the Triage line asserts. Per check 10 an invented Triage discriminator corrupts triage decisions.
Secondary item, same entry: *"A stated 48-hour investigation covering control fidelity, device trust and on-network access"* — the source's 48 hours is a lookback window, not a duration: *"Our investigation, which included a complete audit of our control fidelity, device trust, on-network access and any suspicious subsequent activity in the last 48 hours."*

**F3-b — AWS keys: the 25 / 0.9% figure is attached to the wrong denominator.**
Entry: `2026-08-24/leaked-aws-keys-still-authenticate-git-history-ci-logs`
Claim: *"Of the broader population, 10,625 keys (16.6%) were root keys across 9,945 distinct accounts, and only 25 keys — 0.9% — were created in the last thirty days, so this is overwhelmingly not a story about fresh mistakes."*
Source fetched this iteration: `https://trufflesecurity.com/blog/leaked-corporate-aws-keys-held-full-admin-rights`. The 25/0.9% sits inside the key-age section whose denominator it states explicitly: *"For the 2,903 keys that permitted iam:ListAccessKeys, AWS reports the exact creation date. … Only 25 keys (0.9%) were created in the last 30 days."* Arithmetic confirms it: 0.9% × 2,903 ≈ 26; 0.9% × 64,024 = 576. The entry's own `sourcing_note` promises the narrow-denominator figures are "attributed as such in the body" — this one is attached to the broad population instead. Fix: attribute it to the 2,903 enumerable keys.

**F3-c — Rapid7: an unsupported comparison period, and it contradicts the entry's own title.**
Entry: `2026-08-24/rapid7-q2-2026-disclosure-volume-outpaces-triage`
Claim: *"while the count of vulnerabilities newly observed under exploitation held roughly steady quarter over quarter at 40"*
Source fetched this iteration: `https://www.rapid7.com/blog/post/tr-new-report-ai-threats-q2-2026-ends-traditional-patch-cycles`. It says only *"Meanwhile, the number of newly exploited vulnerabilities held roughly steady (40)"* — no period is named, and the preceding sentence sets a year-on-year frame (*"double the number reported in the same quarter last year (4,268)"*). "Quarter over quarter" is the entry's addition, and the entry's **title** frames the identical pairing the other way: *"disclosures doubled year on year to 8,539 while the number newly exploited held flat at 40."* Fix: drop the qualifier or use year on year consistently across title, summary and body.

**F3-d — FTP dead drop: token extraction attributed to the process command line rather than the operator's task string.**
Entry: `2026-08-24/ftp-banner-dead-drop-resolvers-e4del-pinhole`
Claim: *"Its execution path extracts an authorisation token from its own command line, calls a download endpoint keyed to that token and a hardware identifier…"*
Source fetched this iteration: `https://socradar.io/blog/ftp-banners-new-dead-drop-resolver-rats/`. The token comes from the C2 command string, in the command table: *"runpackage%e4del%[token] extracts an authentication token and reaches out to the C2's /api/download?token=[token]&clientId=[hwid] endpoint."* E4del *does* read its command line, but for something else entirely — an environment guard: *"E4del immediately evaluates its command-line arguments to dictate its behavior and verify its environment. It looks for the --init flag followed by a username, checking that the current Windows user matches the provided argument."* The conflation is detection-relevant: it sends a hunter looking in process command lines for a value that never appears there. Fix: *"extracts an authorisation token from the operator's task string"*.

### Unsupported / hallucinated facts

**F4-a — ReliaQuest: the entire ShinyHunters leak-site claim is uncited. (Attack target 3 — this is the answer.)**
Entry: `2026-08-24/reliaquest-vishing-mfa-push-device-trust-contained`
Claims (body lead): *"Around 22-23 August 2026 the extortion brand **ShinyHunters** — an actor this store already tracks across a long run of European and North American victims — listed the managed-detection vendor ReliaQuest on its leak site, supplying three screenshots that appear to show a logged-in session on an identity-provider end-user settings page, and no data samples."* Summary: *"ShinyHunters listed ReliaQuest on its extortion leak site around 22-23 August 2026 with three screenshots appearing to show a logged-in identity-provider settings page and no data samples."* Title: *"A ShinyHunters leak-site listing said an MDR vendor was compromised…"*
The entry's `sources[]` has exactly **one** record. I fetched it in full this iteration and read the whole article body: it never mentions ShinyHunters, a leak site, an extortion listing, screenshots, or data samples. The **only** occurrence of "ShinyHunters" anywhere on the page is inside `<meta name="keywords">` and the mirrored JSON-LD `keywords` string (`"Cyber Threats, Domain Impersonation, ShinyHunters, Subdomain Impersonation, Threat Intelligence, Threat Research, Threat Research - RSS"`) — an SEO tag, which asserts nothing about a listing, its date, its screenshot count, what the screenshots show, or the absence of samples.
So **every** leak-site fact in this entry — the actor, the 22–23 August dating, "three screenshots", what they depict, "no data samples" — rests on no cited source, and it drives the title, headline, summary, body lead, `entities: [actor:shinyhunters]` and `tags: [data-breach]`. The run record's own `fetch_failures` record for `databreaches-net` says *"the aggregator was needed only to confirm the leak-site listing exists and is unverified, for which a second outlet substituted"* with `covered_anyway: true` — that substitute outlet never reached the entry's `sources[]`.
Remediation, either: (a) add the corroborating outlet as a `sources[]` record with the right role and re-derive `verification` (it would no longer be single-source-victim on the listing half), or (b) strip the leak-site claim from title/headline/summary/body, drop `actor:shinyhunters` and the `data-breach` tag, and let the entry stand on the device-trust control-boundary lesson it is actually about — which is also the fix for the secondary framing concern under check 5 (an out-of-nexus incident framed around the victim's name rather than the transferable lesson; the *body* does the right thing, the *headline furniture* does not).
Note in the entry's favour: its `sourcing_note` restraint is correct and should survive whichever route is taken — no source does establish that the listing and the vishing call are the same event, and the entry rightly declines to assert it, in the prose, the tags and the registry (no `attributed-to`/`related-to` edge from the incident to `actor:shinyhunters`).

**F4-b — Run record § Single-source items and carve-outs: three statements contradicted by the entries they describe.**
File: `runs/2026-08-24/2026-08-24T0410Z-intel.md`
Claims: *"The Keycloak correction is `single-source-national-cert` under the first-party-authority limb: Red Hat is the only authority on its own products' state."* and *"Four entries are plain `single-source` — the ShieldBreak mechanism, SynkLoader, the AWS-key measurement, the FTP dead-drop research and the Rapid7 quarterly."*
Against `grep -H "^verification:" entries/2026-08-24/*.md`:
- `cve-2026-18963-keycloak-red-hat-product-states-corrected.md` carries **`verification: single-source`**, and its `sourcing_note` explicitly rejects the carve-out the run record asserts: *"Recorded as plain `single-source` rather than a carve-out value — the two-source carve-outs cover a national CERT or government authority for its own jurisdiction and a victim's own disclosure, and a vendor PSIRT is neither."* The entry is right and the run record is wrong (and Red Hat is a vendor PSIRT, not a national CERT — the value `single-source-national-cert` would itself be a misapplied carve-out).
- "Four entries" is followed by a list of **five**.
- `silkparasite-dll-sideload-pairing-google-drive-c2.md` is also plain `single-source` and is omitted from the paragraph entirely.
Actual distribution across the nine: `single-source` = **7** (ShieldBreak, Keycloak, SynkLoader, AWS keys, FTP dead drop, Rapid7, SilkParasite); `single-source-national-cert` = 1 (BACS); `single-source-victim` = 1 (ReliaQuest). The run record's verification notes publish to the site, so this ships reader-facing.

### Quantifier without source

**F14-a — AWS keys: "the four largest leak sources" is a ranking the source does not make.**
Entry: `2026-08-24/leaked-aws-keys-still-authenticate-git-history-ci-logs`
Title: *"…and none of the four largest leak sources is the current working tree"*. Headline: *"…they leak from Git history, dataset repos, container layers and CI logs — not from the working tree"*.
Source fetched this iteration names **five** surfaces and ranks exactly one: *"Our scanners have verified 64,024 unique AWS key pairs across 431,875 public findings: git history, Hugging Face datasets, Docker images, package registries, CI logs"* and *"The largest single source is Hugging Face."* There is no four-way ranking on the page, and the headline's list of four silently drops package registries — which the entry's own body does carry (*"The rest of the measured surface is Git history, container images, package registries and CI logs"*). The body's version of the claim (*"**None of those is the current working tree**"*) is sound; only the title/headline quantifier is invented. Fix: *"none of the leak sources it measured is the current working tree"*.

### Needs more research

**F8-a — SynkLoader drops the persistence mechanism, which is both the most hunt-ready artifact in the source and the thing that raised the alert.**
Entry: `2026-08-24/synkloader-teams-helpdesk-impersonation-six-module-loader`
The entry says only: *"A **persistence module** is reached through a second fake-runtime-named DLL that maps libraries into memory without writing them to disk."* `techniques[]` = `[T1566.003, T1684.001, T1204.002, T1036.005, T1620, T1059.001, T1056.002, T1090, T1113, T1082, T1057, T1007, T1018, T1078, T1584.006]` — no `T1053.005`.
What the cited source (fetched this iteration) carries and the entry drops: *"The DLL itself installs the task by leveraging the COM … By using the COM provider CLSID_TaskScheduler with RIID IID_ITaskService, the malware bypasses the need for invoking the Task Scheduler via the command line. This persistence technique is used to evade EDRs by sidestepping behavioral detections which monitor command line arguments for artifacts related to scheduling tasks or look for processes which execute schtasks.exe."* Plus: *"The scheduled task itself uses a random 12-character alphanumeric name, which is randomized every time the module is run. The task is configured to launch ss.py via the pythonw.exe executable"*, with triggers *"Every time the user logs on to the system"* and *"Every day at 10 a.m. local time"*.
That is a named, behavioural, telemetry-anchored detection concept (task-creation events for a task whose action is `pythonw.exe` on a script under a user-writable path, registered without `schtasks.exe` in the process tree) — and the entry's own opening sentence cites the alert it produced (*"found on 2026-08-18 while investigating an endpoint-detection alert on a scheduled task"*) while never telling the reader what the task looked like. The same run's ShieldBreak entry makes exactly this COM-vs-command-line point about `QueueReporting`, so the depth standard is demonstrably reachable. Add the mechanism (2–3 clauses) and `T1053.005`.

### Classification missing / inconsistent

**F17-a — FTP dead drop rates reliability B against its source's registered C, on a single-source entry, with no stated basis.**
Entry: `2026-08-24/ftp-banner-dead-drop-resolvers-e4del-pinhole` — `classification: {reliability: B, credibility: 2}`, `verification: single-source`, sole source `socradar.io`.
`sources/sources.json` records `socradar` at `"reliability": "C"` with the note *"Single-vendor investigative claims should be corroborated."* The C definition in that file's own `reliability_codes` is *"Fairly reliable — some doubt about consistency, OR the source mainly aggregates / re-reports rather than originates. Corroborate before acting."* B is one letter above the store's own assessment of the only source on the entry, and nothing in the entry explains the uplift. Either set `reliability: C`, or — if the uplift is deliberate because this piece is SOCRadar STRU first-hand reversing and naming rather than aggregation, which is a defensible reading — say so in `sourcing_note` so the code is auditable. Every other entry in the run matches its source's registered letter (BACS A/A, `trustwave-spiderlabs` B/B, `expel` B/B, `rapid7-research` B/B, `truffle-security` B/B, `reliaquest` B/B, `bitdefender-threat-debrief` B/B), so this is the one outlier. `credibility: 2` is correct throughout — single uncorroborated source.

### Editorial / less-is-more flags (advisory)

**F11-a — ShieldBreak: a source hedge removed in three places, including the run's only estate-wide hunt action.**
LevelBlue: *"The file is not expected to exist natively on supported Windows versions."* Entry, body: *"**`C:\Windows\System32\phoneinfo.dll` does not exist natively on any Windows version**"*; summary: *"a file that exists natively on no Windows version"*; `actions[0]`: *"the file ships with no Windows version"*. LevelBlue does call it *"the strongest unconditional detection point"*, so the operational conclusion holds and the action is right to exist — but "not expected to exist natively on supported Windows versions" is what the source will bear.

**F11-b — ShieldBreak: `techniques[]` omits T1218, which the source's own ATT&CK table assigns and the body describes.**
`techniques: [T1574.001, T1053.005, T1036.005, T1027.007, T1106]`. LevelBlue's published mapping includes *"T1218 Signed Binary Proxy Execution — wermgr.exe (signed) used as SYSTEM execution vehicle"*; the entry's body describes exactly that (*"that task runs as SYSTEM, so `wermgr.exe` processes the report and loads `phoneinfo.dll` with SYSTEM privileges"*). `T1218` is active in the v19.2 pin. (Credit where due: the entry correctly uses `T1574.001` rather than the source's `T1574.002`, and correctly omits `T1562.001` — both revoked in v19.2.)

**F11-c — SilkParasite headline leads with the two facts the in-window strategic entry already published.**
Headline: *"SilkParasite: five newly named RATs, one tasked through a shared Google Drive folder and another through HTTP Cookie and ETag headers."* `2026-08-23/weekly-w34-c2-rendezvous-moved-to-services-you-cannot-block` (in the dedup index, shares the `campaign:silkparasite-central-asia-2026` key) already carries *"a China-nexus toolset whose families use a shared Google Drive folder for operator commands and HTTP cookie and ETag header values as a command channel"*; `2026-08-23/weekly-w34-ai-bought-throughput-not-capability` already carries the AI-tells triad the entry's third paragraph repeats. `prompts/cti-run.md` Phase 1: *"intel runs never duplicate strategic entries."* **Not a drop** — the substance is genuinely new operational depth (six side-loading pairs, the pairing-as-signal formulation, WMI-instead-of-cmd execution, five registered families, per-host ChaCha20 keying, the four attribution grounds) and the run record documents the decision honestly. But the headline should lead with the pairing detection formulation the **title** already leads with, since that is the part the weekly does not carry. Advisory: headline framing only.

**F11-d — Run record DR1 note: the quote-verification count understates the carried set.**
*"literal-substring-verified all sixteen carried quotes plus the six BACS fragments — 22 of 22 passed."* The nine entries carry **19** English `evidence[]` quotes (ShieldBreak 3, SynkLoader 3, Rapid7 2, AWS keys 3, FTP 3, ReliaQuest 3, SilkParasite 2). `work/2026-08-24T0410Z-intel/quotes.json` holds exactly the 16 that `qcheck.py` machine-checked and excludes the three ReliaQuest quotes, so "all sixteen carried quotes" overstates coverage of the carried set. No truth consequence — I re-derived all 22 independently this iteration and all 22 pass, ReliaQuest included. Advisory: correct to 19 + 3 German, or state which subset the automated check covered.

**F11-e — BACS headline/title imply a change the report describes as continuity.**
Headline: *"public sector is **now** the largest share of Swiss mandatory CI reports at 19.4%"*; title: *"…makes public administration the top-reporting critical-infrastructure sector"*. The report's wording is explicitly continuous: *"wobei der öffentliche Sektor (19.4 %) und die IT und Telekommunikation (18.6 %) **weiterhin** am häufigsten Cyberangriffe gemeldet haben."* The body is accurate; only the headline furniture carries the novelty implication. Advisory.

*(For the record, and against attack target 4: the BACS deep dive verified clean on everything else. Re-extracted from the byte-identical PDF, all of the following are the report's own and correctly attributed — 27,128 / 35,727 / 200 / 19.4% / 18.6% / "knapp 26 Prozent" / 13.5% / 12.7% + 12.7% / "rund acht Prozent" / 10,759 / 8,877 / 3,965 / 387 at −89% / 258 at −57% / 3,817 at −64% / the FDV call-spoofing credit including "wirkte vor Inkrafttreten" / at-least-191 WordPress sites / ClickFix and the ClearFake Zoom-update variant / the email-bombing→Teams-help-desk chain cited to Unit 42 / the Trivy tag compromise / "log in, not break in" / 30 wind and PV parks / ~500,000 heat customers / MFA-less internet-exposed remote access, reused and factory-default passwords / firewalls, controllers, Mikronika RTUs over SSH, Hitachi Relion IEDs, SYNDIS HMI workstations / firmware reboot loops, OS-file deletion, factory reset with network-config change, wipers on Windows control machines / the month of reconnaissance, PsExec enumeration, the SMB "scada"-named file-system probing / the LSASS dump attempt, the Rubeus Diamond ticket, the ntds.dit dump / DynoWiper via GPO blocked by EDR, the modified build, dd disk overwrite, RAID manipulation / the stolen FortiGate configuration from a criminal forum, the credential-harvesting firewall routines that disable MFA, the Slack channel, the SSL-VPN tunnel with Impacket, LazyWiper via GPO from a network share / the Exchange-Teams-SharePoint search for ICS/OT/SCADA documents / the manual-effort and opportunistic-signalling readings / the basic-measures conclusion / the Swiss GCP-isolation caveat and the NTC private-photovoltaic study / the March 2026 Iran-linked medical-device factory-reset case with Swiss downstream customers / >20 Dream Job cases over five years, "einigen Tausend bis rund 60 Millionen", the weekly-contact-and-equal-hindsight balance, shame/asymmetry/awareness, the multiple-employees-at-one-company finding / the DPRK hedge as "attributed by other authorities and researchers to actors presumed to operate on behalf of the DPRK" / memory-only OtterCookie/BeaverTail leading to a bespoke DPRK backdoor / "keine Fälle gemeldet, in denen andere bekannte Bedrohungsakteure diese spezifischen Schadsoftware-Exemplare eingesetzt hätten". No Poland-chapter fact is misattributed between BACS and CERT.PL: the technical anatomy and the Static Tundra attribution are correctly placed with CERT.PL in the `sourcing_note`, BACS's own conclusions ("first publicly documented sabotage operation", "basic measures would have been the key") are correctly attributed to BACS, and the Unit 42 and NTC borrowings are placed with their owners. All 23 ATT&CK ids resolve active in the v19.2 pin and each maps to a behaviour the body describes in plain language — T1078.001 to the factory-default passwords, T1556.006 to the MFA disabling, T1484.001 to the GPO pushes, T1003.001/.003 and T1558 to the LSASS/ntds.dit/Diamond-ticket sequence, T1495/T1561/T1485 to the three destruction methods, T1213.002/T1114.002 to the SharePoint/Exchange document hunt, and so on. The management-summary-vs-chapter-1 tension is carried honestly rather than smoothed — "auf einem hohen Niveau stabilisiert hat" against "ein Rückgang auf insgesamt 27'128 Meldungen … verglichen mit 35'727" — in both the `sourcing_note` and the run record, with both statements labelled as the report's own.)*

### Missed angles

None I can name a plausible in-window source for. Coverage looks complete for the window as constrained: the S1 zero-item negative is well-evidenced (KEV catalogue version 2026.08.21 with all additions already covered, the EUVD exploited feed carrying the same four covered CVEs, CERT-EU and NCSC-NL both on the already-covered Zimbra flaw), the two uncovered items S1 did surface were correctly recorded as backlog rather than forced through recency, and the six transport gaps plus the terminated research sweep are each logged with the rich shape and honestly characterised. The one observation worth the main agent's glance, offered as an observation rather than a finding: the research-domain hole means the run has no independent read on anything a lab published 08-22 → 08-24 outside the six backlog primaries, and the credit-exhausted reader pool means that hole cannot be closed by transport escalation on this fire — so the *next* fire should treat 08-22 → 08-24 lab output as an explicitly widened window rather than assuming this run's backlog covered it. The run record already implies this; making it an explicit carry-forward would close the loop.

### Verdict

**NEEDS_FIXES (truth: 7, editorial: 2, advisory: 5)**

Truth (7): F4-a (ReliaQuest ShinyHunters claim uncited), F4-b (run-record single-source paragraph contradicts the entries), F3-a (ReliaQuest authenticator-enrolment + invented Triage sequence, + the 48-hour misreading), F3-b (AWS 25/0.9% wrong denominator), F3-c (Rapid7 "quarter over quarter"), F3-d (E4del token from command line), F14-a (AWS "four largest leak sources").
Editorial (2): F8-a (SynkLoader persistence mechanism + T1053.005 dropped), F17-a (SOCRadar reliability B vs registered C).
Advisory (5): F11-a … F11-e.

The single most important fix is **F4-a**: as it stands, the run's one `incident` entry leads its title, headline and summary with a set of facts no cited source carries, and the second outlet the run record says substituted for the 403'd aggregator never reached the entry. Everything else is repairable in place; nothing warrants a drop.

### Findings summary (machine-readable)

```yaml
# Findings summary (machine-readable)
- code: F4
  category: hallucinated-fact
  section: incidents
  item: "2026-08-24/reliaquest-vishing-mfa-push-device-trust-contained"
  url_or_quote: "\"Around 22-23 August 2026 the extortion brand **ShinyHunters** ... listed the managed-detection vendor ReliaQuest on its leak site, supplying three screenshots that appear to show a logged-in session on an identity-provider end-user settings page, and no data samples.\" / summary: \"ShinyHunters listed ReliaQuest on its extortion leak site around 22-23 August 2026 with three screenshots appearing to show a logged-in identity-provider settings page and no data samples.\""
  summary: "The entry's only sources[] record is https://reliaquest.com/blog/threat-spotlight-social-engineering-attempt-against-reliaquest-what-we-found (fetched this iteration, full body read). Its article text never mentions ShinyHunters, a leak site, an extortion listing, screenshots, or data samples; the ONLY occurrence of 'ShinyHunters' anywhere on the page is inside <meta name=\"keywords\"> / the JSON-LD keywords string, which asserts nothing about a listing, its date, or its contents. Every leak-site fact (the actor, the dating, the screenshot count, what the screenshots show, the absence of samples) is therefore uncited, and it drives the title, headline, summary, body lead, entities[actor:shinyhunters] and tags[data-breach]. The run record's databreaches-net fetch_failures record says 'a second outlet substituted' for confirming the listing exists — that substitute never reached the entry's sources[]. Remediation: add the corroborating outlet as a sources[] record (and re-derive verification), or strip the leak-site claim from title/headline/summary/body and drop actor:shinyhunters + data-breach."
- code: F3
  category: claim-not-supported
  section: incidents
  item: "2026-08-24/reliaquest-vishing-mfa-push-device-trust-contained"
  url_or_quote: "\"ReliaQuest also records that the attacker tried to enrol a new authenticator\" and Triage: \"whereas this one followed immediately after a single push approval from a session already being denied\""
  summary: "https://reliaquest.com/blog/threat-spotlight-social-engineering-attempt-against-reliaquest-what-we-found lists authenticator enrolment only as an element of the INDUSTRY playbook this attempt is consistent with: 'This playbook is consistent with what we are seeing across the industry: an impersonation call, a throwaway lookalike domain registered and burned within the hour, a harvesting page behind a content delivery network, MFA push abuse, and a rapid attempt to enroll a new authenticator.' It never states an enrolment attempt occurred in this incident, and it states the opposite of durable access: 'no persistence was established.' The Triage line then invents a sequence (enrolment immediately after a push approval, inside a session already being denied) that no cited text supports, and closes 'Treat an enrolment attempt inside such a session as an incident' on that invented basis. Same entry, secondary item: 'A stated 48-hour investigation' misreads the source — '...and any suspicious subsequent activity in the last 48 hours' is the lookback window for subsequent activity, not the investigation's duration."
- code: F3
  category: claim-not-supported
  section: research
  item: "2026-08-24/leaked-aws-keys-still-authenticate-git-history-ci-logs"
  url_or_quote: "\"Of the broader population, 10,625 keys (16.6%) were root keys across 9,945 distinct accounts, and only 25 keys — 0.9% — were created in the last thirty days, so this is overwhelmingly not a story about fresh mistakes.\""
  summary: "https://trufflesecurity.com/blog/leaked-corporate-aws-keys-held-full-admin-rights (fetched this iteration) places the 25/0.9% figure inside the key-age section, whose stated denominator is the enumerable subset: 'For the 2,903 keys that permitted iam:ListAccessKeys, AWS reports the exact creation date. ... Only 25 keys (0.9%) were created in the last 30 days.' 0.9% x 2,903 = 26; 0.9% x 64,024 = 576, so the figure cannot be read against 'the broader population'. The entry's own sourcing_note promises the narrow-denominator figures are 'attributed as such in the body' — this one is attached to the wrong population instead. Fix: attribute it to the 2,903 keys that permitted access-key enumeration."
- code: F14
  category: quantifier-without-source
  section: research
  item: "2026-08-24/leaked-aws-keys-still-authenticate-git-history-ci-logs"
  url_or_quote: "title: \"...and none of the four largest leak sources is the current working tree\" / headline: \"they leak from Git history, dataset repos, container layers and CI logs\""
  summary: "https://trufflesecurity.com/blog/leaked-corporate-aws-keys-held-full-admin-rights names FIVE leak surfaces ('git history, Hugging Face datasets, Docker images, package registries, CI logs') and ranks only one of them: 'The largest single source is Hugging Face.' There is no four-way ranking anywhere on the page, and the headline's list of four silently drops package registries — which the entry's own body does include ('The rest of the measured surface is Git history, container images, package registries and CI logs'). Fix: 'none of the leak sources it measured is the current working tree' (or 'the largest source is a public ML dataset platform; none of the measured sources is the working tree')."
- code: F4
  category: hallucinated-fact
  section: run-record
  item: "runs/2026-08-24/2026-08-24T0410Z-intel.md — § Single-source items and carve-outs"
  url_or_quote: "\"The Keycloak correction is `single-source-national-cert` under the first-party-authority limb\" / \"Four entries are plain `single-source` — the ShieldBreak mechanism, SynkLoader, the AWS-key measurement, the FTP dead-drop research and the Rapid7 quarterly\""
  summary: "All three claims are contradicted by the entries the paragraph describes (grep of entries/2026-08-24/*.md): (a) cve-2026-18963-keycloak-red-hat-product-states-corrected.md carries `verification: single-source`, and its sourcing_note explicitly REJECTS the carve-out ('Recorded as plain `single-source` rather than a carve-out value — ... a vendor PSIRT is neither'); (b) 'Four entries' is followed by a list of five; (c) silkparasite-dll-sideload-pairing-google-drive-c2.md is also plain `single-source` and is omitted entirely. Actual counts across the nine entries: single-source = 7 (shieldbreak, keycloak, ftp-banner, aws-keys, rapid7, silkparasite, synkloader), single-source-national-cert = 1 (BACS), single-source-victim = 1 (ReliaQuest). The run record's verification notes publish to the site, so this ships as reader-facing error."
- code: F3
  category: claim-not-supported
  section: annual-report
  item: "2026-08-24/rapid7-q2-2026-disclosure-volume-outpaces-triage"
  url_or_quote: "\"while the count of vulnerabilities newly observed under exploitation held roughly steady quarter over quarter at 40\""
  summary: "https://www.rapid7.com/blog/post/tr-new-report-ai-threats-q2-2026-ends-traditional-patch-cycles (fetched this iteration) says only 'Meanwhile, the number of newly exploited vulnerabilities held roughly steady (40)' — no comparison period is specified, and the immediately preceding sentence sets a year-on-year frame ('double the number reported in the same quarter last year (4,268)'). 'Quarter over quarter' is the entry's own addition, and it contradicts the entry's OWN title, which frames the same pairing year on year ('high- and critical-severity disclosures doubled year on year to 8,539 while the number newly exploited held flat at 40'). Fix: drop the period qualifier, or use year on year consistently across title, summary and body."
- code: F3
  category: claim-not-supported
  section: research
  item: "2026-08-24/ftp-banner-dead-drop-resolvers-e4del-pinhole"
  url_or_quote: "\"Its execution path extracts an authorisation token from its own command line, calls a download endpoint keyed to that token and a hardware identifier...\""
  summary: "https://socradar.io/blog/ftp-banners-new-dead-drop-resolver-rats/ (fetched this iteration) places the token extraction inside the C2 COMMAND table, not the process command line: 'runpackage%e4del%[token] extracts an authentication token and reaches out to the C2's /api/download?token=[token]&clientId=[hwid] endpoint.' The token comes from the operator's task string. E4del does separately evaluate its command line, but for a different purpose — an environment guard ('E4del immediately evaluates its command-line arguments ... It looks for the --init flag followed by a username, checking that the current Windows user matches the provided argument'). Conflating the two is detection-relevant: it points a hunter at process command lines for a value that never appears there. Fix: 'extracts an authorisation token from the operator's task string'."
- code: F8
  category: needs-more-research
  section: threats
  item: "2026-08-24/synkloader-teams-helpdesk-impersonation-six-module-loader"
  url_or_quote: "\"A **persistence module** is reached through a second fake-runtime-named DLL that maps libraries into memory without writing them to disk.\" (techniques[] = T1566.003, T1684.001, T1204.002, T1036.005, T1620, T1059.001, T1056.002, T1090, T1113, T1082, T1057, T1007, T1018, T1078, T1584.006 — no T1053.005)"
  summary: "https://expel.com/blog/synkloader-when-you-throw-in-everything-but-the-kitchen-sink/ documents the persistence mechanism in detail and explains why it was built that way, and the entry drops all of it: the DLL 'installs the task by leveraging the COM ... provider CLSID_TaskScheduler with RIID IID_ITaskService, the malware bypasses the need for invoking the Task Scheduler via the command line ... to evade EDRs by sidestepping behavioral detections which monitor command line arguments ... or look for processes which execute schtasks.exe'; 'The scheduled task itself uses a random 12-character alphanumeric name ... configured to launch ss.py via the pythonw.exe executable'; triggers 'Every time the user logs on' and 'Every day at 10 a.m. local time'. This is the most hunt-ready artifact in the report AND the thing that raised the original alert ('the EDR alerted on a scheduled task'), which the entry's own opening cites. techniques[] also omits T1053.005 (Scheduled Task) as a result. The same run's ShieldBreak entry makes exactly this COM-vs-schtasks point, so the depth standard is demonstrably reachable here."
- code: F17
  category: classification
  section: research
  item: "2026-08-24/ftp-banner-dead-drop-resolvers-e4del-pinhole"
  url_or_quote: "classification: {reliability: B, credibility: 2}; sources: socradar.io only; verification: single-source"
  summary: "sources/sources.json records id `socradar` at reliability `C`, and the record's own note says 'Single-vendor investigative claims should be corroborated.' The C definition in sources.json reliability_codes is 'Fairly reliable — some doubt about consistency, OR the source mainly aggregates / re-reports rather than originates. Corroborate before acting.' The entry rates B — one letter above its own source record — on a single-source entry with no corroboration and no explanation. Either set reliability C, or (if the uplift is deliberate because this is SOCRadar STRU first-hand reversing rather than aggregation) state that reasoning in the sourcing_note so the code is auditable. Every other entry in the run matches its source's registered letter (BACS A/A, trustwave-spiderlabs B/B, expel B/B, rapid7-research B/B, truffle-security B/B, reliaquest B/B, bitdefender B/B)."
- code: F11
  category: editorial-advisory
  section: vulnerabilities
  item: "2026-08-24/shieldbreak-defender-remediation-mechanism-hunting-package"
  url_or_quote: "body: \"**`C:\\Windows\\System32\\phoneinfo.dll` does not exist natively on any Windows version**\"; summary: \"a file that exists natively on no Windows version\"; actions[0]: \"the file ships with no Windows version\""
  summary: "LevelBlue's wording is hedged and narrower: 'The file is not expected to exist natively on supported Windows versions.' The entry removes the hedge and widens the scope in three places, including the run's only estate-wide hunt action. LevelBlue does call it 'the strongest unconditional detection point', so the operational conclusion holds — but 'not expected to exist on supported Windows versions' is what the source will bear. Advisory."
- code: F11
  category: editorial-advisory
  section: vulnerabilities
  item: "2026-08-24/shieldbreak-defender-remediation-mechanism-hunting-package"
  url_or_quote: "techniques: [T1574.001, T1053.005, T1036.005, T1027.007, T1106]"
  summary: "The cited source publishes its own ATT&CK table which includes 'T1218 System Binary Proxy Execution — wermgr.exe (signed) used as SYSTEM execution vehicle', and the entry's body describes exactly that behaviour ('that task runs as SYSTEM, so `wermgr.exe` processes the report and loads `phoneinfo.dll` with SYSTEM privileges'). T1218 is active in the pinned v19.2 dataset and is absent from techniques[]. (The entry is correct to use T1574.001 rather than the source's T1574.002, which is revoked in v19.2, and correct to omit T1562.001, also revoked.) Advisory."
- code: F11
  category: editorial-advisory
  section: threats
  item: "2026-08-24/silkparasite-dll-sideload-pairing-google-drive-c2"
  url_or_quote: "headline: \"SilkParasite: five newly named RATs, one tasked through a shared Google Drive folder and another through HTTP Cookie and ETag headers\""
  summary: "The two channels the headline leads with are the exact facts the in-window strategic entry 2026-08-23/weekly-w34-c2-rendezvous-moved-to-services-you-cannot-block already published ('a China-nexus toolset whose families use a shared Google Drive folder for operator commands and HTTP cookie and ETag header values as a command channel'), and the AI-tells paragraph repeats 2026-08-23/weekly-w34-ai-bought-throughput-not-capability ('leftover Go test functions, a hardcoded AES key set to a sequential placeholder and a configuration field still reading change_this_key'). cti-run.md Phase 1 states 'intel runs never duplicate strategic entries'. The entry's genuinely new operational content — the six side-loading pairs, the pairing-as-signal detection formulation, the WMI-instead-of-cmd execution point, the five registered families, the per-host ChaCha20 keying, the attribution grounds — is what the TITLE leads with and what the headline should. Not a drop: the substance clears the bar and the run record documents the decision. Advisory (headline framing only)."
- code: F11
  category: editorial-advisory
  section: run-record
  item: "runs/2026-08-24/2026-08-24T0410Z-intel.md — sub_agents.DR1.note"
  url_or_quote: "\"literal-substring-verified all sixteen carried quotes plus the six BACS fragments — 22 of 22 passed\""
  summary: "The nine entries carry 19 English evidence[] quotes, not sixteen: shieldbreak 3, synkloader 3, rapid7 2, aws-keys 3, ftp-banner 3, reliaquest 3, silkparasite 2. work/2026-08-24T0410Z-intel/quotes.json holds exactly the 16 that were machine-checked and excludes the three ReliaQuest quotes, so 'all sixteen carried quotes' overstates coverage of the carried set. (I re-derived all 22 independently this iteration against freshly re-fetched pages — all 22 pass, including the three ReliaQuest quotes and all three German BACS fragments, which are contiguous verbatim substrings as printed with no hyphenation artifact. Note the three that fail a naive tag-to-space strip — the Truffle '768 live keys' quote and the two SOCRadar quotes — pass on a tag-to-empty-string strip, i.e. they are contiguous in rendered text and are NOT defects.) Advisory: correct the count to 19 + 3 German, or say which subset the automated check covered."
- code: F11
  category: editorial-advisory
  section: annual-report
  item: "2026-08-24/bacs-halbjahresbericht-2026-1-poland-sabotage-dream-job"
  url_or_quote: "headline: \"public sector is **now** the largest share of Swiss mandatory CI reports at 19.4%\" / title: \"...makes public administration the top-reporting critical-infrastructure sector\""
  summary: "Both formulations carry a whiff of novelty the report does not claim. The report's wording is continuity: 'wobei der öffentliche Sektor (19.4 %) und die IT und Telekommunikation (18.6 %) weiterhin am häufigsten Cyberangriffe gemeldet haben' — 'weiterhin' = continue to be the most frequent reporters. The body is accurate ('the public sector accounts for the largest single share at 19.4%, ahead of IT and telecommunications at 18.6%'); only the headline/title framing implies a change. Advisory. Everything else in this entry verified clean against the re-fetched PDF (identical MD5 f085ff2341c9854023f890950b7f67fb, independently re-extracted): 27,128 / 35,727 / 200 / 19.4% / 18.6% / knapp 26% / 13.5% / 12.7% / ~8% / 10,759 / 8,877 / 3,965 / 387 -89% / 258 -57% / 3,817 -64% / 191 WordPress sites / 30 parks / ~500,000 heat customers / DynoWiper+GPO+EDR block / LazyWiper via GPO from a network share / LSASS + Rubeus Diamond ticket + ntds.dit / Slack channel / Impacket over SSL VPN / Exchange-Teams-SharePoint ICS-document search / 'first publicly documented sabotage operation' / >20 Dream Job cases / CHF 60m / OtterCookie+BeaverTail memory-only + bespoke DPRK backdoor / 'no case reported of any other known actor using those specific samples' / NTC photovoltaic study / March 2026 Iran-linked medical-device factory-reset case with Swiss downstream customers. All 23 ATT&CK ids resolve active in the v19.2 pin and each maps to a behaviour the body describes in plain language. The management-summary-vs-chapter-1 tension is handled honestly in the sourcing_note and the run record ('auf einem hohen Niveau stabilisiert hat' vs 'ein Rückgang auf insgesamt 27'128'), and every borrowed-work attribution (CERT.PL, Unit 42, NTC) is placed with the right party."
```
