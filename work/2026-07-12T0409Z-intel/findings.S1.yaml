domain: S1
run_id: 2026-07-12T0409Z-intel
model: Sonnet 5
model_id: claude-sonnet-5
started_at: 2026-07-12T04:12:11Z
ended_at: 2026-07-12T04:18:56Z
duration_seconds: 405
self_telemetry:
  webfetch_calls: 3
  websearch_calls: 4
  bridge_fetches: 28
items: []
# No items cleared the gate this run. Rationale below.
#
# Recency context: gap since previous run (2026-07-11T2009Z-intel) is only ~8h;
# window_hours floor is 24h. Every essential + most standard S1 source was
# fetched fresh in this run and cross-checked against prior_coverage.json
# (93 CVEs / 81 entity keys already published in the last 14 days, including
# today's earlier coverage). Every candidate surfaced by this sweep was either
# (a) already published (exact CVE match in prior_coverage) or (b) stale
# relative to window_hours (freshest available source predates the window
# with no in-window delta).
#
# Ground-truth check (CISA KEV): fetched full catalog (cisa-kev bridge).
# catalogVersion 2026.07.10, dateReleased 2026-07-10T17:00:25Z — i.e. the
# catalog itself has not been updated since ~35h before this run. The 10
# most-recently-added KEV entries (CVE-2026-56291 Balbooa Forms,
# CVE-2026-48939 iCagenda, CVE-2026-48908 SP Page Builder, CVE-2026-55255
# Langflow, CVE-2026-56290 Joomlack Page Builder, CVE-2026-48282 Adobe
# ColdFusion, CVE-2026-45659 SharePoint, CVE-2026-48558 SimpleHelp,
# CVE-2026-12569 PTC Windchill/FlexPLM, CVE-2026-20230 Cisco Unified CM) are
# ALL already present in prior_coverage.json (dates 2026-07-08 through
# 2026-07-10). Corroborated by WebSearch: no "CISA Adds ... to Catalog" alert
# newer than 2026-07-10 found.
#
# ENISA EUVD (exploited / criticals / lastvulnerabilities lists): same set of
# already-covered CVEs (Balbooa, iCagenda, Langflow, Joomlack) plus a handful
# of Microsoft Edge/Entra/Azure OpenAI EUVD records whose datePublished is
# 2026-07-02/03 (dateUpdated 07-11 is a metadata refresh, not new content) —
# outside window, no fresh delta. CERT-PL-sourced R-SOFT DMS / mtr entries all
# datePublished 2026-07-10.
#
# National CERT / NCSC sweep: NCSC-NL RSS (advisories.ncsc.nl) newest item
# NCSC-2026-0223 (BeyondTrust RS/PRA) published 2026-07-10T05:54Z — already
# covered 2026-07-08 (beyondtrust-rs-pra-preauth-bypass-cve-2026-40138-cluster).
# NCSC-CH Security Hub (bridge ncsc-csh recent) newest posts: id 12757 (Zimbra
# Classic Web Client) 2026-07-10T12:45Z and id 12755 (Gitea CVE-2026-20896)
# 2026-07-10T08:55Z — both already covered as 2026-07-10 entries
# (zimbra-classic-web-client-code-exec-ncsc-ch;
# gitea-cve-2026-20896-ncsc-ch-actively-exploited-update). CERT-FR (avis feed)
# newest avis CERTFR-2026-AVI-0866 dated 10 juillet 2026 (MOVEit, NetApp
# ONTAP, CPython, Suricata, Siemens, Linux kernel distro rollups, GitLab,
# Traefik) — all outside window, no unique new CVEs beyond what CERT-FR
# itself already flagged and BSI/NCSC-NL mirror. BSI CERT-Bund WID RSS newest
# item Fri 10 Jul 2026 10:xx UTC (DENX U-Boot, Samsung Android, Linux kernel
# distro updates) — outside window. CERT-EU advisories RSS last entry
# 2026-008 dated 10 June 2026 (stale, no July advisory yet). CERT-PL news
# listing newest post dated 2026-06 (UNC1151/Ghostwriter Gmail campaign) —
# outside window. NCSC-UK reports-advisories page (both direct WebFetch and
# jina reader) returned a stale/cached render whose newest listed item is
# dated 7 Apr 2026 — see coverage_gaps; treated as unusable for this run
# rather than cited.
#
# Vendor PSIRT / research-blog sweep (Cisco PSIRT RSS, Keycloak RSS, VulnCheck
# blog, watchTowr Labs RSS, Exodus Intelligence RSS, 0patch RSS, Censys blog
# RSS, JPCERT/JVN, Apple security releases, Synacktiv publications, Sansec
# research, GMO Flatt Security research, Microsoft Security Blog RSS, MSRC
# recent-releases API): every feed's newest item predates the window (latest
# found: Cisco PSIRT "Advance Notification for July 15 advisories" published
# 2026-07-08; Keycloak 26.7.0 released 2026-07-09, feature release with no
# flagged CVEs distinct from the already-covered 26.6.4 CVE-2026-11800/
# CVE-2026-9800 deep dive; MSRC recent-releases shows a same-day batch of
# ~20+ routine Chromium/Edge CVEs stamped 2026-07-11T22:41Z, none flagged
# exploited or publicly disclosed — routine batch, does not clear the
# imminent-exploitation bar). WebSearch cross-check on "actively exploited
# vulnerability" / "CVE exploited in the wild" for the window surfaced only
# already-covered items (CVE-2026-48282 Adobe ColdFusion — SecurityAffairs
# article itself dated 2026-07-06, outside window; CVE-2026-46817 Oracle
# EBS — already in prior_coverage).
#
# Watchlist duty: products — org profile configures NO product watchlist, so
# this sweep is a documented no-op per spawn instructions.
watchlist_sweep:
  duty: products
  products_checked: 0
  suppliers_checked: 0
  hits: 0
  note: "no product watchlist configured in org-profile.yaml — sweep is a no-op per spawn instructions; general S1 coverage above already checked the full essential+standard source slice with no watchlist-specific gap."
candidate_sources: []
coverage_gaps:
  - source_id: ncsc-uk
    reason: "Both plain WebFetch and the jina reader (python3 tools/fetch_source.py jina) returned the SAME stale/cached render of https://www.ncsc.gov.uk/section/keep-up-to-date/reports-advisories whose 'On this page' list tops out at an item dated 7 Apr 2026 (APT28 router DNS hijacking) despite fetching live in this run. This looks like an upstream CDN/edge cache serving a snapshot rather than the current page (the page is JS-hydrated and the reader's execution may be hitting a cached shell). No in-window NCSC-UK item could be confirmed via this route this run; recommend the source record's notes be updated to try a dated RSS/Atom endpoint if NCSC-UK exposes one, or re-probe next run in case the cache clears."
  - source_id: horizon3-ai
    reason: "https://horizon3.ai/attack-research/ is a JS-hydrated (Bricks builder) filtered listing — the raw HTML body carries only the filter-sidebar markup (categories/tags), not the actual post entries, under both plain WebFetch (redirected to an ad-tracker pixel via a stray outbound redirect) and direct bridge fetch (--direct served the real page shell but posts load via AJAX, not present server-side). No structured feed/sitemap route attempted yet this run given time budget — recipe gap, not a transport failure; recommend probing for an RSS endpoint (e.g. /feed/) or a JSON REST endpoint (WordPress sites typically expose /wp-json/wp/v2/posts) on a future run."
  - source_id: calif-codex
    reason: "https://blog.calif.io/rss.xml returned upstream HTTP 404 on direct fetch; jina fallback returned a valid-but-empty feed (count: 0). The RSS path in sources.json for this record may be stale/wrong — recommend checking blog.calif.io's actual current feed path (or confirming the blog is still active) on a future run."
fetch_failures: []
