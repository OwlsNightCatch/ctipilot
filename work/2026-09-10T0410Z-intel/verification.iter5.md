**Model:** Sonnet 5 (`claude-sonnet-5`)
**Timestamps:** started_at=2026-09-10T05:46:13Z · ended_at=2026-09-10T05:52:56Z · duration_seconds=403

## Verification report — 2026-09-10T0410Z-intel (iteration 5)

### Prior-iteration deltas (iteration 4 → 5) verified

1. F4 (bluemoon `tool:ghostchrome-x`): confirmed fully removed. `grep -rn "ghostchrome" entities/registry.yaml runs/2026-09-10/2026-09-10T0410Z-intel.md entries/2026-09-10/*.md` returns zero hits in the registry or any entry; only the run record's own history section references the string (expected, describing the fix). `entities_added` in the run record no longer lists `tool:ghostchrome-x`. `tool:gemstone-browser-extension`'s registry summary now reads "...using a publicly documented HMAC-forgery technique to bypass Chrome's Secure Preferences integrity check..." — generic, no uncited name. Confirmed against Proofpoint's own post (fetched this iteration): the string "GhostChrome" appears only inside a Proofpoint hyperlink slug (`zerolabs.rubrik.com/blog/inside-ghostchrome-x-...`) pointing to Rubrik Zero Labs, which the entry does not cite in `sources[]`; Proofpoint's own visible prose never uses the term. Remediation correct.
2. F6 (sap `sources[]` order): confirmed. Entry now lists both Onapsis Research Labs posts first with `role: primary`; CERT-EU is third with `role: corroborating`. Remediation correct.

### Independent cold-read pass — new findings

#### Unsupported / hallucinated facts

**#1** — `2026-08-04/cve-2026-20079-cisco-secure-fmc-auth-bypass-root-hotfix`: this run's own `## Update — 2026-09-10T05:32:00Z` section states verbatim: *"This confirms active exploitation that Cisco's own advisory had not reported as of its last revision, and reverses the 'not aware of any malicious use' framing this entry's main analysis carried."* The update record's `fields` list includes `body`, and `cves[0].status` was correctly moved to `[exploited, cisa-kev, patch-available]`. But `git diff HEAD` shows the only body change this run was the *appended* new `## Update` section — the pre-existing main-analysis paragraph was left completely untouched and still reads: *"Cisco states it is 'not aware of any public announcements or malicious use' of this CVE. The reason to treat it as out-of-band anyway sits on the same web interface..."* — present tense, framed as the entry's current position. This is exactly the check-4c(e) scenario: *"an update that made the CVE exploited while the analysis still says 'no exploitation observed' is F4."* The update record itself flags the contradiction but does not fix it. Fix: edit the main-analysis sentence in place (e.g., "Cisco stated at the time... — since reversed, see Update below") so the entry does not simultaneously assert exploited and not-observed.

**#2** — `2026-09-06/chaotic-eclipse-falconflank-prettyprague-edr-av-lpe-drops` (and the mirrored `tool:falconflank` registry summary): the new `## Update — 2026-09-10T05:10:00Z` section states *"LevelBlue's own testing found CrowdStrike's cloud ML detection catches the artifact in default configurations"* (body), and the changelog record's own summary says *"which LevelBlue found CrowdStrike's own cloud ML detection catches in default test conditions."* I fetched the cited LevelBlue SpiderLabs post (`https://www.levelblue.com/blogs/spiderlabs-blog/expanding-the-attack-surface-analyzing-nightmare-eclipses-latest-pocs`) this iteration: the word "default" appears exactly once in the whole article, and only in an unrelated sentence about Kaspersky ("In the default KES v14 configuration, avpui.exe does not automatically restart"). For FalconFlank, LevelBlue's own text is: *"CrowdStrike Policy setting for Microsoft Office file malicious macro removal enabled alongside CrowdStrike's policy setting recommendations, which shows that this is an **optional** setting for 'Phase 3 – Optimal Protection' settings,"* and *"For CrowdStrike clients to be susceptible to this local privilege escalation vulnerability, an endpoint must be assigned to a Prevention Policy with the ... setting enabled."* Since the exploit itself only works when that non-default, optional setting is on, and LevelBlue never characterizes the detection as firing "in default configurations," the "default configurations/test conditions" qualifier is an inserted quantifier the source does not support. Fix: reword to something like "in the tested configuration" or "when the same optional setting the exploit itself requires is enabled," in the body, the changelog summary, and the `tool:falconflank` registry entry (`entities/registry.yaml`).

#### Claims missing inline citation

**#3** — `2026-09-10/sap-september-2026-overpass-s4get-preauth-rce`, main analysis: the sentence *"Onapsis notes SAP patches have historically been reverse-engineered into working exploits within 72 hours — as happened with the RECON vulnerability (CVE-2020-6287) — and that window keeps shrinking with AI-assisted tooling, while a comparable pre-auth SAP disclosure (CVE-2025-31324) became 2025's most-exploited CVE by Mandiant's count"* carries no inline citation (compare the sentences immediately before and after, which are each closed with an `([Onapsis, 2026-09-09](url))` or `([CERT-EU, ...])` link). I fetched both Onapsis posts this iteration; the content is accurate — the OVERPASS post states *"History also shows attackers can reverse-engineer SAP patches within 72 hours, as with RECON (CVE-2020-6287), and that window keeps shrinking now with AI-assisted tooling. The 2025 mass exploitation of CVE-2025-31324, named by Mandiant's 2026 M-Trends report as the most exploited vulnerability of the year..."* — but the entry's sentence has no link attached, violating check 3 ("every fact... needs a link"). Fix: append `([Onapsis, 2026-09-09](https://onapsis.com/blog/sap-overpass-remediation/))` to that sentence.

#### Editorial / less-is-more flags (advisory)

**#4** (low confidence) — `2026-09-06/chaotic-eclipse-falconflank-prettyprague-edr-av-lpe-drops`: the 2026-09-10 changelog record is typed `type: update`, which per `docs/pipeline.md`'s table floats `updated_at` (it did: null → 2026-09-10T05:10:00Z) and is reserved for "a material new development... new actor, victim, CVE in the chain, patch shipped, exploitation-status change... confirmed law-enforcement action." What the record actually adds — an independent research lab (LevelBlue) reproducing and confirming existing PoCs with added mechanism detail, no new CVE/actor/victim, no patch shipped, no exploitation-status change — reads as a closer match to the `improvement` type definition: *"precision or depth added without reversing a claim — a second independent source... the section states what was added and on what basis."* If `improvement` is the better fit, `updated_at` should not have moved and this entry should not have re-floated to the top of the live brief this run. I flag this as low confidence because the FalconFlank precondition-narrowing and GreenSection re-characterization are borderline correction-adjacent, and the main agent may reasonably judge the aggregate delta material enough to warrant `update`.

**#5** (carried forward, reconfirmed) — `2026-09-10/checkpoint-quantum-vpn-cert-preauth-rce-cvss98`: fetched Forkast News this iteration; it still reads as templated/aggregator content (uniform structure recycled across CVE, PaperCut, N-able, Microsoft, SAP and Ivanti write-ups on the same domain, a stale 2024 Censys figure presented as current context that this entry does not use). Facts cited from it (the exact quote used) check out. No action needed — role is corroborating and the two Check Point vendor advisories remain the solid primaries, consistent with iterations 1/3/4's disposition.

**#6** (carried forward, reconfirmed) — `2026-09-06/chaotic-eclipse-falconflank-prettyprague-edr-av-lpe-drops`: T1574.001-vs-T1574.008 fit for HardBreacher's session-namespace symlink DLL-load-path redirect remains a genuine imperfect-fit case; declining again, consistent with iterations 3/4.

### Checks performed with no findings

- Fetched and cross-checked every inline source on all 5 new entries against the claims attached to them: CISA KEV alert page and JSON feed (confirmed all 4 CVE additions, `dateAdded`/`dueDate` for CVE-2026-19490, CVE-2025-25249, CVE-2026-87491, CVE-2026-20079 all match exactly what each entry states), SOCRadar's PivotC2 post (CVSS figures, ASLR-defeat/heap-grooming mechanics, 30,000/178 victim counts, ATT&CK table T1055.002/T1059.001/T1027/T1003.001/T1046/T1021.001/T1567.002/T1071.001, ATT&CK-mapped run.ps1/RDP/reverse-SSH post-exploitation detail), Chrome release notes + NVD API (CVSS 3.1 8.8/HIGH confirmed directly against NVD's JSON — the "Medium" label some outlets cite is Chrome's own internal bug-severity rubric, not a CVSS conflict), Help Net Security, The Hacker News, CERT-FR advisory (all quotes verbatim), both Onapsis SAP posts and CERT-EU's advisory (10,000+ internet-facing-interfaces quote, S4GET mechanism, and the `KRNL64NUC`/`KRNL64UC`/`KERNEL`/`WEBDISP` affected-version table match CERT-EU's table exactly), both Check Point vendor advisories sk1000118/sk1000117 (quotes, CVSS, affected/fixed versions all verbatim), Forkast News, and Proofpoint's full BlueMoon post (all four actor clusters' first-seen dates, targeting, TTPs, the Windows-build support table, and the GemStone C2/keylogger mechanics all verified against the source).
- Verified the PaperCut, Berlin, NetScaler and Cisco updated entries' new changelog sections against their newly cited sources this run (GreyNoise's full post for PaperCut — every quoted figure, both attack-path descriptions and the DCSync/noPac/LSASS mechanics match exactly; heise's 2026-09-07 Krempl article for Berlin — every translated quote is a verbatim match to the German original; the CISA KEV JSON feed for the NetScaler correction and the Cisco update).
- Confirmed the NetScaler `correction` record (iteration 3's F10 remediation) correctly left `updated_at` unmoved (still 2026-09-08), consistent with a `correction` never re-floating an entry.
- Dedup: none of the five new entries' CVEs (CVE-2025-25249, CVE-2026-87491, CVE-2026-44756, CVE-2026-58240, CVE-2026-85103, CVE-2026-85102) appear in `work/2026-09-10T0410Z-intel/prior_coverage.json`; CVE-2026-85046 and CVE-2026-85880 (BlueMoon's declared `references[]`) do appear there, consistent with the entry's own dedup declaration.
- Registry sanity: no duplicate keys created for `actor:apt31` or any of the other newly registered entities; all `entities[]` values used in the five new entries resolve to exactly one registry record each.
- Style discipline: no IOCs, no vanity metrics, no workflow-internal language found in any of the ten entries or the run-record notes.
- Missed angles: none identified beyond what the run record's own coverage notes already surface (Veradigm/Mantax Otax drops, coverage-backlog re-checks) — those drop rationales read as sound applications of the out-of-nexus breach gate.

### Verdict

NEEDS_FIXES (truth: 2, editorial: 1, advisory: 3)

Truth: #1 (Cisco main-analysis self-contradiction), #2 (FalconFlank "default configurations" unsupported qualifier, in both the entry and the registry).
Editorial: #3 (SAP missing citation).
Advisory: #4 (chaotic-eclipse update-vs-improvement type, low confidence), #5 and #6 (both carried-forward, reconfirmed, no action needed).

This is NOT the first CLEAN — two new, evidenced defects remain (one truth-class contradiction spanning frontmatter/body/changelog consistency, one truth-class unsupported qualifier duplicated across an entry and the registry). Both are small, surgical fixes. Recommend: (a) edit the Cisco entry's main-analysis paragraph to remove the present-tense "not aware of any malicious use" framing or explicitly mark it as superseded, declared via the existing 2026-09-10 update record's `fields:[body]`; (b) reword "default configurations"/"default test conditions" to an accurate characterization (the tested/required Prevention Policy configuration) in the chaotic-eclipse body, its changelog summary, and the `tool:falconflank` registry summary; (c) add the missing inline citation to the SAP entry's RECON/Mandiant sentence.

### Findings summary (machine-readable)
