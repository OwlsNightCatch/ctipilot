**Model:** Opus 5 (`claude-opus-5`)
**Timestamps:** started_at=2026-08-21T05:21:47Z · ended_at=2026-08-21T05:43:09Z · duration_seconds=1282
**Self-telemetry:** urls_checked=31 · webfetch_calls=0 · bridge_fetches=31 (all fetching done through `tools/fetch_source.py url` / `pdf` / `enisa-euvd` / `msrc` / `cisa-kev`; the jina rung is pool-exhausted and was never needed)

## Verification report — 2026-08-21T0410Z-intel (iteration 1)

Cold read, no prior-iteration deltas. All 11 entries read end-to-end (frontmatter + body), plus the run record, `work/2026-08-21T0410Z-intel/prior_coverage.json` (150 records), `entities/registry.yaml`, `sources/sources.json`, `attack/enterprise-attack.json` (v19.2 pin) and `site/taxonomy.yaml`.

**Fetch coverage — every inline source URL on every entry was fetched fresh in this iteration, not sampled.** All primaries and all corroborating records: rust-lang.org, stepsecurity.io, wiz.io, safedep.io, rustsec.org (+RUSTSEC-2026-0259/0261/0262 for the sibling-advisory claim), cert.pl, berlin.de, heise.de, wiwo.de, research.checkpoint.com, cloud.google.com (GTIG), ibm.com/think, levelblue.com, msrc (via `msrc cve`), ics-cert.kaspersky.com ×2, euvd.enisa.europa.eu ×3 (via the API recipe), blog.talosintelligence.com ×2, franceinfo.fr, presse.economie.gouv.fr, occrp.org, ic3.gov PDF (via the new `pdf` transport), CISA KEV JSON. Zero 404s, zero DNS failures, zero homepage/index/landing URLs, zero NVD/MITRE per-CVE citations.

**Quote fidelity — every `evidence[]` quote (33 across 11 entries) and every inline-quoted phrase in every body was tested as a literal contiguous substring** of the fetched page after HTML-tag stripping, entity unescaping, NBSP/thin-space normalisation and Unicode-quote folding only. **All 33 pass.** Notably: the Check Point NBSP-heavy page, the GTIG quote carrying curly quotation marks around "verification code", the four French-language quotes (accents and apostrophes intact), and both S7 PDF spans — which are contiguous in the extraction once its hard line breaks are collapsed, exactly as the entry's sourcing_note claims. No splices, no inserted ellipses, no invented terminal punctuation, no re-hedged words. The two spliced quotes caught pre-verification did not leave residue.

### Citation does not support the claim

**F3-1 — `trueconf-server-kev-head-mare-trojanized-client-installers`: the sandbox quote is attached to the advisory, but lives in the report.**

The entry says:

> That script lands in a sandbox "where potentially hazardous libraries (io, os, etc.) are unavailable" — and CVE-2026-72530 is the way out of it, letting the attacker "break out of the isolated environment and execute arbitrary code on the host system" ([Kaspersky ICS-CERT KLCERT-26-058, 2026-08-11](https://ics-cert.kaspersky.com/advisories/2026/08/11/trueconf-server-breakout-from-isolated-environment/))

One sentence, two quotes, one trailing citation — so the citation claims both. I fetched KLCERT-26-058 in full (the whole advisory is 3,746 characters of text; I read all of it). It carries the second quote verbatim in its Description field. It does **not** contain the string `hazardous libraries` anywhere. That phrase is a verbatim substring of the *other* co-cited Kaspersky page, the 2026-08-12 report, which reads: "…hazardous libraries (io, os, etc.) are unavailable. However, attackers can exploit another vulnerability (assigned internal Kaspe[rsky…]". Shape (i) of the adjacency class: a true fact cited to the co-cited source that does not state it. Fix is one citation, not a rewrite.

### Unsupported / hallucinated facts

**F4-1 — `atutor-13-cves-eol-no-fix-unauthenticated-admin-takeover`: CVE-2026-64969 is typed as information disclosure; the advisory describes destruction.**

Frontmatter: `- id: CVE-2026-64969 … type: info-disclosure`. Body: "an insecure direct object reference letting any authenticated user — a student included — supply another user's member identifier against the profile-picture endpoints (CVE-2026-64969)."

CERT Polska's advisory (fetched this pass) says:

> The vulnerability CVE-2026-64969: ATutor is vulnerable to Insecure Direct Object Reference (IDOR) attack in profile picture related endpoints. Any authenticated user, including a student, can supply another user's member_id in a POST request to the profile album endpoint and **permanently delete** that user's profile picture, including those of instructors and administrators.

Nothing is disclosed. The advisory's own CWE for this id is CWE-639 (Authorization bypass through User-Controlled key), and the impact is unauthorised deletion — an integrity/availability effect. `type: info-disclosure` contradicts the cited source; `auth-bypass` or `logic-flaw` from the taxonomy fits. Compounding it, the body sentence stops one clause short of the impact, so the entry tells a reader that a student can supply someone else's id but not that doing so permanently destroys an administrator's data. The run record makes a point of the per-CVE mapping being "read off the advisory's own per-identifier metadata and description blocks rather than paired by position" — this is the one record where that reading did not carry through.

**F4-2 — same entry: CVE-2026-64972 names the wrong parameter.**

Entry: "reflected XSS in the restore functionality (CVE-2026-64971) and **via a preview parameter** (CVE-2026-64972)."

Advisory: "CVE-2026-64972: ATutor is vulnerable to Reflected XSS **via popup parameter in preview.php**. An authenticated attacker can inject a double quote into the popup parameter, break out of the attribute value, and append a new event handler such as onload."

The injectable parameter is `popup`; `preview.php` is the script it lives in. Small, but the entry is otherwise a per-identifier reference table and a hunter reading it would look for the wrong parameter name.

### Claims missing inline citation

**F5-1 — `arrayref-crates-io-build-script-supply-chain-proc-macro1`: the CVE identifier and its score are in no cited source and carry no link.**

> Only arrayref carries a CVE — CVE-2026-77651 at CVSS 9.8, scoped in ENISA's record to arrayref 0.3.10 alone; internment and append-only-vec are tracked under RustSec and OSV malicious-package identifiers with no CVE of their own.

No inline citation terminates that clause, and I searched all five cited pages after stripping markup: `blog.rust-lang.org` — no CVE id; `stepsecurity.io` — no `CVE-2026-77651`, and its only occurrence of `9.8` is inside "winit (49.8M downloads)"; `wiz.io` — neither; `safedep.io` — neither; `rustsec.org/advisories/RUSTSEC-2026-0260.html` — neither (I read the full advisory text; it names the package, the 86-minute window and "no evidence of actual usage", but assigns no CVE). The frontmatter `cves[]` record carries the same id and score.

The claim is **true** — I verified it independently against the per-CVE authority the entry names but does not link: `https://euvdservices.enisa.europa.eu/api/search?text=CVE-2026-77651` returns exactly one record, EUVD-2026-63780, `aliases: CVE-2026-77651`, `baseScore: 9.8`, `baseScoreVersion: 3.1`, `baseScoreVector: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H`, `epss: 0.0`, product `arrayref` version `0.3.10`. So this is a traceability defect, not a factual one: add the EUVD record as a corroborating `sources[]` entry and cite it inline. The TrueConf entry in this same run does precisely that for its own CVE metadata, so the run is internally inconsistent on the point.

### Needs more research

**F8-1 — `btr-reforged-defender-remediation-driver-kernel-primitive`: the frontmatter says the PoC is public; the body never tells the reader.**

`tags: [vulnerabilities, no-patch, poc-public, lpe, priv-esc]`. The body's closing framing is:

> Check Point observed no real-world abuse of BTR.sys in the manner it demonstrates, which is what makes this a detection-engineering window rather than an incident.

The cited Check Point post links three separate times to `https://github.com/Dump-GUY/BTR_CLI` and describes the tool as "a fully functional Proof-of-Concept (PoC) demonstrating the offensive utility of the Microsoft Boot Time Removal driver (BTR.sys)" whose "source code implements a complete exploitation chain". It also records that the tool was tested successfully from Windows 7 Build 7601 through Windows 11 25H2. A publicly released, universal, working implementation is the fact that decides whether a SOC schedules the `SeLoadDriverPrivilege` review this week or next quarter, and it is exactly the kind of exploitation-status/prerequisite depth the source plainly supported. State it in prose (no URL — IOC/tooling-link discipline aside, the sentence does the work).

**F8-2 — `uat-10147-spectre-byovd-kernel-callback-unlinking-agentic-ai`: every initial-access CVE identifier the source names is dropped.**

> Initial access is entirely old, already-patched, publicly disclosed flaws in internet-exposed software: unauthenticated remote code execution in Zimbra Collaboration Suite, a deserialization flaw in AjaxPro, arbitrary code execution in Nacos through a scripting service-provider interface that ends in a shell, and the long-lived Telerik UI for ASP.NET AJAX deserialization flaw

The cited Talos agentic-AI post gives all of them by identifier: **CVE-2022-27925** (Zimbra unauthenticated RCE), **CVE-2021-23758** (AjaxPro deserialization RCE), **CVE-2021-29441** and **CVE-2021-29442** (Nacos arbitrary code execution), **CVE-2019-18935** (Telerik UI for ASP.NET AJAX .NET JSON deserialization — with the probe-then-fingerprint-then-weaponised-PoC sequence the entry does describe). The Linux chain is likewise fully identified (CVE-2022-0995, CVE-2021-3156 "Baron Samedit", CVE-2015-5287 ABRT, CVE-2015-3246 libuser, CVE-2010-3904 RDS, CVE-2022-0847 "Dirty Pipe") and the entry compresses it to "kernel flaws dating from 2010 to 2022", which is fine. The initial-access ids are not fine: the Defender takeaway ends on "an internet-facing Zimbra, Nacos or Telerik instance nobody owns is the whole precondition", and a reader cannot check that against an estate without the identifiers. Name them inline; they need not enter `cves[]` (reserved here, correctly, for the two BYOVD drivers).

### Missed angles

**F10-1 — the Elementor Pro deferral is avoidable: CVE-2026-32475 was reachable on two transports this run used successfully.**

The run record defers it as a coverage-backlog row:

> not published — a vulnerability entry that cannot state which version to patch to is filler, and inventing the missing fields is the exact defect class the gate exists to stop. … The teaser carries no CVE id, no CVSS, no affected/fixed version boundary and no mechanism.

Every one of those four fields is available, and I retrieved all of them this pass without touching the blocked Wordfence article body:

1. **The ENISA EUVD API** — the same recipe that supplied both TrueConf CVE records this run — returns `EUVD-2026-62595`: `aliases: CVE-2026-32475`, description *"Unrestricted Upload of File with Dangerous Type vulnerability in Elementor Elementor Pro allows Using Malicious Files. This issue affects Elementor Pro: from n/a through 4.2.1."*, `baseScore: 9.0` (`CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:C/C:H/I:H/A:H`), `epss: 0.42`, `assigner: Patchstack`, `datePublished: Aug 19, 2026`, `dateUpdated: Aug 20, 2026` — in-window.
2. **Patchstack** — the assigning CNA and the originating discloser, whose research post the direct bridge reads cleanly (133 KB): `https://patchstack.com/articles/critical-unauthenticated-file-upload-to-rce-in-elementor-pro-plugin`. Headed *"Elementor Pro Unauthenticated Arbitrary File Upload to Remote Code Execution CVE-2026-32475 CVSS 9.0"*. It carries the mechanism — *"In version 4.2.1 and below, the plugin's Forms module handles an uploaded file in two independent steps: it validates the file's extension in one loop, and it moves the uploaded file to a public directory in a second loop. These two loops treat empty [entries differently]"* — the upload-URL disclosure path (the confirmation email returns the precise upload URL to the attacker-controlled submitted address, "with no brute-forcing at all"), and the fix: *"Update Elementor Pro to version 4.2.2 or later."*

So the item is a pre-auth arbitrary-file-upload-to-RCE at CVSS 9.0, disclosed in-window by the CNA that assigned it, on a plugin family that Swiss federal, cantonal and communal web estates run widely — a widely-deployed-tech CVE with exposure-driven urgency, which is the exposure class the relevance gate admits, not a routine patch-cycle item. The backlog row's stated ground ("the article body is unreachable") is true of Wordfence and irrelevant to publication: the entry never needed Wordfence. This is the one silent blind spot I found in the window. Suggested query if a fresh pivot is preferred to the URL above: `Patchstack Elementor Pro CVE-2026-32475 4.2.2 arbitrary file upload`.

Note the second-order point for the run record: this run added Wordfence as a candidate source specifically because it was "the originating discloser" for two earlier entries — but for this item Wordfence is *not* the originating discloser; Patchstack is (it is the CVE assigner). Patchstack is untracked in `sources/sources.json`, and the one-candidate-per-run rule means it cannot be added this fire; the entry can still cite it.

### Editorial / less-is-more flags (advisory)

**F11-1 — Berlin: "independently" overstates a shared wire.** The sourcing_note says "heise independently reported the Senate Chancellery's statements two days earlier." berlin.de's byline is `Autor:in: dpa/BerlinOnline`; the heise article's byline is `Von Nico Ernst mit Material der dpa`. Both derive from the same dpa report of the same Senatskanzlei statement. Because the entry already prices itself at `verification: single-source-victim`, nothing downstream is wrong — but drop "independently" or qualify it. (The entry's *other* one-wire caution, about the two outlets relaying Reuters, is correctly applied: heise is dpa and wiwo is Reuters, so those two are genuinely different wires, and the entry does not count either as a second source.)

**F11-2 — ShieldBreak: the summary invents a midpoint.** Summary: "…in roughly ten seconds." LevelBlue says "The exploit completes in approximately eight to12 seconds on an unloaded system", which the body carries correctly as "roughly eight to twelve seconds on an idle system". Use the range in both places; no source says ten.

**F11-3 — BTR Reforged: `vulnerabilities` and `no-patch` tags on a deliberately-not-a-vulnerability item.** The body's opening thesis is "it is not a vulnerability at all", the summary records that MSRC declined to service it, and the takeaway says "there is nothing to patch". Defensible for discoverability; flagged so the choice is deliberate.

**F11-4 — TrueConf: an inline-cited source missing from `sources[]`.** The body cites `https://euvd.enisa.europa.eu/enisa/EUVD-2026-62587` for the CVE-2026-72529 mechanism, but only EUVD-2026-62588 appears in `sources[]`. I verified 62587 through the API and it supports the clause exactly (`aliases: CVE-2026-72529`; "could execute an arbitrary script by calling an undocumented function"; `baseScore: 9.3`, CVSS 4.0; `exploitedSince: Aug 20, 2026`). Adding it makes the id-to-flaw pairing the sourcing_note describes fully traceable.

**F11-5 — Berlin: `event_date` points at the corroborating source, not the primary.** `event_date: "2026-08-17"`; the `role: primary` berlin.de page states "Veröffentlichung: 19. August 2026". `docs/pipeline.md` defines the field as "recency anchor of the underlying event (primary-source publication date)". Reading it as "first public disclosure of the compromise" (heise, 17 Aug) is defensible, but it renders the entry two days staler than its primary. Worth a deliberate call.

### Checked and found sound — the judgement calls I was asked to challenge, and did

I state these explicitly because a defensible negative is a signal, and because on each one I looked for the defect and it was not there.

1. **Berlin publish decision, mapping, and sourcing value.** The `techniques: [T1566.001, T1204.002]` mapping is evidence-bound, not bolted on. I read the WirtschaftsWoche page and the quoted German sentence is a verbatim contiguous substring: "Nach Reuters-Informationen aus Sicherheitskreisen hatte es vor mehr als einer Woche einen erfolgreichen Cyberangriff auf die genannten Senatsverwaltungsbereiche über eine PDF-Datei mit Schadsoftware gegeben, die ein Mitarbeiter in einer E-Mail geöffnet hatte." A malware-bearing attachment plus a staff member opening it is precisely what those two ids denote, nothing further is inferred, and the body goes out of its way to fence the inference ("specifically do not infer an exploited PDF-reader flaw, because nobody has said one was involved"). This is not the bolted-on-access-vector class: that defect is a mapping with *no* source behind it, and this one has a source, correctly de-rated. `single-source-victim` is the right value — berlin.de is the government describing its own compromise (press conference at the Rotes Rathaus, Wegner and Spranger both quoted), and the vector claim is separately de-rated in the sourcing_note ("the vector claim alone would rate B3"). No wire double-count (see F11-1 for the one wording fix). One derivation I checked rather than assumed: neither berlin.de nor heise prints "14 August" — both say "bereits am Freitag". heise published Monday 17.08.2026 17:40, so the preceding Friday is unambiguously 14 August. Correct inference; I am not flagging it.
2. **TrueConf at `notable`.** Defensible, and I would have reached the same call. Exploitation is real and authority-confirmed — I pulled the KEV feed directly: `catalogVersion 2026.08.20`, and CVE-2026-72530 ("TrueConf Server Code Injection Vulnerability") and CVE-2026-72529 ("TrueConf Server Missing Authentication for Critical Function Vulnerability") are that catalogue's *only* two additions, both `dateAdded 2026-08-20`; EUVD records both `exploitedSince Aug 20, 2026`. But KEV binds US federal agencies, not this constituency, the vendor is a Russian conferencing provider whose footprint in Swiss and European government estates is negligible, a fix has existed since 18 June, and the entry says all of this in its first paragraph and rests its value on the transferable collaboration-platform-as-distribution-channel lesson. `high` would have put a product almost nobody here runs at the top of the rendered window. The pairing also checks out against the per-CVE authority rather than the roundup: 9.3/9.5 CVSS 4.0, EPSS 0.28/0.34, ranges `< 5.3, 5.3 < 5.3.9, 5.4 < 5.4.9, 5.5 < 5.5.5` — all as published. Worth noting the entry omits one detail in its own favour: Kaspersky's KLCERT-26-058 rates attack complexity High, "it may take several attempts to successfully exploit the vulnerability". That supports `notable`, so its absence costs nothing.
3. **The Siemens S7 update's justification holds, verified against the PDF itself.** I extracted the advisory with `fetch_source.py pdf` (62,519 chars, 12/28 streams, byte-encoding decode) and diffed its content against the 2026-08-20 entry. The earlier entry carries the device list, the scan-service discovery, the snap7/python-snap7 AI-assisted tooling, the intent assessment, and its *own* triage analysis (which independently reaches two of the five detection axes). It does **not** carry: the five named detection classes as the agencies frame them, the gold-copy firmware verification, the engineering-workstation mapping, the DMZ/internet-facing patch prioritisation, the ProductCERT consultation step, the test-in-development step, the explicit protection-levels instruction, or the systems-integrator/MSP direction. All eight are verbatim in the PDF — e.g. "Configure protection levels ( such as write protection and read/write protection) on Siemens S7 Series devices" and "E ntities that rely on systems integrators or third - party managed service providers should share this advisory with those parties and request implementation of the following mitigations". The earlier entry's own sourcing_note asked a later fire to "re-read it and publish any delta", and this is that delta. Both `evidence[]` spans are contiguous in the extraction. And the sourcing_note's promise holds under test: **nothing numeric in the entry rests on the extraction** — the only figures are the controller model names, which appear in the PDF as "S7 - 200, S7 - 300, S7 - 400, S7 - 1200, and S7 - 1500" (the split-digit artefact the note warns about) and are written correctly in the entry. `T1046` is not in the advisory's own mapping appendix (which carries T1588.007, T1596.005, T1694 and the ICS T08xx set), but it is derived from a behaviour the advisory explicitly describes ("Sequential IP scanning on port 102"), which is the permitted basis.
4. **All five attribution modalities preserve their source's exact hedge.** DPRK/crates.io: Wiz says "The arrayref infrastructure substantially overlaps with operations attributed to recent North Korean actors", and names Sapphire Sleet (Microsoft's attribution of the *Mastra* campaign) and UNC1069 (Mandiant's, for the axios npm attack) — both attached to the earlier campaigns, exactly as the entry states. UAT-10147: all three modalities are correctly separated and I read each in Talos's own words — PentestGPT installation/use is "Talos observed the threat actor installing…" (observed); the operator-class claim is "assesses with moderate-to-high confidence" (assessed); DeepAudit is "While we have not directly observed the actor exploiting vulnerabilities discovered by DeepAudit in victim environments, we did observe the framework installed on their management server. Consequently, we assess with high confidence that they intend to use it" — which is precisely what the entry says. GTIG/ICE RELIC: "We assess with moderate confidence that UNC6293 is a sub cluster of ICE RELIC (formerly APT29)…" and, separately, "GTIG assesses with moderate confidence that UNC6293 and UNC7005 are related to a subcluster of ICE RELIC that we associate with initial access operations" — so the sourcing_note's extension of the moderate-confidence claim to UNC7005 is supported, not inferred. Head Mare: "PhantomCore malware is typically associated with the activity of the Head Mare APT group", and the entry calls it "an association, not a confirmed operator identification". ITG27 naming: X-Force says "ITG27 activity overlaps with threat actors publicly reported as Mustang Panda, Stately Taurus, UNK_SteadySplit, Camaro Dragon, Twill Typhoon, Polaris, and Earth Preta" — an overlap claim, and HoneyMyte is indeed absent from that list, as the sourcing_note says. Nothing has hardened.
5. **Both exploitation-status calls are right.** crates.io: RUSTSEC-2026-0260 (arrayref) says the version was "removed approximately 86 minutes later, with no evidence of actual usage", and I also read RUSTSEC-2026-0262, which is the append-only-vec advisory the entry refers to, plus 0259/0261 for the attacker-owned crates — all carry the same formula. StepSecurity's counterweight is exactly as characterised: "The stage-2 binaries were not recoverable from public sources, but a developer who was infected on Linux reported the post-execution behavior in the public RustSec thread." No primary reproduces it, so not marking the CVE exploited is correct. ShieldBreak: LevelBlue makes no in-the-wild claim anywhere in its text, and Microsoft's record — which I queried directly rather than through the blog — confirms it: `"exploited": "No"`, `"publiclyDisclosed": "Yes"`, `"latestSoftwareRelease": "Exploitation More Likely"`, `"baseScore": "7.8"`, `"vectorString": "CVSS:3.1/AV:L/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H/E:P/RL:O/RC:C"` (E:P = PoC, RL:O = official fix unavailable), and `revisions[0]` is version 1.1 dated 2026-08-19 with description "CWE added. Informational change only." Every claim in the entry's delta paragraph is confirmed field-by-field.
6. **The ATT&CK work is clean, including the two revoked-id replacements.** Every id on all 11 entries resolves in the pinned v19.2 dataset and none is revoked or deprecated. The run record's claim about the ShieldBreak source's mapping table checks out precisely: LevelBlue publishes T1562.001 and T1574.002; in the pin, `T1562.001 → revoked_by T1685` and `T1574.002 → revoked_by T1574.001`, and the entry carries the two recorded successors. No `threat`/`incident`/`vulnerability` entry has an empty `techniques[]`. Prose T-ids appear only where they earn it. All 25 `entities` keys across the run resolve to registry records, aliases included (`actor:storm-2945` carries alias `UNC7005` — and GTIG does say "UNC7005 (aka STORM-2945)", which I confirmed after an initially case-sensitive miss; `actor:midnight-blizzard` carries alias `ICE RELIC`; `tool:chocoshell-powershell-stealer` carries alias `CHERRYPIE`; `actor:mustang-panda` carries `HoneyMyte`).
7. **`actions[]` discipline: no findings.** Seven of eleven entries ship `actions: []`, which is the right call on each (the two research/awareness items, the two out-of-nexus-lesson threats, the two updates whose delta changes no task, and the incident cluster with no endpoint signal). The four non-empty lists total five actions, every one concrete, self-contained, start-now and derived from its own entry's mechanics — the lockfile/cache grep with literal version strings, the timed compromise-assessment window, the ATutor decommissioning inventory, and the two S7 items. No generic advice, no body restatement, no hedging, no duplication of an in-window action (the S7 update's actions are distinct tasks from the 2026-08-20 entry's internet-exposure inventory), no padded list.
8. **Classification: no findings.** All 11 entries carry a `classification` block, all codes in vocabulary, no `org_triage` block anywhere (correct — no scheme configured), no `watchlist_hit: true` and no `watchlist` tag anywhere (correct — no watchlists configured). Reliability letters track the cited sources' nature and `sources/sources.json`: `B` on all five research-lab items (checkpoint-research, mandiant-gtig, ibm-xforce, talos, trustwave-spiderlabs are each `reliability: B` in the source list); `A` on cert-pl (`A`, essential), on the five-agency advisory, on the government-portal Berlin disclosure, and on the French cluster where DGCCRF's own press release is a co-primary. Credibility numbers track actual corroboration: `1` only where a second party genuinely confirms (four independent parties on crates.io; MSRC alongside LevelBlue on ShieldBreak), `2` on every single-assessor item.
9. **Style discipline: no findings.** Zero IOCs across all 11 entries — no hashes, no IP addresses, no attacker domains, no rule code — which is notable given that four of the primaries are hash- and IP-dense (the Havencode C2 table, the Wiz IOC table, the GTIG sample hashes, the SPECTRE driver analysis). Zero vanity metrics. English throughout, with foreign-language quotes correctly presented and translated. The published § Verification & coverage notes body is free of workflow-internal language. ("main agent" and "Phase 4" do appear in the run record's `sub_agents[].note` frontmatter fields — I checked `site/build.py` and `_ops_render_subagent_card` does not render the `note` field, so this is repo-internal telemetry rather than reader-facing text. Not raised as a finding.)
10. **Dedup and update decisions: no findings.** All four `update_of` targets and all thirteen `references` targets exist on disk. `prior_coverage.json` (150 records) shows zero prior coverage of arrayref/crates.io, ATutor, TrueConf, Head Mare, PhantomCore, UAT-10147, SPECTRE, Havencode, Claimloader, HEADRUSH, UNC6293/5976/7005, Bloctel, or the Berlin compromise; ShieldBreak (3 records) and Toneshell (1) and ZeroBytes (1) are all correctly routed as `update_of` deltas against them. No F15 name collision: "SPECTRE" and "BTR" have no prior-coverage referent, and the ShieldBreak PoC's internal `BERLIN` placeholder never surfaces in published text, so it cannot collide with this run's Berlin incident. Each update's body carries only its delta, and the ITG27 entry is explicit that it is different-lab research on a covered actor — which is what the dedup polarity requires rather than a fresh entry.
11. **The two other drops I was asked to challenge both hold.** The Swiss-headquartered extortion ultimatum: leak-site claim only, no victim statement, no samples, no regulator notice. Publishing it would be the exact "leak-site claims as fact" pattern the fake-news guard names, and the run record's decision to re-check next fire because the deadline is today is the correct disposition. The Cisco Crosswork / Secure Workload hardening releases: internally discovered, not known to be exploited, no exposure-driven urgency, and the store already published this grouped-hardening-release pattern on 2026-08-08 — squarely the routine-patch-cycle exclusion, high CVSS notwithstanding. (I did not independently re-fetch the two Cisco advisories; the drop rests on a criterion the pipeline states, not on a fact I need to verify.) Neither is a finding.
12. **No F6 and no F12.** No entry cites an NVD/MITRE/cve.org per-CVE page. The ATutor entry's sole source is a national-CERT page on a CVE item — the pattern check 6 names — but CERT Polska here is the coordinating CSIRT that received the report and owns the advisory (credits: "Michał Majchrowicz and Marcin Wyczechowski from AFINE Team"), the product is end-of-life with no vendor PSIRT to promote to, and the org profile lists CERT-PL in the carve-out. The entry declares `verification: single-source-national-cert` with a sourcing_note naming the basis, so the carve-out is correctly taken and reader-visible. All four other single-source items likewise carry the correct `verification` value plus a sourcing_note naming what stands on one assessor, and the run record's § Single-source items and carve-outs paragraph enumerates all six. No F9: I found no contradiction between co-cited sources that the entries resolve silently — the one genuine tension (crates.io exploitation status) is carried openly in both the body and the sourcing_note, which is the correct handling.

### Verdict

NEEDS_FIXES (truth: 3, editorial: 4, advisory: 5)

The run is in good shape and the substance is sound: sourcing is strong, every URL resolves to a specific primary, every one of 33 evidence quotes survives a literal contiguous-substring test, all five attribution modalities keep their source's hedge, both exploitation-status calls are correct, the ATT&CK revoked-id handling is exactly right, and the Siemens S7 update is genuinely justified against the PDF. The truth findings are three small, surgical fixes (one misattributed co-citation, one wrong CVE `type`, one wrong parameter name). The one finding that materially changes what the reader gets is **F10-1**: an in-window, CVSS 9.0, pre-auth file-upload-to-RCE in very widely deployed WordPress tooling was deferred on the ground that its fields were unobtainable, when the CNA's own research post and the EUVD API — both transports this run used successfully — carry every field. Completeness, not soundness, is where this window falls short.

### Findings summary (machine-readable)

See `work/2026-08-21T0410Z-intel/verification.iter1.findings.yaml` (12 records, same payload).
