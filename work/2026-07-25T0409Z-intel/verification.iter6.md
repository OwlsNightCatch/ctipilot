**Model:** Sonnet 5 (`claude-sonnet-5`)
**Timestamps:** started_at=2026-07-25T06:01:33Z · ended_at=2026-07-25T06:14:48Z · duration_seconds=795

## Verification report — 2026-07-25T0409Z-intel (iteration 6, confirmation pass)

Cold read of all 7 new entries + the run record, independent of iteration 5's judgement. Every inline source URL was fetched this iteration (WebFetch, `fetch_source.py url`, or direct curl with a desktop-Chrome UA where WebFetch/bridge failed); every CVSS score, evidence quote, entity relation, dedup claim, and action item was cross-checked against the fetched page or the repo's own state files (`entities/registry.yaml`, `state/cves_seen.json`, `work/<run-id>/prior_coverage.json`).

### Editorial / less-is-more flags (advisory)

- **F11** — entry `2026-07-25/ta458-roundpress-webmail-zero-days-sogo-cve-2026-8496`, source record `{url: "https://github.com/Alinto/sogo/releases/tag/SOGo-5.12.8", date: "2026-07"}`. The GitHub release page itself (fetched three times via `WebFetch`, consistent each time) shows "QHivert released this 12 May 13:23" with the release body referencing a preceding nightly build "of the 8th of May 2026" — i.e. the actual publish date of SOGo 5.12.8 is **2026-05-12**, not `2026-07` as the frontmatter's corroborating-source date field states. The release notes also still carry the placeholder "CVE ID will be updated once they're created" with no visible "CVE-2026-8496" string on the page — the CVE-to-version mapping in the entry rests on Proofpoint's own primary-source claim ("it was patched as CVE-2026-8496 in version 5.12.8", confirmed verbatim on the Proofpoint blog, fetched separately), which the GitHub release does not contradict, it simply doesn't itself carry the CVE number. This is a citation-metadata date discrepancy on a non-primary (corroborating) source; it does not affect any claim in the body or any `evidence[]` quote, and the underlying CVE/version/vendor facts are independently confirmed by the primary source. Advisory-level: worth a one-line date correction (`"2026-07"` → `"2026-05"` or `"2026-05-12"`) at the main agent's convenience; does not block publication.

### What I verified and found sound (no findings)

- **Certighost / CVE-2026-54121**: MSRC vulnerability-update-guide API confirms CVSS 8.8 (`CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H/E:U/RL:O/RC:C`), `publiclyDisclosed: No`, `exploited: No`, release 2026-07-14 — matches frontmatter exactly, and the `evidence[]` quote is verbatim. The CybersecurityNews corroborating article (fetched successfully via `curl` with a desktop UA after `WebFetch`/bridge/jina all failed on it — jina pool is HTTP-402-exhausted per the run record's own fetch_failures, third consecutive day) verbatim-supports every mechanism claim in the body: the `cdc`/`rmd` chase attributes, the `SERVER_TRUST_ACCOUNT` flag (value 8192) + SID-comparison patch gate, the default `ms-DS-MachineAccountQuota` (10) self-registration path, PKINIT, and the DCSync/krbtgt outcome. `vector: zero-click` + `auth: post-auth` is correct per `site/taxonomy.yaml`'s own definition ("victim-interaction requirement only... an authenticated, no-interaction bug is `vector: zero-click` + `auth: post-auth`") — not a defect, as I initially suspected before reading the taxonomy comment.
- **Check Point CVE-2026-62144 / CVE-2026-62145**: Check Point's own sk185152/sk185153 pages (fetched, `__NEXT_DATA__` JSON extracted) confirm the exact `evidence[]` quotes verbatim and confirm sk185152 carries only `severityType: "High"` with no numeric CVSS field — matching the entry's claim that Check Point publishes no numeric score for CVE-2026-62144. The CVE.org CNA record (`cveawg.mitre.org/api/cve/CVE-2026-62145`) confirms Check Point's own CNA-submitted `cvssV3_1.baseScore: 7.5` (`AV:N/AC:H/PR:L/UI:N/S:U/C:H/I:H/A:H`) exactly matching the frontmatter's `cvss: "7.5"` for CVE-2026-62145. NCSC-NL's advisory page lists all three CVEs with CVSS v4 scores exactly as cited: CVE-2026-16232 10.0, CVE-2026-62145 9.4, CVE-2026-62144 10.0. CERT-FR's advisory confirms the three CVEs and cites the same sk185152/sk185153 dates (22 July 2026). This is the exact CVSS-attribution fix iteration 4 made and iteration 5 confirmed — my independent re-fetch of every source (Check Point, cve.org CNA record, NCSC-NL, CERT-FR) confirms it is still correct with no regression.
- **TA458/RoundPress + SOGo CVE-2026-8496**: Proofpoint's blog (fetched) verbatim-supports the GRU attribution quote, the SOGo/Alinto/CVE-2026-8496/5.12.8 claim, the half-click definition quote, the TA422-no-overlap quote, the SpyPress name, the Ukraine/Albania/Greece/Moldova/Türkiye targeting plus "occasional targeting of chemical, telecommunications, and technology firms" (verified verbatim — matches the entry's "occasional chemical, telecom and technology victims"), and the Roundcube CVE-2025-49113 GPG-deserialization-gadget chain. ESET's welivesecurity source confirms the Sednit/APT28 attribution "with medium confidence" for the original 2025 RoundPress reporting — matching the entry's stated attribution divergence exactly.
- **LAUNDRY BEAR / ZimReaper update**: Proofpoint's second blog (fetched) verbatim-supports the CSS-`@import` sanitizer-bypass-by-reassembly quote, the ZimReaper name, the DNS-tunnelled-exfiltration Base32 schema, the `CreateAppSpecificPasswordRequest`/"ZimbraWeb" app-password persistence mechanism (bypasses 2FA, survives password reset + patch), the exact TA458-non-usage quote (this iteration's fetch confirms the single contiguous sentence iteration 2 fixed to is indeed verbatim on the page), the "upstream Russian intelligence taskmasters" phrase, and "no activity from TA488 since February 2026." CISA AA26-204A itself could not be re-fetched this iteration (403 direct + jina-402, matching the run record's own known 3-day jina-pool-exhaustion note) but `WebSearch` independently confirms it is a real, currently-existing CISA advisory page at that exact URL — not a hallucinated link.
- **Microsoft Email Threat Landscape Q2 2026**: Microsoft's blog (fetched) verbatim-supports both `evidence[]` quotes (Teams-vishing "nearly ten times the mid-2025 baseline," credential-phishing "94–96% of all payload-based attacks each month"), the PDF→DOC/DOCX drift and QR-code near-zero collapse tied to Tycoon2FA disruption, the Python/`email.mime`/Amazon-SES BEC campaign (67,000+ users, 42,000+ organizations, under 3 hours, role-based mailboxes), and the EML-voicemail/multi-tenant-Entra-app/OAuth-redirect/BAT-dropper chain. The body's "obscured true destination from scanners and recipients" is correctly unquoted paraphrase (not presented as a verbatim quote) — the iteration-2 F3 fix holds; no regression.
- **Stiftung Autismuslink**: downloaded and text-extracted the actual victim PDF (`pypdf`, byte-preserving). Both `evidence[]` quotes are exact verbatim substrings of the extracted German text. The body's "the relevant authorities were notified and a criminal complaint filed with the police" matches the PDF's "Die offiziellen Stellen vom Bund wurden sofort über die Umstände informiert und es wurde Anzeige bei der Polizei erstattet" — no NCSC naming, confirming the iteration-4 fix holds. The data-class list (IV, BKD, teacher contracts, doctors' certificates, 2016–2023 client dossier archive) matches the PDF's bullet list. Ransomware.live's page confirms the INC Ransom leak-site claim against autismuslink.ch dated 2026-07-24. No sector-focus claim about INC Ransom remains in the body (iteration-4 fix confirmed).
- **Thailand MoF / Hermes AI agent**: Hunt.io's blog (fetched) verbatim-supports the 585-files/470MB figure, the YOLO-mode quote, the LinPEAS/CVE-2026-43503/31431/43284/43500 probe list, the Hades implant capabilities (persistence, HTTPS-blending C2, AES-256-GCM, kill-dates), the HiveServer2/Ambari/GlassFish exploitation chain, Bob Diachenko's credit, the low-to-medium-confidence Chinese-speaking-operator assessment, and the 2026-07-15 ThaiCERT/NCSA notification date. BleepingComputer's article (fetched) confirms the exact "has not confirmed that its systems were breached... only show that particular systems were targeted rather than successfully compromised" quote and confirms it is a re-report, not independent corroboration — matching the entry's own sourcing_note. `state/cves_seen.json` confirms CVE-2026-43503/31431/43284/43500 and Roundcube's CVE-2025-49113 are all pre-existing in the store, validating the run record's dedup-notes claim that these were correctly excluded from `cves[]` on both the Thailand and RoundPress entries.
- **Dedup / entity checks**: `entities/registry.yaml` confirms `actor:ta458-roundpress`, `malware:spypress`, `tool:hermes-ai-agent`, `tool:hades-implant`, `incident:thailand-finance-ministry-hermes-ai-agent-2026`, and `report:microsoft-email-threat-landscape-q2-2026` are all newly and correctly registered, with `tool:ulej-flowerbed` carrying the pre-existing "ZimReaper" alias (confirming the LAUNDRY BEAR/ZimReaper entry did not create a duplicate entity — no F15 name-collision). Both `update_of` targets (`2026-07-23/check-point-smartconsole-auth-bypass-cve-2026-16232`, `2026-07-24/laundry-bear-zimbra-zero-click-cve-2025-66376`) resolve to real files on disk.
- **Action items (F18)**: all 5 actions across the 4 entries that carry any are concrete, self-contained, do-now tasks tied to each entry's own mechanics (patch-confirmation, hotfix-application, SOGo-patch + webmail-inventory, app-password revocation) — no generic advice, no restated body guidance, no padding, no duplicates. Matches `check_run.py`'s own `action-items` PASS.
- **Classification (F17) / org-triage (F16) / priority calibration (5b)**: all 7 entries carry exactly one Admiralty `classification` block (no `org_triage`, correctly, since no triage scheme is configured for this deployment); reliability/credibility letters and numbers are all defensible against the cited sources' own tier in `sources/sources.json` and the corroboration each entry actually shows (spot-checked Check Point's PSIRT knowledge-base domain, MSRC, Proofpoint-single-vendor credibility-2 fixes from iteration 2). No entry clears the `critical` bar without also being actively exploited/imminent — all three `high` entries and four `notable` entries are correctly calibrated; no false critical, no under-alerted `notable`.
- **Relevance / nexus (check 5)**: the two out-of-nexus items (Autismuslink — in-nexus via home region; Thailand MoF — genuinely out-of-nexus APAC) both explicitly earn their place: Autismuslink via direct Swiss home-region/public-sector nexus, Thailand via an explicitly-named transferable TTP ("unattended AI-agent post-exploitation transferable to any government or finance-sector network") — the entry states which of the four higher-grounds criteria it clears, as required.
- **IOC/style discipline**: no hashes/IPs/domains in any entry (matches `check_run.py`'s `ioc-scan` PASS); no workflow-internal language leaked into any entry or the run record.
- **Mechanical gate**: `python3 tools/check_run.py 2026-07-25T0409Z-intel` — 37 pass · 0 warn · 1 fail, the expected `verification-confirmation` FAIL this pass resolves.

### Verdict

CLEAN — one F11 advisory-level finding (corroborating-source date field on the RoundPress/SOGo entry), no truth defects, no editorial defects. This is the second-model (Sonnet) confirmation of iteration 5's (Opus) CLEAN — the double-CLEAN publish gate is satisfied.

### Findings summary (machine-readable)

```yaml
- code: F11
  category: editorial-advisory
  section: null
  item: "2026-07-25/ta458-roundpress-webmail-zero-days-sogo-cve-2026-8496"
  url_or_quote: "sources[] record: {url: \"https://github.com/Alinto/sogo/releases/tag/SOGo-5.12.8\", date: \"2026-07\"}"
  summary: "The GitHub release page (fetched 3x via WebFetch, consistent) shows 'QHivert released this 12 May 13:23' with the release body referencing a preceding nightly build of '8th of May 2026' -- actual publish date is 2026-05-12, not 2026-07. Does not affect any body claim, evidence[] quote, or the CVE-2026-8496 attribution (independently confirmed on Proofpoint's primary blog). Advisory-level date-field correction only; does not block publication."
```
