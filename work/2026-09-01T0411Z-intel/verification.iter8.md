**Model:** Sonnet 5 (`claude-sonnet-5`)
**Timestamps:** started_at=2026-09-01T06:58:31Z · ended_at=2026-09-01T07:10:55Z · duration_seconds=744

## Verification report — 2026-09-01T0411Z-intel (iteration 8)

### Prior-iteration deltas — re-verified

1. **Exchange MRSProxy entry, MB VRED paragraph.** Re-fetched `https://vred.mbbank.com.vn/p/analysis-of-exchange-server-pre-auth` directly. All four new `evidence[]` quotes are verbatim substrings of the page: "The attacker sits inside the network, especially inside a domain-joined PC!"; "This one works only for multiple Exchange servers setup because the captured hash cannot be relayed to itself!"; "It requires lot of non-realistic conditions to be exploited in the real world:"; "So, for the defensive guys, don't be panic!". The body now correctly frames the domain-joined-PC/two-server precondition as "MB VRED's own most plausible hypothesis — not a confirmed finding" (the source itself says "The most plausible hypothesis to answer this question is: *The attacker sits inside the network...*"), includes both new caveat quotes, and no longer attributes a PR:L-consistency claim to MB VRED (the source never mentions MSRC/CVSS/PR:L at all — confirmed by full-text read). Remediation is accurate and complete.
2. **ValleyRAT entry / registry relation note.** Body: "That campaign postdates a June 2026 Chinese police crackdown... this QN Wallpaper campaign's own timing is not independently established beyond Kaspersky's 2026-08-31 publication date, so it cannot be dated relative to the arrests with the same confidence." Registry `actor:silver-fox` → `incident:silver-fox-arrests-china-2026` relation note carries the identical caveat. Cross-checked against The Hacker News ("about five weeks earlier" — Cato/Japan campaign) and Risky Bulletin ("67 suspects... arrests took place across five provinces", 2026-06-17): the arithmetic (five weeks before 2026-08-31 ≈ late July, after the 17/18 June arrests) supports "postdates" for the Cato campaign only, and the QN Wallpaper campaign is correctly left undated relative to the arrests in both locations. Remediation is accurate.

### Independent cold pass — additional checks performed

- Fetched and cross-checked every inline source on all three new entries and the changed portions of all four updated entries against the live pages: `docs.jfrog.com/releases/docs/jfrog-security-advisories`, `docs.jfrog.com/releases/docs/artifactory-self-managed-releases` (confirmed 7.111.20/7.111.21 and 7.146.36/7.146.38 release-note detail directly, not just the summary table), `github.com/advisories/GHSA-c5pf-6p5j-gj87`, `ionix.io/threat-center/cve-2026-82329`, `securelist.com/valleyrat-backdoor-adware`, `thehackernews.com` (ValleyRAT), `news.risky.biz` (Silver Fox arrests), `bleepingcomputer.com`, `helpnetsecurity.com`, `darkreading.com` (Anthropic), `github.com/advisories/GHSA-v4p8-mg3p-g94g`, `nzz.ch` (Liechtenstein), `inside-it.ch` and `netzwoche.ch` (HWZ), `msrc.microsoft.com` (via jina), `social.bund.de/@certbund`, `heise.de`, `advisories.ncsc.nl` (via jina), `frankysweb.de`. All evidence[] quotes checked this iteration were verbatim substrings of the fetched pages, including both German-original/English-translation pairs on the Liechtenstein entry (checked against the live NZZ page).
- `git diff HEAD` run against all four updated entries: every changed line in each is covered by that run's `updates[]` record `fields[]`; no silent edits found.
- Confirmed `state/cves_seen.json` records for CVE-2026-82329 and CVE-2026-42271 (title now says "CVSS 8.7") are in sync with the published entries.
- Confirmed `check_run.py 2026-09-01T0411Z-intel` still reports exactly 46 pass · 1 warn (aggregator-only, already documented) · 1 fail (run-clock, expected pre-Phase-6) — nothing new.
- Checked `techniques[]` against the pinned ATT&CK dataset directly (`attack/enterprise-attack.json`): T1574.001 is the correct active id for DLL sideloading in this pin (T1574.002 is revoked→merged into T1574.001), so the ValleyRAT entry's mapping is correct, not an oversight.
- Registry check: no pre-existing `malware:winos*` or duplicate Silver Fox/ValleyRAT keys found; the three new entities (`actor:silver-fox`, `malware:valleyrat`, `campaign:claude-session-hijack-infostealers-2026`) are genuinely new, no name-collision.
- Missed-angles search (Swiss-cyber and actively-exploited-CVE queries for the 2026-08-31/09-01 window): nothing found beyond what the run record's own "Coverage gap" note already discloses (Aurora/Cursor AI, Gryxa, Fire Ant, Cl0p retrospective — already flagged as a structural window-boundary gap, not a miss this run could have closed). No new F10 to raise.

### Editorial / less-is-more flags (advisory)

**#1.** `entries/2026-08-29/exchange-mrsproxy-auth-bypass-cve-2026-62911-poc.md` — the `updates[]` record dated `2026-09-01T04:40:00Z` (`type: update`, not `internal: true`, so its `summary` is reader-facing per `docs/pipeline.md` §"The changelog record": *"This is the text the live timeline row, the day page's § Updates and the feed item show"* — confirmed against `site/build.py`'s `render_update_block()`, which renders `record.get("summary")` verbatim into `<p class="entry-update__summary">` for exactly these non-internal records) contains: *"the entry's `cves[].auth` field is corrected from post-auth to pre-auth to follow that weight of evidence."* This names a raw frontmatter field path in text that ships to readers — the same defect class iterations 3 and 4 already fixed twice this run (run-record notes' PD-11/S1-S4 shorthand; the Liechtenstein entry's sourcing_note referencing `techniques[]` directly). The body's own `## Update — 2026-09-01T04:40:00Z` section is clean, inline-cited prose with no jargon; only the changelog record's `summary` field is affected. Fix: rephrase to plain language, e.g. "...against MSRC's own CVSS vector alone (PR:L/UI:R, 'authorized attacker'); this entry's authentication precondition is corrected from post-auth to pre-auth to follow that weight of evidence."

### Verdict

CLEAN — one F11 advisory item (above), which the main agent may leave; no truth or blocking-editorial defects found after a full independent cold pass of all three new entries, all four updated entries (main analysis, frontmatter, and every changelog section), the run record, and the registry entities in scope. Both prior-iteration deltas re-verified correct against primary sources.

### Findings summary (machine-readable)

```yaml
- code: F11
  category: editorial-advisory
  section: updated-entries
  item: "CVE-2026-62911 Exchange MRSProxy entry — 2026-09-01T04:40:00Z update record"
  url_or_quote: "the entry's cves[].auth field is corrected from post-auth to pre-auth to follow that weight of evidence"
  summary: "The non-internal updates[] record's summary field (type: update, not internal:true) names a raw frontmatter field path (\"cves[].auth field\") — this exact text renders verbatim to readers via site/build.py's render_update_block() (<p class=\"entry-update__summary\">) on day pages and feeds per docs/pipeline.md's own description of what updates[].summary shows (\"the text the live timeline row, the day page's Updates section and the feed item show\"). This is pipeline-internal record-keeping narration leaking into reader-facing text, the same defect class iterations 3 and 4 already fixed twice elsewhere in this run (run-record notes; Liechtenstein entry's sourcing_note). The body's own \"## Update — 2026-09-01T04:40:00Z\" section is clean prose with no jargon — only the changelog record's summary field is affected. Fix: rephrase to plain language, e.g. \"...against MSRC's own CVSS vector alone (PR:L/UI:R, 'authorized attacker'); this entry's authentication precondition is corrected from post-auth to pre-auth to follow that weight of evidence.\""
```
