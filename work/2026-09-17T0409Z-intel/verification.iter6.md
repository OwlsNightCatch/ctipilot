**Model:** Sonnet 5 (`claude-sonnet-5`)
**Timestamps:** started_at=2026-09-17T05:54:32Z · ended_at=2026-09-17T06:03:29Z · duration_seconds=537

## Verification report — 2026-09-17T0409Z-intel (iteration 6)

Confirmation pass following iteration 5's CLEAN (truth=0, editorial=0, advisory=1, remediated). No prior-iteration deltas block was attached (per protocol for a post-CLEAN confirmation pass); this pass anchors on the run's current output only, read cold, independently re-deriving every check rather than trusting iteration 5's verdict.

Scope covered this iteration: all 7 new entries end-to-end (frontmatter + body), the run record (frontmatter telemetry + verification-notes body), `work/2026-09-17T0409Z-intel/prior_coverage.json` (73 records, 14-day window) and `entities/registry.yaml` for dedup/entity-collision checks, `state/cves_seen.json` for the three CVE ids in this run, the pinned ATT&CK v19.2 dataset for every technique id used across all 7 entries, and `tools/check_run.py` (47 pass · 0 warn · 1 fail — the fail is exactly the expected `verification-confirmation` gate this pass exists to resolve).

Every inline source URL across all 7 entries was fetched fresh this iteration (Cisco PSIRT ×3 advisories, CISA KEV alert page ×2 + JSON feed, Google Pixel bulletin, MITRE CVE record, TechCrunch, FrenchBreaches, Ransomware.live, Ayuntamiento de Velilla de San Antonio, AEPD blog, heise ×2, ddropattack.eu, Intel PSIRT, AMD Product Security bulletin, CrowdStrike (extract + raw HTML for a paragraph trafilatura dropped), Endor Labs, BleepingComputer, Mandiant's Google Cloud report page, Help Net Security). Every `evidence[]` quote was checked as a literal/near-literal substring against the fetched page text; both non-English `evidence[]` records (Kairos/Libercourt French, AEPD Spanish ×2) had their `original:` field grepped against the source page and the English `quote:`/`translated from` framing checked for fidelity — all matched. All three CVE ids (CVE-2026-76460, CVE-2026-76423, CVE-2026-58704) were cross-checked against `state/cves_seen.json` (none previously seen — genuinely new, no dedup miss) and against the vendor's own per-CVE authority (Cisco's fixed-release tables; the MITRE CVE record + CISA-ADP Vulnrichment block for the Pixel CVE). All 24 distinct `techniques[]` ids used across the 7 entries resolve to active, non-revoked, non-deprecated techniques in the pinned `attack/enterprise-attack.json` (v19.2).

No F1–F4, F6, F7, F9, F10, F12–F18 findings. Two items below.

### Claims missing inline citation

F5 — `ddrop-dram-interposer-defeats-confidential-computing`: the sentence "Intel says it is evaluating \"Platform Owner Endorsements\" — a mechanism to let remote parties verify who physically holds sensitive hardware — and next-generation memory-encryption schemes with built-in freshness protection" carries no inline citation. Intel's own PSIRT announcement (`intel-security-announcement-2026-08-11-001.html`, cited on the preceding sentence) states the Platform Owner Endorsements fact ("including the development of Platform Owner Endorsements which enable remote parties to establish who is in physical possession of the hardware") but says nothing about "next-generation memory-encryption schemes with built-in freshness protection." That specific framing is stated only by heise Security, cited elsewhere in the entry's `sources[]` ("Intel schränkte dazu jedoch ein, dass das zwar Out-of-Scope sei, jedoch nicht Out-of-Mind; das Unternehmen prüfe Speicherverschlüsselung der nächsten Generation mit stärkerem Hardwareschutz" — Intel qualified that while out of scope it is not out of mind, and is examining next-generation memory encryption with stronger hardware protection) and by the ddropattack.eu FAQ page's own paraphrase of an Intel keynote. Fix: attach the heise citation (or the ddropattack.eu FAQ anchor) to this specific sentence.

### Editorial / less-is-more flags (advisory)

F11 — `phantomraven-npm-llm-generated-infostealer`: `techniques[]` (`[T1195.001, T1552.001, T1552.007, T1071.001, T1016.001, T1082, T1036.005, T1072, T1027.009, T1587.001, T1041, T1005]`) still omits techniques CrowdStrike's own ATT&CK table (fetched fresh this iteration) maps for this exact malware, after five prior remediation rounds (iterations 2–5 progressively added T1016.001/T1082/T1036.005/T1072/T1027.009, then T1587.001/T1041, then T1005). Remaining gaps: **T1083** (File and Directory Discovery — CrowdStrike's table: "*PhantomRaven* searches for and reads `package.json` files, Git configuration files, and npm configuration files") is the clearest miss — the entry's own body already describes this exact behavior ("Git- and npm-configured usernames and emails") without the id. Also unmapped: T1583.001 (Acquire Infrastructure: Domains), T1059.007 (Command and Scripting Interpreter: JavaScript), T1614.001 (System Location Discovery), T1119 (Automated Collection), and **T1104** (Multi-Stage Channels) — the last corresponds to a fact dropped from the body entirely: both cited sources describe a WebSocket fallback exfiltration channel (CrowdStrike: "*PhantomRaven*'s code also contains an incomplete fallback WebSocket exfiltration method"; Endor Labs' infection-chain summary: "Data sent via HTTP GET → HTTP POST → WebSocket (redundant exfil)"), but the entry's body never mentions it. Low operational stakes — CrowdStrike itself calls the WebSocket URL "highly likely a placeholder and not malicious infrastructure" — so this is left as advisory rather than a technical-depth (F8) blocker.

### Verdict

NEEDS_FIXES (truth: 0, editorial: 1, advisory: 1)

Both findings are minor and narrowly scoped (one uncited sentence in one entry; one incrementally-narrowing ATT&CK-completeness gap in a second entry that has already been improved five times). No truth defects found anywhere in the run: every CVE id/CVSS/affected/fixed field, every named entity (Cisco, Google/Pixel, Kairos, Velilla de San Antonio, AEPD, DDRop/Intel/AMD, CrowdStrike/PhantomRaven, Mandiant/DARK CASTLE-UNC2814), every quoted evidence string, every case-study detail in the Mandiant deep dive, and the run record's own KEV disposition and coverage-backlog claims all check out against freshly fetched primary sources. Dedup is clean (none of the 3 new CVEs or 6 new entities collide with `prior_coverage.json`, `cves_seen.json`, or an existing `registry.yaml` key/alias; the Kairos/Libercourt entry correctly cross-references the existing Velilla de San Antonio entity and entry rather than duplicating it). The run-record notes body is free of workflow-internal language. Given the narrow scope, a targeted remediation (add one citation; add up to six technique ids to one entry) should clear this without disturbing anything else verified clean this pass.

### Findings summary (machine-readable)

See sibling file `work/2026-09-17T0409Z-intel/verification.iter6.findings.yaml` (reproduced below).

```yaml
- code: F5
  category: missing-citation
  section: ddrop-dram-interposer-defeats-confidential-computing
  item: "DDRop: a $159 DDR5 hardware interposer silently drops targeted memory writes..."
  url_or_quote: "Intel says it is evaluating \"Platform Owner Endorsements\" — a mechanism to let remote parties verify who physically holds sensitive hardware — and next-generation memory-encryption schemes with built-in freshness protection."
  summary: "This sentence carries no inline citation of its own. Intel's own PSIRT announcement (cited in the preceding sentence) states the Platform Owner Endorsements fact but does not mention \"next-generation memory-encryption schemes with built-in freshness protection\" — that specific framing is stated only by heise (\"das Unternehmen prüfe Speicherverschlüsselung der nächsten Generation mit stärkerem Hardwareschutz\") and by the ddropattack.eu FAQ, neither cited on this clause even though heise is in the entry's sources[]."
- code: F11
  category: editorial-advisory
  section: phantomraven-npm-llm-generated-infostealer
  item: "PhantomRaven: CrowdStrike attributes an LLM-generated npm infostealer..."
  url_or_quote: "techniques: [T1195.001, T1552.001, T1552.007, T1071.001, T1016.001, T1082, T1036.005, T1072, T1027.009, T1587.001, T1041, T1005]"
  summary: "CrowdStrike's own ATT&CK table (fetched this iteration) maps 18 techniques for PhantomRaven, six of which remain unmapped after five prior remediation rounds. T1083 (File and Directory Discovery — \"searches for and reads package.json files, Git configuration files, and npm configuration files\") is the clearest gap: the body already describes exactly this behavior (\"Git- and npm-configured usernames and emails\") without the id. Also unmapped, with weaker body support: T1583.001 (Acquire Infrastructure: Domains), T1059.007 (Command and Scripting Interpreter: JavaScript), T1614.001 (System Location Discovery), T1119 (Automated Collection), and T1104 (Multi-Stage Channels — the WebSocket fallback exfiltration method CrowdStrike and Endor Labs both describe but which the entry's body never mentions at all: CrowdStrike — \"PhantomRaven's code also contains an incomplete fallback WebSocket exfiltration method\"; Endor Labs' infection-chain summary — \"Data sent via HTTP GET → HTTP POST → WebSocket (redundant exfil)\")."
```
