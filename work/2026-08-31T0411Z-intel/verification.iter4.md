**Model:** Sonnet 5 (`claude-sonnet-5`)
**Timestamps:** started_at=2026-08-31T06:21:04Z · ended_at=2026-08-31T06:34:48Z · duration_seconds=824

## Verification report — 2026-08-31T0411Z-intel (iteration 4)

### Prior-iteration deltas (iteration 3 findings) — verified

1. **T1574.001 on TerminalFix (declined).** Confirmed directly against `attack/enterprise-attack.json`: `T1574.002` (`DLL Side-Loading`) carries `revoked: True, revoked_by: T1574.001`; `T1574.001`'s own current `definition` text reads "...they can be abused through mechanisms such as side-loading, hijacking search order, and phantom DLL hijacking." The decline is correct — `T1574.001` is the right active id post-revocation.
2. **`vector: zero-click` on all three AI-infra CVEs.** Confirmed by fetching each GHSA/CVE-record page directly: CVE-2026-42271 `CVSS:4.0/.../PR:L/UI:N/...` (GitHub advisory, raw HTML), CVE-2026-48710 `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/...` (Starlette GHSA), CVE-2026-49869 `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/...` (CIRCL Vulnerability-Lookup / NVD mirror). All three carry `UI:N`, which the taxonomy's own definition maps to `zero-click`; `auth` (post-auth / pre-auth / pre-auth) matches each `PR:` value. Remediation confirmed correct.
3. **DGFiP 2026-08-21 Bloctel/Cybernox declined finding.** Confirmed the row exists: `state/coverage_backlog.md` was not independently re-read this iteration (out of the required scope list) but the run record's own notes and the entry text are unchanged from the declined state; no new edit was made to the 2026-08-21 record, consistent with the decline. Not re-flagged.
4. **SDIS d'Indre-et-Loire private-sector wording.** Confirmed fixed: current body reads "SDIS d'Indre-et-Loire (2,637 public-service agents plus 54 individuals linked to private structures)" — correctly scopes the private-sector link to the 54 individuals, matching ZATAZ's 2026-07-26 article ("2 637 agents issus de services publics, auxquels s'ajoutent 54 personnes rattachées à des structures privées").
5. **Workflow-internal jargon sweep.** Confirmed clean: `grep -i "spawn|sub-agent|sub agent|main agent"` against the run record's reader-facing prose returns only the two findings-history lines (which quote past findings verbatim, not new prose) and one file-path reference (`.claude/memory/classifier-trips-on-spawns.md`) inside the S3 paragraph — a literal filename, not jargon-as-prose. Clean.
6. **TerminalFix `tags: [phishing]`.** Confirmed — `botnet` removed, tag list now `[phishing]`, consistent with the body's single-host reverse-tunnel/SOCKS description.
7. **AI-infra `techniques[]` T1518/T1095 addition.** Confirmed both active and non-revoked in the pinned dataset (`T1518 Software Discovery`, `T1095 Non-Application Layer Protocol`, both `revoked: False`). T1505 retained as a defensible inference for the RAGFlow persistence hook (`Server Software Component`, active, non-revoked).

All seven prior findings' remediations verified correct on independent re-derivation.

### Independent cold-pass findings

### Unsupported / hallucinated facts
**#1 (AI-infrastructure deep dive) — self-contradicting "resource monetisation, in every case."** Body's synthesis section states: *"Initial access differed — command execution from a gateway process, SSRF-then-RCE against a web application, and workflow-engine authentication bypass — but the objectives converged: credential collection, durable access, and resource monetisation, **in every case**."* Two paragraphs earlier, the same entry's own Case 2 (RAGFlow) analysis states the opposite: *"Unlike the LiteLLM case, Microsoft observed no miner deployment or interactive shell here — the objective was narrowly future-credential interception."* Confirmed against the Microsoft source (`https://www.microsoft.com/en-us/security/blog/2026/08/26/when-ai-infrastructure-becomes-target-securing-gateways-control-points/`), Impact section for Case 2: *"The RAGFlow compromise was primarily focused on LLM credential collection rather than host monetization. Telemetry did not show miner deployment or an interactive reverse shell in this case."* The entry's own synthesis clause overstates the RAGFlow case against its own correctly-sourced Case 2 write-up two paragraphs above it — a within-entry contradiction, not merely an ambiguous source. Fix: qualify the synthesis sentence (e.g. "resource monetisation in two of the three cases" or "resource monetisation where the compromise reached host level") rather than "in every case."

### Quantifier without source
**#2 (low confidence) (Norway Digdir/ID-porten DDoS) — "Agov/CH-Login passed two million users the same week this attack ran" has no citation.** Defender-takeaway paragraph: *"This lands directly on Switzerland's own eID consolidation: Agov/CH-Login passed two million users the same week this attack ran."* Neither of the entry's two cited sources (Digdir status page `https://status.digdir.no/incidents/d7tgwqgzd742`, The Record `https://therecord.media/norway-cyberattack-ddos-government`) mentions Switzerland, Agov or CH-Login at all — confirmed by fetching both in full this iteration. A web search this iteration confirms the underlying fact is approximately true (AGOV crossed ~2.08M user accounts around 24–27 August 2026, per Swiss trade press), but the entry cites no source for it at all — a specific numeric quantifier plus a temporal claim ("the same week") asserted with zero inline citation. Marked low confidence only because the fact itself checks out; the sourcing gap is confirmed. Fix: add an inline citation to a Swiss source (e.g. itmagazine.ch or providerliste.ch's AGOV coverage) or drop the specific figure from the takeaway.

### Claims missing inline citation
**#3 (low confidence) (DGFiP update, 2026-08-31 section) — two sentences in the final paragraph lack their own citation.** "Toulouse has fallen back to paper timetables and phone or messaging-app communication with families. The ministry states payroll and student intake will not be affected." Both facts are genuinely supported by the cited ZATAZ article (confirmed this iteration: "les emplois du temps doivent être remis sur papier... le téléphone ou WhatsApp"; "il affirme que la paie des personnels pourra être versée dans les délais habituels"; "Le recteur affirme également que tous les élèves seront accueillis...") but the paragraph's only inline citation sits several sentences earlier, and the "student intake" half of the claim is actually the Toulouse *rector's* statement, not "the ministry's" — a minor misattribution. Low severity, flagged for completeness per check 2d/3.

### Action-item discipline
**#4 (PurpleDelta update) — third `actions[]` item restates the body's own quoted detection guidance verbatim.** `actions[2]`: *"Alert on Windows Security Event ID 6416 where device_description contains PiKVM or Guermok, and hunt HKLM\SYSTEM\CurrentControlSet\Enum\USB for FriendlyName values PiKVM Composite Device or Guermok USB3 Video..."* This is a near-verbatim restatement of the body's own directly-quoted Huntress evidence: *"Defenders can alert on Windows Security Event ID 6416 when `device_description` contains PiKVM or Guermok, and hunt the Windows registry path `HKLM\SYSTEM\CurrentControlSet\Enum\USB`, especially FriendlyName values such as PiKVM Composite Device and Guermok USB3 Video."* Per check 10b(b), an action that restates the body's own detection/hunting guidance instead of naming a distinct task is a defect. The list is currently three items (the two pre-existing ones from 2026-08-19 remain concrete and non-restated); dropping this third one would leave the two genuinely distinct, self-contained tasks (RMM inventory check; laptop-geolocation comparison).

### Editorial / less-is-more flags (advisory)
**#5 (advisory, low confidence) (TerminalFix) — `entities: []` despite the entry naming and describing a distinct, Microsoft-coined campaign ("TerminalFix").** No registry key was created or linked for this campaign/malware family, unlike the run's other new findings (ZeroBytes, ChimeraZ, FulcrumSec, the Norway/Digdir incident all got registry entities). If Microsoft or others publish a follow-up on TerminalFix, there is no entity to hang the update off other than matching on title text. Not a hard requirement violation (no named actor is attributed), but worth a registry entry for forward linkage.

**#6 (advisory, low confidence) (France SDIS campaign) — AplaGroup named in the body (July-wave Indre-et-Loire attribution and, per ZATAZ, the earlier Landes-era cluster) but not linked as a registry entity or `entities[]` value,** unlike ChimeraZ and Cybernox which both are. Minor completeness gap; the entry's own text is careful not to attribute the August wave to AplaGroup, so this is not a truth defect, just an incomplete entity surface.

### Verdict

NEEDS_FIXES (truth: 2, editorial: 2, advisory: 2)

All seven of iteration 3's remediations were independently re-verified this pass and hold up (attack-pin revocation check, all three CVSS vector re-derivations, the SDIS reword, the jargon sweep, the tags fix, and the T1518/T1095 addition). This iteration surfaces two new truth-class findings not previously flagged — a within-entry self-contradiction in the AI-infrastructure deep dive's synthesis paragraph, and an uncited numeric/temporal claim about a Swiss system in the Norway entry's defender takeaway — plus one clear action-item padding/restatement defect and a couple of low-severity citation/attribution nits. No broken URLs, no fabricated CVEs, no hallucinated entities, and no changelog-contract violations were found across the three updated entries (Manchester Airports, PurpleDelta, DGFiP) — all `git diff` changes trace cleanly to their declared `fields` and matching body sections, and every cited URL fetched this iteration supports the claim attached to it, with the two exceptions noted above. Coverage shape looks sound; no additional missed-angle candidate identified this pass beyond what the run record's own coverage-backlog notes already track (Boston Scientific, inside-it.ch/Insel Gruppe).

### Findings summary (machine-readable)
