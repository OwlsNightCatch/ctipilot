# Findings summary (machine-readable)
- code: F4
  category: hallucinated-fact
  section: trending-vulnerabilities
  item: "GLPI 11.0.8 / 10.0.26 — critical RCE + MFA bypass"
  url_or_quote: "cves[] CVE-2026-49470: auth: pre-auth"
  summary: >
    CVE-2026-49470 frontmatter carries auth: pre-auth, but every cited source describes it
    as 'account takeover via 2FA brute force' (GitHub 11.0.8 changelog line '[SECURITY - High]
    Account takeover via 2FA brute force (CVE-2026-49470)'; IT-Connect: 'account takeover via
    brute-force attack on the second factor (2FA)', explicitly 'context suggests post-auth').
    Brute-forcing the second factor requires the victim's first-factor credentials, so the
    mechanism is post-auth; no cited source states pre-auth. Also internally inconsistent: the
    sibling and stronger MFA flaw CVE-2026-52848 ('complete MFA bypass') is correctly typed
    auth: post-auth in the same entry. Fix: change CVE-2026-49470 auth to post-auth.
    Lower-confidence secondary (main agent to verify, not counted): 'affected: GLPI < 11.0.8
    and < 10.0.26' may be over-broad — MFA is a GLPI 11.x feature (the entry's own body says
    52848 bypasses 'GLPI 11's MFA', scoped 11.0.x-only) and a web search indicated 49470
    affects GLPI 11.0 only; branch tag could not be confirmed from the changelog fetch.
