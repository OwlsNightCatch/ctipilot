**Model:** Sonnet 5 (`claude-sonnet-5`)
**Timestamps:** started_at=2026-09-20T15:30:01Z · ended_at=2026-09-20T15:36:26Z · duration_seconds=385

## Verification report — 2026-09-20T1308Z-audit (iteration 8)

### Prior-iteration delta check

Iteration 7's F4 (the "8 imprecisions" three-bucket summary summing to seven, and AEPD double-counted) is fixed. The report's itemization now reads: two fixed through a changelog record (NTC `correction`, Gyazo internal `improvement`) + six recorded without one (Pixel adjacency, GemStuffer, LiteLLM, Zimbra, GitLab, Windows) = 8, with the three declined findings (Swiss Bitcoin Pay T1213, DDrop T1553, AEPD verification value) and the three reader-text `improvement` entries (Salt, AEPD, Chosen Brick) explicitly separated out as a different population, and AEPD's double appearance called out by name.

I cross-checked this itemization against the raw truth-pass output on disk (`work/2026-09-20T1308Z-audit/truth-{A,B,C,D}.yaml`), which the delta instructed me to do:

- Raw verdict counts across the four truth passes: truth-A 8 clean + 5 imprecision (13); truth-B 2 factual-error + 8 imprecision (10); truth-C 5 clean + 2 factual-error + 4 imprecision (11); truth-D 1 imprecision (1). Total 35, matching the 35-entry scope.
- The six "recorded, not fixed" defects match the truth-pass text verbatim in substance: GemStuffer's `techniques[]` T1567.004 has no supporting body behavior though the source does describe a webhook technique (truth-C); LiteLLM's EPSS nulls against live 0.836/0.363 FIRST.org scores and the Starlette GHSA authority is only in a non-rendered internal record (truth-C); Zimbra's NCSC-UK-cited sector list is verbatim CISA AA26-204A (truth-C); GitLab's exploitation claim traces to an advisory's internal edit-log "reason" field rather than published content (truth-C, itself marked low-confidence there); Windows' two facts pinned to a co-cited source that doesn't carry them — the affected-version list wrongly cited to BleepingComputer, the researcher-credit clause wrongly cited to ZDI (truth-D); the Pixel `AV:A`/adjacency omission (truth-A), consistent with watch item 2's text.
- Of the raw 18 "imprecision" verdicts: 6 (Check Point, Acronis, FamousSparrow, MovieReaper, Unbound, Waterplum, all in truth-B) carry *only* an em-dash-count defect with every substantive claim independently verified clean — these are exactly the population the report's separate "em-dash question, closed" section covers, not double-counted as imprecisions. 1 (GTG-27005/Anthropic account count, truth-A, itself marked low-confidence and "ambiguous") is elevated by the report into the factual-errors table as the F14 quantifier-class error, which is a defensible read of an ambiguous-becomes-definite claim. 3 (Swiss Bitcoin Pay, DDRop, AEPD) are the declined findings. That leaves 18 − 6 − 1 − 3 = 8, which is exactly the itemized count, and 13 raw-clean + 6 em-dash-only + 3 declined = 22, matching the report's headline "22 of 35 verified clean." The numbers reconcile once this (undocumented-in-one-place-but-consistent) methodology is traced through both files; I found no arithmetic error in it.

### Independent verification this iteration (fresh, beyond the delta)

I re-fetched the Oracle CSPU page and NCSC-NL's advisory live (not just relying on the saved iteration-2/3/4 fix history) against the current state of `entries/2026-09-20/oracle-september-2026-cspu-five-unauthenticated-cvss-10.md`:
- Oracle's page (`src_oracle_cspu.txt`, re-checked): "This Critical Security Patch Update contains 673 new security patches" / "153 new security patches for Oracle Fusion Middleware... 78 of these vulnerabilities may be remotely exploitable without authentication" — matches the entry's summary and body verbatim. The risk-matrix rows for all six CVEs (CVE-2026-83021, -71133, -83099, -83059, -83020, -87230) match the entry's `cves[]` affected-version strings and CVSS exactly, including Hyperion's row showing Availability = None where the other five show High — matching the body's "the first five also reach high availability impact; Hyperion Financial Management's is rated none."
- NCSC-NL's advisory page (fetched live via `jina`, since `extract`/`url` both return only a client-side redirect stub): "Prioriteit Hoog" is the only rating field on the page, and the body text names exactly five CVSS-10.0 CVEs (71133/83099/83059/83020/83021, not 87230/Hyperion). The entry's sourcing_note ("assigned it a high priority, which is a second assessment of severity") and body ("relayed the Fusion Middleware half of the release... and assigned it priority 'Hoog'") both correctly scope the NCSC-NL claim to the Fusion Middleware five, not the sixth (Hyperion) CVE, and correctly describe a single priority field rather than the dual likelihood/damage marker that iteration 2 found was not on this page (it is on the RSS feed instead, `[H/H]`, which the entry does not cite). No residual defect from the propagation-failure chain iterations 2–5 fixed.

I re-verified all six changelog-record diffs (`git diff HEAD --`) for field-declaration discipline and factual accuracy against the saved/re-fetched sources:
- **Cisco FMC** (`correction`, fields `[headline, cves, actions, body]`): diff touches exactly those surfaces; `src_cisco_fmc.txt` confirms verbatim "Replaced hot fixes with the security hardening releases" (rev 2.6, 2026-SEP-16) and the exact fixed-release table (7.0→7.0.10 … 10.1→10.1.0) plus "Cisco recommends that customers upgrade devices to the appropriate hardening release."
- **Linux KEV** (`correction`, fields `[verification, sourcing_note, sources, evidence, body]`): diff replaces the three blocked NVD REST endpoints with Red Hat's per-CVE pages in both `sources[]` and inline citations, as declared. Red Hat's live page for CVE-2025-39682 (fetched via `jina` this iteration) shows a "Known exploit" badge, and the saved `src_thn.txt` carries the exact quote the correction attributes to The Hacker News: "'This CVE is high risk and there are known public exploits leveraging this vulnerability,' Red Hat said. 'Address this vulnerability with high priority.'" `src_rh_53266.txt` independently confirms "An Important flaw... allows a local attacker to achieve privilege escalation, memory corruption, or denial of service," matching the rewritten CVE-2026-53266 sentence.
- **Brevo** (`correction`, fields `[title, summary, body]`): diff changes "up to 100,000" to "more than 100,000" in exactly those three surfaces; `src_sansec.txt` confirms verbatim "Brevo served malware to visitors of its own site and more than 100 thousand customer sites."
- **NTC, Gyazo, Salt, Chosen Brick, AEPD** (corrections/internal improvements): all five diffs touch only the fields their `updates[]` record declares (`[classification, sourcing_note]`, `[sourcing_note]`, `[body]`, `[sourcing_note]`, `[body, sourcing_note]` respectively), no silent edits found, `internal: true` records carry no `## <Type>` section as required, the non-internal NTC `correction` does carry its section, and none of the "(PD-5)"/policy-code removals introduce a duplicated clause (the Gyazo duplication iteration 2 caught is gone).

I independently recounted the em-dash figure directly against the entry store: `grep`-equivalent count over `entries/*/*.md` gives 917 total files, 906 with at least one em dash, 8,416 total occurrences — exact match to the report's "8,416... across 906 of 917."

I attempted to independently reproduce the "118 of 748 across 60" verifier-counter-mismatch figure with a standalone script applying the shipped check's own comparison rule (`truth+editorial+advisory == len(findings)`) over every `runs/*/*.md` file. My script (a plain regex-plus-`yaml.safe_load` pass, skipping files that fail to parse) returned 140 mismatches across 779 iterations in 68 records — matching the report's own description of "the looser first pass over every file under runs/" almost exactly (140, 68), not the claimed authoritative 118/748/60. One run record's frontmatter failed strict YAML parsing under my script (an unescaped backslash in a Windows path inside a double-quoted string), which the actual `check_run.py` frontmatter loader evidently handles or excludes differently, so this is not solid counter-evidence — my method is demonstrably the cruder one the report already distinguishes from its authoritative count, and iteration 7 (immediately prior) already recorded independently reproducing 118/748/60 specifically. I flag this only as a residual, low-confidence uncertainty rather than a finding: I could not myself reproduce 118/748/60 with a naive script, but I also have no basis to say it is wrong, since my script's own limitations plausibly explain the gap and the figure has already survived three rounds of independent verifier recomputation.

### Verdict

**CLEAN.**

What I checked hardest to get here: (1) the prior-iteration delta's itemization fix, traced line-by-line against the raw truth-pass YAMLs rather than taken on faith — it reconciles; (2) the audit's own recovered entry (Oracle CSPU) against freshly re-fetched primary sources (Oracle's own risk matrix, NCSC-NL's advisory via `jina` since the direct page is a JS redirect), which had been through five rounds of prior fixes and is now internally and externally consistent; (3) every one of the ten changelog diffs for silent-edit compliance and source fidelity, with three (Cisco FMC, Linux KEV, Brevo) re-verified against saved or freshly-fetched source text word-for-word. No new truth or editorial defect found. The one residual uncertainty (the verifier-counter-mismatch corpus figure) is disclosed above rather than either asserted as a finding or silently dropped, per the coverage/evidence balance this role is held to; it does not change the verdict given my own measurement method is the one the report already identifies as the less precise one, and two prior iterations already independently reproduced the report's figure.

### Findings summary (machine-readable)

```yaml
[]
```
