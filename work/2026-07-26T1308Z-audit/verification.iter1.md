**Model:** Opus 5 (`claude-opus-5`)
**Timestamps:** started_at=2026-07-26T14:25:12Z · ended_at=2026-07-26T14:42:06Z · duration_seconds=1014
**Self-telemetry:** urls_checked=22 · webfetch_calls=5 · bridge_fetches=24 · websearch_calls=0

## Verification report — 2026-07-26T1308Z-audit (iteration 1)

Cold read, no prior-iteration deltas. Scope: the 9 new entry files, the run record, and `docs/audits/2026-07-26-weekly-quality-audit.md`. Every inline source URL on all nine entries was fetched in this iteration (Rapid7 x2, SecurityWeek, IBM PSIRT, ZDI-26-036, go4it.ro x2, psnews.ro, 20min.ch, ICTjournal, Huntress, Help Net Security, BleepingComputer, The Hacker News, mysites.guru x4, NCSC-2026-0252 + its CSAF, Oracle cpujul2026, CERT-FR AVI-0920, CSOonline, Zscaler) — no 404, no DNS failure, no homepage/index/listing citation, no NVD/MITRE-only primary. Every `evidence[]` quote was substring-checked against raw page text (bridge/jina, HTML stripped, curly quotes normalised) rather than against a summariser's paraphrase. KEV ground truth taken from `tools/fetch_source.py cisa-kev` directly. The audit report's checkable numbers were recomputed from the truth-pass YAMLs, the entry store, `sources/sources.json`, `state/cves_seen.json`, `git diff` and `tools/check_run.py --all`.

**What verified clean and is worth recording:** the Langflow correction — the highest-risk item in the run — is sound on both legs, independently confirmed against the IBM bulletin ("Langflow OSS: 1.0.0-1.10.1", "CVSS Base score: 8.8", "IBM strongly recommends addressing the vulnerability now by upgrading Langflow OSS to version 1.10.2") and ZDI-26-036 ("Authentication is not required to exploit this vulnerability.", `exec_globals` to the validate endpoint, CVSS 9.8, no configuration precondition anywhere on the page, dated 2026-01-09). The WP2Shell exploitation flip is confirmed: KEV `dateAdded` 2026-07-21 for both CVE-2026-63030 and CVE-2026-60137; the Rapid7 quote is a contiguous verbatim prefix of "Given confirmed exploitation in the wild, Rapid7 strongly recommends investigating for signs of compromise even after patching." and the page's own "Updates" block records "July 22, 2026: Updated post to reflect public reporting of exploitation in the wild"; the SecurityWeek Hexastrike quote is verbatim and watchTowr is corroborated ("WatchTowr has also seen in-the-wild exploitation attempts."). Both Romanian PS News quotes are verbatim. All four update_of targets exist, are the same story, and carry only deltas. The joomla and teleshim entries carry `verification: single-source` with sourcing notes that name the situation (F12 satisfied). Both single-source entries' CVE ids, versions and the 9.1 dispute quote check out against mysites.guru. Zero IOCs, zero vanity metrics, zero workflow-internal jargon in any entry body, English throughout. `actions[]` discipline is good (four entries with one concrete action, five empty) — no F18. Audit-report numbers that recomputed exactly: 57 entries / 32 clean / 22 imprecisions / 3 factual errors, per-batch 8-of-15, 9-of-15, 5-of-13, 10-of-14, ~181 URLs (49+47+39+46), 12 run records all `publish_status: ok`, actions 38/15/4, priority 17 high / 40 notable / 0 critical / 0 routine, ratings A1 14 · A2 11 · B1 2 · B2 28 · B3 1 · C2 1, 11 update_of deltas, ATT&CK pin 19.1, 11 new `cves_seen` records, all 11 promotion notes matching their claimed contributing-run counts with `sources.promotion_due` now empty, `zscaler-threatlabz` added as candidate, both verifier bodies byte-identical (49,264 chars each) with the adjacency check present in both, v3.29 banners on all three master prompts, the CHANGELOG 3.29 entry, `promotion_due`/`--promote-after` in `tools/run_summary.py`, the three sources.json recipe corrections, the new acknowledgment record, and `.claude/memory/classifier-trips-on-spawns.md` plus its MEMORY.md index line. `check_run.py --all` currently reports 0 warn · 9 acknowledged with a single FAIL that is this run's own not-yet-populated `verification.iterations` — the report's "0 warn · 0 fail · 9 acknowledged" holds once the loop records itself.

### Citation does not support the claim

**F1 — `oracle-july-2026-cpu-fusion-middleware-cvss10-unauth`: version list and component names attributed to NCSC-NL, which carries neither.**
Claim: "NCSC-NL named two in particular: CVE-2026-47056 in the Oracle Data Integrator REST Service, and CVE-2026-60217 in Oracle Coherence Core affecting 12.2.1.4.0, 14.1.1.0.0, 14.1.2.0.0 and 15.1.1.0.0 ([NCSC-NL, 2026-07-22](https://advisories.ncsc.nl/advisory?id=NCSC-2026-0252))".
Ground truth: the advisory page 403s the routine UA and returns a redirect shell to both `WebFetch` and the `url` bridge, so I escalated to the jina reader (7,355 chars) and additionally pulled its CSAF (`https://advisories.ncsc.nl/csaf/v2/2026/ncsc-2026-0252-0.json`, 28,074 bytes). Neither carries any of `12.2.1.4.0`, `14.1.1.0.0`, `14.1.2.0.0`, `15.1.1.0.0` (every `product_version_range` is `vers:unknown/*`), nor the strings "Coherence Core", "REST Service" or "raw TCP". NCSC's own per-CVE text is "A critical unauthenticated remote code execution vulnerability in **multiple versions** of Oracle Coherence" and, for 47056, "Oracle Data Integrator versions 12.2.1.4.0 and 14.1.2.0.0 ... via HTTP". Every one of those facts is on the **co-cited** Oracle matrix, verbatim: "CVE-2026-60217 Oracle Coherence Core TCP Yes 10.0 Network Low None None Changed High High High 12.2.1.4.0, 14.1.1.0.0, 14.1.2.0.0, 15.1.1.0.0" and "CVE-2026-47056 Oracle Data Integrator Rest Service HTTP Yes 10.0 ... 12.2.1.4.0, 14.1.2.0.0". "NCSC-NL named two in particular" also understates — the advisory names nine CVEs, all at 10.0. Fix: cite `https://www.oracle.com/security-alerts/cpujul2026.html` (already in `sources[]`, never cited inline) for the versions, components and protocols. The same paragraph's uncited "CVE-2026-61211 (CVSS 9.9) abuses DBMS_CLOUD" sentence is Oracle-matrix material too ("CVE-2026-61211 RDBMS Execute DBMS_CLOUD Oracle Net No 9.9 ... 19.3-19.31, 23.4.0-23.26.2") and should take the same citation.

**F2 — `fakeagent-claude-artifact-lure-sectoprat-dll-sideloading`: autofill / payment-data / blockchain-C2 details cited to BleepingComputer, which carries none of them.**
Claim: "targets browser credentials, cookies, autofill and payment data, with command-and-control resolved through blockchain-hosted records for takedown resilience ([BleepingComputer, 2026-07-25](https://www.bleepingcomputer.com/news/security/fake-claude-app-promoted-by-bing-ads-pushes-sectoprat-malware/))".
Ground truth: fetched the BleepingComputer article in full via the jina reader (15,620 chars) — `blockchain` 0 occurrences, `autofill` 0, `payment` 0; it does carry "hands-on", "credential" and "cookie". Both missing details belong to the co-cited Huntress primary: "command-and-control (C2) data stored in the Ethereum blockchain", "This act of storing command-and-control information in the Ethereum blockchain is a known tactic called \"EtherHiding\"", and "Plaintext strings include references to browser logins, cookies, autofills, credit cards, chromium key theft, FTP, Discord, messaging clients, etc." Split the citation.

**F3 — `rapid7-exposed-webdav-delivery-lab-cve-2025-33053-clickfix`: lure-theme detail cited to The Hacker News, which contains neither word.**
Claim: "generated README documentation, and bulk-produced lure variants on invoice and salary-statement themes rather than hand-crafted one-offs ([The Hacker News, 2026-07-20](https://thehackernews.com/2026/07/exposed-server-reveals-ai-assisted.html))".
Ground truth: the THN article (fetched, 11,426 chars) contains `invoice` 0 times and `salary` 0 times. It does legitimately carry the quoted phrase that opens the sentence — "Rapid7's summary is blunt: \"the attacker used LLMs to operate more like a modern software product team.\"" — so the quote's attribution is fine, but the trailing citation also claims the invoice/salary themes, which are in Rapid7's own post (`invoice` x2, `salary` x2). Re-attribute the second clause.

**F4 — `ifage-geneva-dragonforce-data-published-student-records`: "after the institute declined to pay" is contradicted by the cited source.**
Claim (summary and body): "DragonForce published the data ... on 2026-07-24 after the institute declined to pay".
Ground truth: 20 minutes (fetched in full via jina, 38,950 chars) states "La fondation avait aussi affirmé qu'elle **n'avait pas reçu de demande de rançon**, mais que, le cas échéant, elle refuserait de payer." — IFAGE said it had received no ransom demand at all, and that it *would* refuse one if it came. The co-cited ICTjournal piece (fetched) contains neither "payer" nor "refus". No cited source supports non-payment as the antecedent of the publication; the causal framing has to go or be re-grounded.

**F5 — same entry: the publication date is a day later than the cited source states.**
Claim: "published the data on 2026-07-24" (headline, summary, body, `event_date: "2026-07-24"`).
Ground truth: the 20 minutes article is itself stamped "Publié 24. juillet 2026, 14:22" and says of the leak "C'est chose faite **depuis jeudi**, selon «Le Temps»" — Thursday, i.e. **2026-07-23** (2026-07-24 is a Friday; `date -d` confirms). Correct the event date, or reframe as "reported on 2026-07-24". (The related claims all check out: the exposure attributed to "un expert sollicité par le quotidien" does refer back to «Le Temps»; "cette cyberattaque fait l'objet d'une plainte pénale. L'enquête est menée par la police cantonale, Fedpol ... et les autorités judiciaires" supports the complaint/authorities sentence; "L'Ifage avait pourtant assuré que seules des données d'anciens et d'actuels collaborateurs avaient été volées" supports the victim-scoping contradiction.)

### Unsupported / hallucinated facts

**F6 — `oracle-july-2026-cpu-fusion-middleware-cvss10-unauth`: a quotation that appears in no cited source.**
Quoted as source text: "Of its 355 new vulnerabilities, 219 are remotely exploitable without authentication, and ten landed at a perfect 10.0".
Ground truth: fetched the CSOonline article in full — that sentence does not exist. Its actual text is "Fusion Middleware was particularly hard hit, with new security patches for 355 security vulnerabilities, 219 of them remotely exploitable without authentication, meaning they can be exploited over a network without requiring user credentials." followed by "Ten of them scored a “perfect” 10.0 on the Common Vulnerability Scoring System (CVSS)." (that second sentence *is* the entry's verbatim `evidence[]` quote, modulo nested-quote marks — fine). Oracle's own page independently confirms the 355/219 figures. So the numbers are true and the quotation is manufactured; drop the quote marks or substitute the source's sentence. This is the same defect shape the audit itself documented in W29 ("a paraphrase presented inside quotation marks as a Kaspersky quote").

**F7 — `teleshim-bindcloak-volume-serial-keying-government-espionage`: `evidence[]` quote spliced from two different sentences.**
Quote: "TELESHIM abused the Telegram API for C2 communication, a technique used to blend in with legitimate internet traffic."
Ground truth: the Zscaler post contains two distinct sentences — the overview's "TELESHIM abused the Telegram API for C2 communication to blend in with legitimate internet traffic." and the C2 section's "TELESHIM abuses the Telegram API for C2 communication, a technique used to blend in with legitimate internet traffic." The entry's version takes the past-tense verb from the first and the ", a technique used to" clause from the second — a hybrid, reproduced in both frontmatter and body. Use either sentence verbatim.

**F8 — `ifage-…`: a word substituted inside a verbatim quote.**
Quote: "La divulgation par les cybercriminels concerne tant des employés de l'institut que des bénéficiaires (étudiants, entreprises, etc.)."
Ground truth: the source reads "**Leur** divulgation par les cybercriminels concerne tant des employés de l'institut que des bénéficiaires (étudiants, entreprises, etc.)." Meaning survives; the quote is not copyable unchanged. Restore "Leur" or move the gloss outside quotation marks.

**F9 — audit report § Findings: "all four truth passes returned `machine_surface: false` on every entry".**
Ground truth: `work/2026-07-26T1308Z-audit/truth-B4.yaml` carries `machine_surface: true` on `entries/2026-07-19/weekly-w29-exploited-internet-facing-enterprise-software.md` (24 records false, 1 true), and the report's own next sentence concedes it ("One verifier initially flagged a `machine_surface: true`"). Self-contradictory as written; rephrase to "on every entry after review of the single flag".

**F10 — audit report § Verdict: the attribution class is credited with all 22 imprecisions.**
Claim: "22 of the 25 non-clean entries are precision defects of one specific kind — a true fact cited to a co-cited source that does not carry it".
Ground truth: the report's own § Imprecisions splits the 22 into "Per-fact attribution (12)", "Boundary and scope precision (6)" and "Novelty and quote fidelity (4)"; the run record says "The other 22 findings are precision defects, and **12 of them** are one class"; the CHANGELOG 3.29 entry says "12 of the audit's 19 truth findings". The Verdict should say 12 of 22 — the largest single shape, not the whole set.

**F11 — audit report § Imprecisions: an enumerated finding that no artifact supports, plus a count that cannot sum.**
Claim: "a sourcing note claiming both CVE ids \"resolve on cve.org\" where one does not".
Ground truth: `grep -rn "cve\.org" work/2026-07-26T1308Z-audit/*.yaml` returns zero matches, and none of the 22 imprecision `defect` strings describes anything of the kind. Same bullet, second limb: it is labelled "(4)" but enumerates **six** defects (`ta458-roundpress`, `gpt56-autonomous`, `weekly-w29-state-nexus`, `moodle-local-o365`, `stiftung-autismuslink`, plus the cve.org item), so 12+6+4 cannot partition the 22 imprecision entries as enumerated (a defensible per-entry partition of the 22 defect strings is closer to 10/7/5). Re-derive the three counts from the YAMLs and drop or substantiate the cve.org item. This is precisely the class the 2026-07-11 audit's own iteration 1 caught — a claimed artifact record that is not there.

### Quantifier without source

**F12 — `oracle-july-2026-cpu-fusion-middleware-cvss10-unauth`: "ten" CVSS-10.0 flaws, where the entry's own primary source says nine.**
Claim: title "ten unauthenticated CVSS 10.0 flaws in Fusion Middleware"; summary "ten at CVSS 10.0, each reachable over a standard network protocol with no credentials"; body "Each of the ten is reachable over a standard network protocol".
Ground truth: NCSC-2026-0252 states "De ernstigste kwetsbaarheden, **9 stuks**, hebben de hoogste score van 10.0 gekregen", and its CSAF enumerates exactly nine CVEs (47056, 60217, 60358, 60360, 60365, 60366, 60379, 60389, 60644). Parsing Oracle's Fusion Middleware risk matrix (343 rows) yields **ten rows** at base score 10.0 but only **nine distinct CVE ids**, because CVE-2026-60365 is listed twice — once under "Oracle HTTP Server", once under "Oracle Weblogic Server Proxy Plug-in". That is exactly why Oracle/CSOonline count ten and the Dutch authority counts nine. The entry presents "ten" as a count of flaws in three places and never surfaces the divergence; note also that NCSC gives the total as 345 where Oracle gives 355. Either correct to "nine distinct CVEs (ten matrix rows)" or add a `Contradiction:` line naming both figures — the entry's own family enumeration (Data Integrator, Coherence, Access Manager, HTTP Server, Platform Security for Java, WebCenter Content, Service Delivery Platform, Unified Directory, WebLogic Proxy Plug-in) is already consistent with nine distinct CVEs.

### Claims missing inline citation

**F13 — `langflow-1-10-2-required-cve-2026-0770-precondition-fix`: two uncited factual claims.**
(a) "but CISA's Known Exploited Vulnerabilities record for CVE-2026-0770 now carries a Langflow release (v1.9.0) as its remediation reference, so a patched release does exist" — repeated in the frontmatter summary. I verified it independently (`tools/fetch_source.py cisa-kev`: CVE-2026-0770, `dateAdded` 2026-07-21, `notes` = "https://github.com/langflow-ai/langflow/releases/tag/v1.9.0 ; …"), so it is true, but neither cited source mentions KEV or v1.9.0. (b) "The AUTO_LOGIN-plus-default-credentials condition belongs to a different, separately-tracked cluster of Langflow code-validation flaws, not to CVE-2026-0770" — no cited source states this; the IBM bulletin's separate default-credential item is adjacent evidence at best. Add a citation or hedge each. Both legs of the correction itself are sound (see the clean summary above) — this is a sourcing gap, not a truth defect.

### Classification missing / inconsistent

**F14 — `wp2shell-cve-2026-63030-60137-confirmed-exploited-kev`: `reliability: A` on a B-rated publisher, inconsistent within this run.**
`classification: {reliability: A, credibility: 1}` with Rapid7's ETR blog as the `role: primary`. `sources/sources.json` rates `rapid7-research` reliability **B**, and this run's other Rapid7-primary entry (`rapid7-exposed-webdav-delivery-lab-cve-2025-33053-clickfix`) rates the same publisher **B**. Align to B. `credibility: 1` is well supported (SecurityWeek corroborates; the KEV catalog independently confirms both `dateAdded` values) and should stay.

### Editorial / less-is-more flags (advisory)

**F15 — four `evidence[]` quotes deviate from their source only in the initial letter's case.** `ancpi-…`: "Infrastructura ANCPI nu beneficia de un antivirus instalat pe serverele care rulau aplicațiile principale." (source: "Potrivit raportului, **i**nfrastructura ANCPI nu beneficia…") and "Nu există indicii că baza de date principală Oracle Exadata ar fi fost compromisă." (source: "… **n**u există indicii că … compromisă**, datele fiind salvate prin replicare periodică**"); `fakeagent-…`: "the malware's decryption routine isn't a typical CPU-based decryption routine: instead, it is a compiled DirectX shader" (source sentence-initial "The"); `rapid7-…`: "The attacker used LLMs to operate more like a modern software product team." (source: "It's that **t**he attacker used LLMs…"). All four are otherwise contiguous and meaning-preserving, and I confirmed each against raw page text — no fact is affected. Flagged only because the standard is "copyable from the page unchanged"; two also add a terminal period where the source continues. Main agent's discretion.

**F16 — `teleshim-…` `sourcing_note` trailing clause is run-process provenance.** "Published by the 2026-07-26 weekly quality audit, which surfaced it as a coverage gap; Zscaler ThreatLabz was added to the source list in the same run, having had no curated discovery path." The first half of the note (sole source, Part 1 of a series) is what a Tier 2 reader needs; the trailing clause is pipeline bookkeeping. Consider trimming.

### Missed angles

None. The three coverage re-sweeps enumerated ~61 in-window research posts across ~30 publishers plus a 58-record vulnerability slice and answered both carried-forward watch items; the nine recoveries plus seven documented borderline drops account for the window, the two carried watch items (French leak-site claims, GTIG cryptonyms) are correctly held rather than published, and the run record's `fetch_failures: []` with three named unreadable publishers is honestly disclosed and cost no recovery I can identify. I could not name a plausible in-window source for any relevant story the run missed, so I am recording "coverage looks complete" rather than manufacturing an angle. The B2 rotation exception is disclosed identically in the run record and the report, with the discarded partial YAML noted — correct handling.

### Verdict

**NEEDS_FIXES (truth: 12, editorial: 2, advisory: 2)**

Truth = F1–F5 (claim-not-supported), F6–F11 (unsupported/hallucinated), F12 (quantifier). Editorial = F13 (missing citation), F14 (classification). Advisory = F15, F16.

The run is substantively strong: the Langflow correction — the item with the most downside risk — is right on both legs and re-verified here against the per-CVE authorities, the WP2Shell flip is confirmed against the KEV catalog itself, and every audit-report statistic I could recompute matched exactly except the three flagged above. The defect concentration is instructive: five of the twelve truth findings (F1–F3 plus F6 and F12) are on the Oracle entry, and F1/F2/F3 are the very per-citation-adjacency class this audit declared the pipeline's dominant residual defect and shipped a v3.29 fix for — the fix does not yet hold in the run that shipped it. The audit report's three findings (F9–F11) are the same class turned inward: claims about its own artifacts that the artifacts do not carry.

### Findings summary (machine-readable)

See `work/2026-07-26T1308Z-audit/verification.iter1.findings.yaml` (16 records; codes F3 x5, F4 x6, F14, F5, F17, F11 x2 — note the YAML uses canonical F-codes, which differ from the sequential F1–F16 numbering in the prose above).
