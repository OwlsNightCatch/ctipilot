**Model:** Sonnet 5 (`claude-sonnet-5`)
**Timestamps:** started_at=2026-08-30T15:48:18Z · ended_at=2026-08-30T15:57:48Z · duration_seconds=570

## Verification report — 2026-08-30T1312Z-audit (iteration 8, confirming pass)

Read cold, independent of iteration 7's verdict. Covered: all 13 changelog-carrying entries, both new 2026-08-30 entries, the run record's `verification.iterations[]` (all 7), `docs/audits/2026-08-30-quality-audit.md`, and the supporting artefacts under `work/2026-08-30T1312Z-audit/` (truth-B1/B2/B3.yaml, `state/coverage_backlog.md`, `state/cves_seen.json`, `sources/sources.json`, prompt banners + CHANGELOG, `tools/kev_window_diff.py`, the two new `check_run.py` checks, a live `check_run.py --all` and `site/build.py` run).

**Iteration 7's two advisory fixes, re-verified:**
1. Gamaredon `techniques[]` T1102→T1102.001: confirmed T1102.001 ("Dead Drop Resolver") is active, non-deprecated, non-revoked in the pinned v19.2 dataset, and Sekoia's own report uses the phrase "Dead Drop Resolver technique" verbatim three times for exactly this mechanism (`~.gif` module resolving C2 via Telegram/Telegra.ph/Teletype/Supabase/Workers). Correct.
2. Protection Civile FrenchBreaches `original:` typographic apostrophes: fetched the live page (`frenchbreaches.com/alertes/protection-civile-mt27j64epv2smy5m0g`) and confirmed the `original:` string (with curly `’`) is a byte-exact contiguous substring of the current page text. Correct.

**Reconciliation and cross-checks that held:** the 44/19/11/14 count in both the run record and the audit report matches a fresh tally of `truth-B1.yaml` (15 entries: 5 clean/6 imprecision/4 factual-error), `truth-B2.yaml` (15 entries: 5/4/6, counting the worst verdict per entry across its 19 findings records), `truth-B3.yaml` (14 entries: 9/4/1) exactly. `state/coverage_backlog.md` has 11 `## Open` rows and exactly 2 rows this run added to `## Struck` (CVE-2026-21962, CVE-2026-60004). `state/cves_seen.json` carries both new CVEs with correct titles/URLs. `sources/sources.json` carries `symantec-security-com` as described. Both prompt banners read v4.8 and `prompts/CHANGELOG.md` head is the matching 4.8 entry. `tools/kev_window_diff.py` runs and its recorded output (`kev-window.txt`) matches its live-rerun output (11 additions, 6 not covered, the same two genuine gaps plus the four legacy UAT-10147 ids). `check_run.py`'s new `frontmatter-yaml` (wired at both `--all` and run scope) and `reader-text-internals` (wired run-scope only, confirmed by call-site inspection) checks exist exactly as described. `python3 site/build.py` emits no warnings. `python3 tools/check_run.py --all` currently shows **25 pass · 1 warn · 0 fail · 25 acknowledged** — the one live warning is `verification-confirmation: … final verdict CLEAN is unconfirmed — iteration 7 is the only CLEAN in the chain`, which is the expected transient state pending this iteration's own verdict, not a discrepancy in the audit's claim of an eventual 0-warn end state.

Fetched and cross-checked against current page text: NetSPI (Oracle entry), SecurityWeek (Oracle entry), SOCRadar SNOWLIGHT report (victimology table: 119/91/1 WebLogic, 80/80 Confluence, 1866/1563/16 cPanel — all match), DOJ press release (both QTFY quotes, including the corrected "targets" wording and the 2018-dating sentence), The Register via jina (ICO / MAG quotes on the Manchester Airports entry), CERT/CC VU#308749 via jina (both quotes in the Kaltura update section, including the exact `Date Last Updated: 2026-08-28 19:59 UTC` timestamp), NVD CVE 2.0 API for CVE-2026-60004 (CVSS 9.8 vector and affected range), FIRST EPSS API for CVE-2023-49105 (0.43205, confirming the owncloud entry's correction), Gitea's own GHSA advisory text (both quoted passages, plus the "no traces of persistence via cron, systemd, or new SSH keys" sentence that justifies dropping T1543/T1505), and Sekoia's Gamaredon report (both replaced `evidence[]` quotes, verbatim). All matched. ATT&CK ids sampled across both new entries and the Gamaredon/Gitea updates (T1190, T1105, T1496, T1552.001, T1203, T1547.001, T1053.005, T1564.004, T1102.001, T1091, T1080, T1027) are all active/non-deprecated/non-revoked in the pinned v19.2 dataset. Entity registrations for `product:oracle-http-server`, `product:oracle-weblogic-server-proxy-plug-in` and `product:gitea` all resolve. `git diff HEAD` for every one of the 13 updated entries shows a changed-line set that matches its changelog record's `fields`, a body section present for every non-internal record and absent for the one `internal: true` record I read in full (Protection Civile's 08-28 editorial-pass record), and no field changed outside a declared record.

### Unsupported / hallucinated facts

**#1 (low severity, evidence-backed).** `entries/2026-08-28/protection-civile-france-eprotec-breach-volunteers.md` — the Franceinfo `evidence[]` record's `original:` field reads `"a annoncé vendredi 21 août avoir été victime..."` with a plain space (U+0020) between `21` and `août`. Both `tools/fetch_source.py extract` and `tools/fetch_source.py url` on the cited Franceinfo URL (`https://www.franceinfo.fr/internet/securite-sur-internet/cyberattaques/la-protection-civile-annonce-avoir-ete-visee-par-une-cyberattaque-en-mars_8156621.html`) show a non-breaking space (U+00A0) at that exact position on the live page (verified byte-by-byte: `'2' '1' '\xa0' 'a' 'o' 'û' 't'`). This `original:` field is therefore not a byte-exact contiguous substring of the cited page as it reads now — it fails the same class of check iteration 7 just applied and fixed for the FrenchBreaches record on the same entry, just one character. No meaning is affected (the English `quote:`/translation is untouched and correct), and this was not part of this run's diff — it predates the 2026-08-28 composition and none of the seven prior iterations caught it because none grepped this specific `original:` byte-for-byte against a freshly fetched page. Fix: replace the plain space with U+00A0 in the `original:` string (single-character edit), or leave as a documented residual given the triviality — flagging per the coverage obligation, not because it changes anything a reader would notice.

### Verdict

`NEEDS_FIXES (truth: 1, editorial: 0, advisory: 0)`

Everything else read cold — both new entries in full, all 13 changelog sections and their frontmatter deltas, the run record's 7 recorded verification iterations (each `remediation_applied` checked against a live re-fetch or the current file state, not just against the record's own prose), and every checkable claim in the audit report — held up. This is iteration 8, the cap: the finding above is real but trivial (a single whitespace character in a non-English `original:` field with zero effect on any reader-facing claim), and it is the only defect found after fetching roughly 15 primary/secondary sources fresh this pass. Given the cap, this belongs in the run record as a logged residual rather than blocking publication.

### Findings summary (machine-readable)

```yaml
- code: F4
  category: hallucinated-fact
  section: entries/2026-08-28
  item: "La Protection Civile (France): eProtec volunteer-management platform breach — Franceinfo evidence record"
  url_or_quote: "original: \"a annoncé vendredi 21 août avoir été victime d'un piratage informatique...\""
  summary: "Live Franceinfo page has a non-breaking space (U+00A0) between '21' and 'août' at this position; the entry's original: field has a plain space (U+0020), so it is not a byte-exact contiguous substring of the cited page. Zero semantic impact; the English translation is unaffected. Pre-existing, not introduced by this run's diff."
```
