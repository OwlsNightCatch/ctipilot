**Model:** Sonnet 5 (`claude-sonnet-5`)
**Timestamps:** started_at=2026-09-02T07:01:51Z · ended_at=2026-09-02T07:11:53Z · duration_seconds=602

## Verification report — 2026-09-02T0411Z-intel (iteration 8)

Prior-iteration deltas walked first: iteration 7's single finding (leftover "even where it adds extra target-country names" clause in the Mirage Kitten `sourcing_note`) was checked by fetching The Record's article fresh this iteration — it names only Egypt, Ethiopia and Afghanistan, matching Kaspersky, adding none. The clause is gone from the current `sourcing_note`. Remediation confirmed correct.

Full cold pass performed: all three new entries end to end; all four updated entries end to end (body, current frontmatter, every changelog section) plus `git diff HEAD` on each; the run record frontmatter and notes; `entities/registry.yaml`'s diff; `state/coverage_backlog.md`'s diff. Every inline source URL across the new entries and the four update sections was fetched fresh this iteration (Kaspersky Securelist in full, The Record, Republik, heise, Inside IT, 9to5Mac, Reuters/FMT, NCSC-CH posts 12901/12902 via the `ncsc-csh` recipe, The Hacker News, SecurityWeek, two WatchGuard PSIRT pages, JFrog's advisory page, Inside Paradeplatz, Exxpress, NZZ 2026-08-07, ZATAZ's Cybernox article). Quotes, quantifiers, dates, version ranges and technique placements were checked against the fetched text; no broken URL, no generic/listing URL, and no hallucinated entity, CVE, or attribution was found. The dedup/coverage-backlog claims in the run record's notes (7-day deep-dive-rotation window, backlog rows struck/advanced, Zurich/OpenShift dated notes) were checked against `state/coverage_backlog.md`'s actual diff and against `prior_coverage.json`'s deep-dive-category records — all matched exactly.

### Unsupported / hallucinated facts

None at truth severity beyond the two low-confidence items below.

### Citation does not support the claim

#1. `2026-09-02/mirage-kitten-noderabbit-pollcat-nodejs-rats` — (low confidence) "delegating NTLM/Negotiate challenges to `curl.exe --proxy-anyauth` when an unauthenticated connection fails." Securelist's own text describes three sequential steps: "It first tries to establish an unauthenticated connection. If it fails, it retries using URL-embedded basic credentials. Finally, it delegates Windows NTLM/Negotiate challenges to `curl.exe --proxy-anyauth --proxy-user`." The entry's clause omits the intermediate basic-credentials retry and states NTLM delegation is triggered directly by the unauthenticated attempt's failure — a causal-chain mismatch against the cited page. The same simplified phrasing ("corporate-proxy traversal") appears in `entities/registry.yaml`'s `tool:noderabbit` summary.

#2. `2026-09-02/mirage-kitten-noderabbit-pollcat-nodejs-rats` — (low confidence) "it adds Outlook OST/PST email harvesting (T1114.001)." Securelist's command table: "`outlook:emails` Harvest account addresses from Outlook OST and PST artifacts." The source scopes the command to account/address harvesting, not general email-content harvesting; "email harvesting" reads broader than what the cited page states. The same wording ("Outlook email harvesting") appears in `entities/registry.yaml`'s `tool:noderabbit` summary.

### Editorial / less-is-more flags (advisory)

#1. `runs/2026-09-02/2026-09-02T0411Z-intel.md` § Verification & coverage notes — "the CVE-2026-16242 OpenShift row was checked against this run's S1 KEV/CERT sweep." A residual sub-agent shorthand label ("S1") survives in the reader-facing coverage-backlog-work bullet, one instance past the three iteration 6 already removed under the same internal-language-ban rationale (iteration 6 finding/remediation: "Removed the (S1)/(S2)/(S4) shorthand labels from the three affected coverage-notes lines"). This fourth instance in the same section was not swept. Cheap fix: replace "this run's S1 KEV/CERT sweep" with "this run's KEV/CERT sweep."

### What checked out clean (no finding)

- Mirage Kitten entry: every technical claim (NodeRabbit v1/v2/v3 mechanics, persistence tables, C2 endpoints, beacon timing, command counts, PollCat's OTP-independent C2 registration, the Retrograde/MiniFast attribution basis, evidence[] quotes) verified verbatim or near-verbatim against Securelist's full article text, fetched and read start to finish. The Record corroborates only Egypt/Ethiopia/Afghanistan and the delivery-chain description — matches the entry and its `sourcing_note`. Technique placements (T1497 on the sandbox-evasion clause, T1053.003/T1053.005 on their respective OS-specific persistence clauses, T1547.001 on the Run-key clauses) all match narrated behavior.
- Swiss E-ID entry: Republik, heise and Inside IT all fetched fresh. The hedged frontmatter/body framing on which outlet ties the CLOUD Act to the confirmed veto reasoning holds up on a fresh read — Inside IT's own paraphrase folds "der dem US Cloud Act untersteht" into the same sentence as the insider-confirmed digital-sovereignty veto reason, while Republik's and heise's own CLOUD Act mentions sit outside that specific insider-sourced clause, as the entry states. All three evidence[] quotes are verbatim. The Basisregister/Vertrauensregister description, the 90-day migration/unilateral-terms contract details, and the E-ID delay reasons (AHV lookups, deepfake risk, EU incompatibility) all match Republik's article precisely.
- Dropbox entry: 9to5Mac and Reuters/FMT fetched fresh (the ~5,000 figure is present in FMT's raw article body — a dateline artifact caused trafilatura's extraction to drop the opening paragraph, not an entry defect). The notification-email quote, the Lenovo statement quote, the "fewer than a third" figure, and the "John Madden" detail are all verbatim matches. `techniques[]` (T1199, T1078.004) is a defensible mapping for federated-IdP trust abuse; on reflection this is not a stretch worth flagging.
- JFrog update: NCSC-CH advisory 12902 (fetched via `ncsc-csh post 12902`) confirms "Current exploitation status: Actively Exploited" verbatim and the 2026-09-01 creation date; The Hacker News and SecurityWeek both fetched fresh and confirm the phantom-join-key mechanism, the Attacker Eye honeypot attribution, the Ganchev quotes, and the "CISA had not yet added it to KEV" claim, each attributed to the correct outlet. `fields[]` on the update record matches every changed line in `git diff`.
- WatchGuard update: NCSC-CH advisory 12901 and both new WatchGuard PSIRT pages (CVE-2026-19318, CVE-2026-78174) fetched fresh; affected/fixed version ranges, the diagnostic-logging precondition, and the Dimension session-hijack impact description all match verbatim. `fields[]` matches the diff.
- Liechtenstein update: Inside Paradeplatz and Exxpress fetched fresh. The VSV-letter/SBVg-separate-statement split, the "24 August" letter date and "seen by Reuters" attribution (correctly sourced to Exxpress, not Inside Paradeplatz, per iteration 1's fix), the "500,000 beneficial owners" figure, and the "extremely attractive target for cyber criminals" quote all check out. The Swiss-register-architecture paragraph (EasyGov, FDJP secured network, in-house build, FDF control unit) is confirmed verbatim against NZZ's 2026-08-07 article, fetched fresh — the article genuinely does cover both the Liechtenstein breach and the Swiss Transparency Register's own architecture in the same piece, validating iteration 1's F5 fix.
- DGFiP update: ZATAZ's Cybernox article fetched fresh. The 3,032,386-phone-number figure, the "don't call me" second handle, the 2026-08-07 ZATAZ publication date (five days before DGCCRF's 2026-08-12 warning), and the hedging quote ("Ces piratages doivent être traités comme des revendications...") are all verbatim matches; the entry correctly declines to upgrade the correlation to attribution, matching both ZATAZ's and DGCCRF's own text.
- Registry diff: `tool:noderabbit`/`tool:pollcat` relations correctly attached to the actor (not the reverse) per this run's own stated fix; no untyped/malformed relations; no watchlist or org-triage fields present anywhere in scope (per this deployment's config, correctly absent).
- Coverage-backlog and run-record notes: every specific claim about backlog rows (struck DGFiP/Bloctel-Cybernox and Swiss-Transparency-Register rows; dated re-check notes on Boston Scientific, Insel Gruppe, Ixa Systems, Zurich verdict, OpenShift) checked against `git diff HEAD -- state/coverage_backlog.md` and matched exactly — no repeat of the false-claim pattern iteration 2 caught earlier in this run.
- Style/classification: no IOCs, no vanity metrics, classification blocks present and consistent with source tiers (Kaspersky Securelist B, heise B) on all three new entries; no `watchlist_hit: true`; no `org_triage` block populated anywhere in scope.

### Verdict

NEEDS_FIXES (truth: 2, editorial: 0, advisory: 1)

Both truth findings are low-confidence, narrow technical simplifications in one paragraph of one entry (and its mirrored registry summary) — not misleading at the level a Tier 2/3 responder would act differently on, but genuine adjacency mismatches against the cited page's precise wording, reported per the coverage obligation. The advisory finding is a single leftover instance of a defect class already established and mostly fixed by iteration 6. Given the extremely narrow scope of what remains (one paragraph's causal-chain precision, one word-choice breadth, one shorthand label), and that this is the loop's 8th and final iteration, the main agent should apply the three cheap, mechanical fixes and record the residuals in the run record as the cap-breach note directs; nothing found here rises to a level that should hold up publication independent of the cap.

### Findings summary (machine-readable)

See sibling file `work/2026-09-02T0411Z-intel/verification.iter8.findings.yaml`.
