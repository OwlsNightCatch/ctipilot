- code: F5
  category: missing-citation
  section: trending-vulnerabilities
  item: "JADEPUFFER — Sysdig autonomous LLM-driven ransomware via Langflow CVE-2025-3248"
  url_or_quote: "title/summary/frontmatter: \"CVE-2025-3248 (CVSS 9.8)\" and cves[].cvss: \"9.8\"; body/actions: \"/api/v1/validate/code\" endpoint path"
  summary: "CVSS 9.8 and the literal endpoint path /api/v1/validate/code appear in the entry but in NEITHER cited source (Sysdig primary + THN corroborating both fetched in full this iteration; neither contains '9.8', 'CVSS', 'validate', or '/api/v1'). Both values are externally correct (NVD rates CVE-2025-3248 9.8; Horizon3 documents the /api/v1/validate/code endpoint), so this is an uncited-fact gap, not a fabrication. Sysdig/THN only describe it functionally as the 'code validation endpoint'. Remediation: add a corroborating advisory that states the CVSS + endpoint (e.g. Horizon3 disclosure or EUVD record) as a second corroborating source, or soften the headline to drop the unsourced 'CVSS 9.8'. The 1.3.0 fix and 'CISA KEV since May 2025' ARE sourced (THN, verbatim) and need no change."
- code: F11
  category: editorial-advisory
  section: active-threats
  item: "NetNut (Popa) residential-proxy botnet disruption"
  url_or_quote: "entities/registry.yaml key campaign:popa-vo1d-residential-proxy-botnet, aliases: []"
  summary: "Advisory only. The reused entity key is correct (no second key invented). But 'NetNut' — now the primary name in both new sources — sits only inside the entity's name string, not in aliases[]. Consider adding 'NetNut' (and 'Popa') as formal aliases so downstream entity-linking on the NetNut name resolves. Not a publish blocker; leave if preferred."
