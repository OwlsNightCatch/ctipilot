# Project memory — ctipilot.ch

This file is the index of `.claude/memory/`. Claude Code's built-in auto-memory feature is **redirected here** by the SessionStart hook (`.claude/hooks/setup-memory.sh`), which symlinks the system auto-memory directory (`~/.claude/projects/<project-hash>/memory/`) to this repo-local directory. Result: every memory Claude writes is **version-controlled** and **shared across all sessions** — local Claude Code, the cloud routine, every operator, every worktree.

The first 200 lines or 25 KB of this file are loaded into every session by the auto-memory feature. Topic files in this directory are loaded on demand when Claude reads them. Keep this index lean — move detail into topic files and shorten index entries.

## How it works

- **Local Claude Code:** the SessionStart hook fires, computes the project hash from the current working directory, and creates the symlink. Subsequent `/memory` writes land here. Approve the hook once when prompted.
- **Cloud routine:** same hook fires in the routine container. Memory writes land in the cloned repo. The routine's Phase 5 commits `.claude/memory/` alongside `state/` files, so the next routine fire (or a local session, or another operator) sees the accumulated memory.
- **Worktrees:** each git worktree has its own auto-memory directory by hash, but the hook in each worktree symlinks to *that worktree's* `.claude/memory/`. Since the directory is committed, all worktrees see the same content via git.

## Conventions for topic files

- One topic per file. Filename = short kebab-case slug. Examples: `source-failures.md`, `webfetch-quirks.md`, `deep-dive-rotation.md`, `check-run-drift.md`, `publishing-races.md`.
- Front the file with a short YAML block: `name`, `description`, `type` (`user` / `feedback` / `project` / `reference`).
- Keep entries factual and dated when relevant. "Why" lines are useful — a fact without a why decays.
- Index entries here: `- [Title](file.md) — one-line hook` (≤150 chars).
- Update this index when you add, rename, merge, or remove a topic file.

## Index

- [Auto-commit/push/deploy for routine fixes](auto-publish-routine-fixes.md) — go end-to-end through commit→push→auto-merge→deploy-site→live URL without pausing; memory writes are pre-authorized in settings.json (`permissions.allow`) so they never prompt/interrupt
- [Changelog hygiene](changelog-hygiene.md) — version history lives only in prompts/CHANGELOG.md; never annotate rules with vN.M; check_run.py `prompt-version` gates it
- [Customization framework](customization-framework.md) — branding.yaml + org-profile.yaml carry ALL org/brand values; never reintroduce identity literals in build.py or lens phrases in prompt prose; PYTHONHASHSEED=0 for build byte-diffs
- [Design system](design-system.md) — the site's visual language = the "CTI Pilot Design Modernization" Claude Design project; component classes + brandable surface (nav/hero/ai-bar copy, accent-rgb auto-derived); DOM/JS contract for the new shell + live timeline; routes are /live/ (rolling), /daily/ (completed days only, excludes rolling day), /weekly/
- [UI writing style](ui-writing-style.md) — NEVER use em dashes (—) in UI chrome (titles, labels, tooltips, footer, hero/ai-bar); use ·/:/, instead; empty cells use en dash –; entry/brief CONTENT may use them sparingly (agent freedom)
- [Model identity & verifier rotation](model-identity-and-rotation.md) — self-ID primary source (v3.15) is the harness-injected "You are powered by…" line in each agent's OWN system prompt: pin-aware, probe-verified 2026-07-09 (pinning/rotation DO work); CLAUDE_FRIENDLY_NAME/CLAUDE_MODEL_ID are container-scoped, blind to `model:` pins, marked-fallback only; uniform env-fallback reports ≠ rotation failure
- [Source fetch blocks & primary-source substitutes](source-fetch-blocks.md) — **PDF-only advisories are readable since v3.32: `fetch_source.py pdf <URL>`, selected on CONTENT TYPE not on failure, mirrors count; an image-only PDF reports "no text objects" = NOT EXTRACTABLE, never "says nothing"**; extraction shape is a quote-fidelity trap (NBSP, whitespace normalisation, PDF hard line breaks — verify against the RAW body); NCSC-CH moved to bacs.admin.ch (old redirects explicitly NOT permanent); fetch ladder (v3.25) RSS→WebFetch→direct bridge (`url`/structured)→jina LAST RESORT (metered credit; force it only for `fetch_method: jina` hosts or after every direct rung fails); reader keys are a POOL (2026-07-13): `JINA_API_KEYS` list + legacy `JINA_API_KEY`, spend order, auto-rotate on 402/401; anonymous free tier is BEST-EFFORT ONLY (observed 401 2026-07-18 — exhausted pool = reader outage; quality audit checks `jina-usage` weekly); reader cost savers (v3.1): local 1 h disk cache (`JINA_CACHE_DIR`/`JINA_CACHE_TTL`, repeat fetches = 0 requests/0 tokens) + `X-Cache-Tolerance: 3600`; github.com egress-proxy-blocked → OSV.dev; CISA Akamai → `cisa page/feed/csaf`; kernel.org Anubis → distro trackers; coe.int/seppmail 401 even to reader = still blocked; ncsc-uk = `feed all-rss-feed.xml` (2026-07-11 — HTML listing is a consent shell; a 200 with no parseable items is a coverage gap, not a success); ransomware.live = JSON API `countryvictims/<CC>`; **the silent recipe gap (2026-08-09)** — a source can 200 forever and contribute nothing when its listing has no extractable dates: `infoguard-labs` was in that state for weeks, hid a 22-CVE DACH home-region disclosure, and the fix was one RSS URL. A `coverage_gaps` recipe-gap note is a repair order, not a status; when you fix one, the top of the feed is a backlog
- [Verifier loop — attribution defects outlast content defects](verifier-attribution-defects.md) — after iteration 1 catches content defects, the Phase 5.7 loop finds almost only citation-metadata errors (publication date vs event date; a tracker's own voice attributed to the victim; a quote labelled with the wrong speaker; run-record prose contradicting its own frontmatter); aim iterations 3+ explicitly at per-clause citation labels and re-derive every count/date in the notes from the structured block
- [Verifier findings are evidence, not verdicts](verifier-findings-are-evidence.md) — verifiers false-positive at a material rate BUT 2026-08-07 saw 26 findings with every truth finding holding, incl. an INVERTED hardening claim and a relay-date-for-disclosure-date error 2 framed passes missed: leave ≥1 odd iteration COMPLETELY UNFRAMED (framing buys depth on axes you suspect, costs the ones you don't); withholding a vendor's generation-pattern hunt rule as if it were an IOC is itself a defect; a verifier's 'no action recommended' is advice, not a ruling. Worst false positives on non-English text (2026-08-05: 3 of 6 iteration-2 truth findings wrong, incl. a quote called "wholly fabricated" that is verbatim); test every finding that changes a claim/quote/attribution against a fresh fetch BEFORE applying — and note the trap: findings that merely ADD a caveat get accepted uncritically and inject unsourced claims (one did, and iteration 3 caught it as a new defect); record rebuttals in the run record with evidence
- [Reader-credential outage — what breaks and what to do](reader-pool-outage-playbook.md) — all four jina keys exhausted (2026-08-02) took out 8 sources in one run incl. the CISA advisories/directives listings (KEV survives via its own api subcommand); NEVER demote for it, probe direct alternatives and record them, escalate to the operator; plus the `probe_url` field that fixes probe-target-mismatch false `needs-demote` flags; **2026-08-22 the second probe-side false demotion — byte count is not health**: a valid `count: 0` JSON envelope (`sec-edgar 8k`, ~120 B) read as a dead recipe, fixed by asserting structure instead of size (sweep now 189/189, all action `none`) — a probe must assert the shape the recipe promises, never a proxy (bytes, non-empty, HTTP 200 have each produced a false demotion here)
- [Classifier trips on sub-agent spawns](classifier-trips-on-spawns.md) — "safeguards flagged this message" kills spawns (clusters on Sonnet + raw offensive-research content: 2026-07-23 lost a whole verifier rotation + 3 research spawns + the deep-reads; 2026-07-26 audit lost 2 B2 spawns); mitigation = defensive role framing + checkpoint the findings YAML every 3–4 items; escalate retry → reframe → other definition (record the deviation) → never block the run; a uniform-model verifier chain may be this, NOT a rotation failure. **The false positive (2026-08-22): absence of output files is NOT a dead spawn** — a healthy iteration-2 verifier with an empty run directory at 11 min was declared dead and retried, then delivered its report 90 s later; wait for the harness notification, and check that a recovery spawn can even advance the rotation it exists to protect (this one was same-model, so it could not)
- [Entity registry graph conventions](entity-registry-graph.md) — v3.20: typed `relations[]` edges `{to, type, source, note}` replaced `related` (retired, gate-FAILed); 10-type vocabulary + direction rules in `content_model.RELATION_TYPES` / docs/pipeline.md § Relationships; `source` = establishing entry id (required); only relate what a source states; /graph/ is the analyst surface. v3.16 basics still hold: name = canonical entity name only, aliases load-bearing (dedup + phrase matching); duplicates → `merged_into` tombstones (edges move to the canonical record); never reference a tombstone in new entries
- [State-file serialization](state-file-serialization.md) — sources.json canonical format is `indent=2, ensure_ascii=False` (re-ratified 2026-07-18 after the 07-17 run flipped 1→2 on main); re-dumping with a different indent/ascii flips every line into a ~6700-line churn diff; check `head -5` before dumping, `git diff --stat` after, bump in place
- [Triage-ready entries](triage-ready-entries.md) — v3.13 actionability shape: observable behavior in vendor-neutral telemetry classes, `techniques[]`/`affected_products[]` frontmatter, `**Triage:**` discriminator omitted-never-invented; migrated_from != null = lower-fidelity tier
- [Classification policy](classification-policy.md) — v3.18: every entry carries exactly one rating (Admiralty, or org_triage only while a triage scheme is configured); check_run FAILs missing ratings + empty behavior-kind techniques[] on v3.18+ runs, --all store-ratings sweep guards it forever; a same-day press write-up of one lab report is NOT a second source (independence = first-hand observation) — single-source + credibility 2
- [MITRE ATT&CK layer](attack-layer.md) — v3.17: pinned `attack/enterprise-attack.json` (tools/attack_data.py; weekly --check duty; NEVER hardcode tactic tables — v19 renamed them); `techniques[]` = canonical complete mapping surface, prose readable without T-numbers; entity/CVE TTPs derived evidence-bound; /attack/ overlap matrix + Navigator layers; revoked ids forward like tombstones
- [Scheduler outages & workflow races](scheduler-and-workflow-races.md) — cadence is OPERATOR-OWNED and variable at will (2026-07-18: 1×/day intended; work off the gap to the last run, never flag a cadence change — only a record-less gap is an availability signal); 2026-07-07 62h gap was a CONFIRMED scheduler outage (not repo-side); runaway main runs + overtaken-run race → v3.21 watchdog + re-sync-and-re-dedup; compose-profile × auto-merge branch-delete race fixed via fallback-to-main. **2026-08-22 was the worst overtake yet: 53 h container lifetime, THREE fires past it, 8 of 16 verified entries stood down as duplicates.** Two of the eight had no CVE and no shared entity key — only reading caught them, an index pass would have shipped both. An overtaking fire's window is gap-to-last-*published*-run, so it swallows yours whole (74 h). Resolve the merge by taking main's shared state and re-applying only your genuinely-new records — `--ours` would have discarded three fires of work; check tooling diffs too (our source_health fix was a rediscovery of main's, and main's was better). A still-valuable duplicate can survive as `update_of` on the overtaking entry, but `update_of` cannot point at a later `discovered_at` and the folder date must equal `discovered_at`
- [CSAF / MSRC transcription](csaf-msrc-transcription.md) — read affected/fixed status from structured fields, not the summary: CSAF `product_status` (known_affected vs fixed) + `remediations[].vendor_fix` (the real patch, not "Recommended Practices"); MSRC `revisions[]` (a July CVE can carry a June patch). 3 of 7 verifier findings on 2026-07-15 were this one root cause; a displayed CVSS may be the TEMPORAL score (check the linked vector for E:/RL:/RC:) — cves[].cvss carries the BASE score
- [Store-wide CVE index vs the 14-day read](dedup-store-wide-cve-index.md) — the 14-day prior_coverage read is a FLOOR, not the dedup boundary; check_run's cross-run FAIL only fires inside 14 days, so month-old coverage passes the gate. 2026-08-08: two of fourteen items flipped from new to update_of on the store-wide `cves.ids` lookup (Kemp LoadMaster KEV addition; Zapscape as a sibling of Januscape). PD-13 nuance: read the covered entry's BODY, not just its `cves[].status` — a status of `exploited` alongside a body saying attempts failed still makes a KEV listing a material delta
- [Structured metadata is a claim too](frontmatter-status-flags-need-sources.md) — a `cves[].status` flag needs a source like any sentence; 2026-08-11 shipped `cisa-kev` on two FortiOS CVEs that neither cited source asserts (the advisory's only catalogue text is #StopRansomware boilerplate + a footer link), true-in-the-world but pure PD-1 recall. Iteration 1 cleared the entry on truth; iteration 2 caught it only by reading the 631-line advisory end to end. Prose defects announce themselves, a wrong enum in frontmatter does not — and nothing in the body depended on the flag, which is the tell. Before commit, name the source sentence for every status flag; aim ≥1 verifier iteration at the structured blocks, not the narrative
- [Inverted claims — fluent, cited, backwards](verifier-inverted-claims.md) — the defect that reads perfectly and says the opposite; 2026-08-08 produced two in three sentences off one informal researcher blog (SIP "does not block" when the post says it does NOT bypass SIP — the "doesn't care" line was about TCC; "read and write as root" when the source says download-only; a version number that appears 0 times on the page). Informal prose with adjacent controls/researchers/bugs is the high-risk shape: grep every control name, privilege level and version string before writing the sentence
- [Entry immutability exceptions](entry-immutability-exceptions.md) — audit log of operator-authorized entry edits; 2026-07-09: four v2→v3 dangling update_of links repointed; 2026-07-11: migration-era entities[] pollution repaired (122 false links from org-name entity names removed, 103 title-supported links added, 201 entries, frontmatter lists only); 2026-07-11 quality audit: three metadata error repairs (wolfSSL CVE ids ×3, BeyondTrust CVSS 9.9→8.5, Odido T1656→T1684.001) — full report docs/audits/2026-07-11-intelligence-quality-audit.md
- [Weekly synthesis citation traps](weekly-synthesis-citation-traps.md) — the weekly re-frames facts it did not fetch, so its defects are sourcing not content (2026-W32: 17/17 iteration-1 truth findings were mis-citations): blog-vs-PDF (mechanics live in the linked report), vendor blog URLs mutate in place (an August-2 quote gone from the August-2 URL), slugs are not datelines (BSI `260601` = 6 January per its own `Datum` field), national-CERT relays carry less than the vendor bulletin (CERT-FR has versions, not CVSS/scope), and two-facts-one-clause; sub-agent figures need `grep -F` before use; drop rather than half-source an attribution; open contradiction — weekly-vuln-rollup `cves[]` per the prompt vs check_run's dedup FAIL, resolved FOUR weeklies running by a body table + `cves: []`. **2026-W33 (7 iterations, 51 findings) added the two dominant mechanics:** (a) an inherited sentence brings the prose but NOT the `sources[]` record — the citation that lands on the clause is whatever was already there (the roll-up cited Adobe's "not aware of any exploits in the wild" bulletin as its only basis for calling a CVE exploited; the real observer, Sansec, was in no source list) — so lift the source record with the sentence; (b) partial remediation — three iterations each caught a defect that was an earlier fix applied to one half of an entry (summary fixed, body not; figures swapped, title not), so after every fix grep the same fact across title/headline/summary/body/evidence/cves. Plus: re-derive every numeral and absolute from the body's own enumeration before commit (four consecutive iterations caught one), and never merge time-to-exploit with time-to-exploitation in one sentence
