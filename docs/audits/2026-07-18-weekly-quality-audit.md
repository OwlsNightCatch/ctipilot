# Weekly quality audit — 2026-07-18

Scheduled weekly audit (run record: [`runs/2026-07-18/2026-07-18T1208Z-audit.md`](../../runs/2026-07-18/2026-07-18T1208Z-audit.md), prompt v3.25 at fire time). Mandate per `prompts/quality-audit.md`: re-verify the trailing window's published intelligence against primary sources (soundness), independently re-research the window and diff against the store (completeness), review the machinery for drift, root-cause every confirmed failure, and ship the fixes.

**Window.** 2026-07-11T14:35Z → 2026-07-18T12:08Z (~166 h). Anchored at the last audit that actually audited (`2026-07-11T1435Z-audit`); the 2026-07-12T1308Z audit record stood down as `duplicate-audit` and covered nothing. Scope: **62 published entries** (47 operational + 15 W28 weekly strategic) and **16 run records**.

**Method.** Four retrospective truth passes (two on Opus 4.8, two on Sonnet 5 — model diversity per the audit rotation) fetched the primary sources behind all 62 entries and checked every CVE id and CVSS against its per-CVE authority, KEV/exploitation claims, version boundaries, victim statements, attributions, verbatim evidence quotes, ATT&CK ids against the pinned v19.1 dataset, classification consistency, and frontmatter⇔body agreement (~150 primary URL fetches). Three independent coverage re-sweeps re-researched the window as if for the first time: G1 vulnerabilities/exploitation (KEV, national CERTs, PSIRTs, EUVD), G2 incidents/ransomware with home-region priority plus the carried-forward watch-item re-check, G3 per-publisher research-blog listing sweeps across 22 publishers. One scoped deep-read pass enriched the two recovery candidates. In parallel: telemetry review over the window's 16 run records, source-health and discipline-drift analysis, fix-effectiveness checks on everything the 2026-07-11 audit shipped, and the July priority-calibration review (Phase 3b — first calibration section this month).

## Verdict

**48 of 62 entries verified fully clean; zero hallucinated facts, zero broken primary URLs, zero wrong CVE ids or CVSS scores in operational entries, zero IOC leaks, all 100+ distinct ATT&CK ids active in the pinned dataset.** The operational pipeline (47 entries) is in excellent shape: 45 effectively clean, two minor documentation-level imprecisions, and one flagged staleness case that resolved as a false alarm — the pipeline had already shipped the correct `update_of` delta. The week's real quality story is concentrated in the **W28 weekly strategic batch**: 12 of 15 entries carry citation dates 1–8 days later than their sources' true publication dates, and four of those additionally attribute a specific fact to a co-cited source that does not carry it. All underlying facts were true and no defender decision changes — but the pattern is systemic to the weekly's synthesis workflow and a prompt fix ships with this audit (v3.26). Completeness: the incident domain has **no gaps**; the vulnerability and research domains had **three genuine misses** (WordPress WP2Shell, Kaspersky GoSerpent, Moodle local_o365), all recovered and published by this audit through the full normal gates, each with a specific, fixed root cause. Machinery: no runaway runs (the 07-11 watchdog fix held), publish follow-through 16/16, discipline metrics healthy — but the v3.23 double-CLEAN verification gate is hitting its 5-iteration cap in the majority of post-v3.23 runs and publishing on fail-open waivers, which needs an operator decision (recommendation 2).

## Findings — false or erroneous published intelligence

Truth-pass results by batch: B1 (operational 07-11→07-14, Opus): 14 clean / 2 imprecisions. B2 (operational 07-14→07-16, Sonnet): 15 clean / 1 flagged, resolved as a false alarm. B3 (operational 07-16→07-18, Opus): 15/15 clean. B4 (W28 weekly strategic, Sonnet): 3 clean / 8 imprecisions / 4 factual errors. Detailed per-entry records: `work/2026-07-18T1208Z-audit/truth-B{1..4}.yaml`.

**No repairs were warranted anywhere in the window** — none of the findings touches the machine-surface class (CVE ids, CVSS in `cves[]`, ATT&CK ids, registry keys) that the immutability exception covers, and none changes a defender decision. Everything below is documented under the 2026-07-11 minor-findings precedent; entries stay immutable.

| # | Entry | Defect | Ground truth |
|---|---|---|---|
| 1 | `2026-07-13/russia-ip-camera-hijacking-nato-military-supply-routes` | evidence[] quote #1 attributed to the cited NL Times URL does not appear verbatim there (reads as syndicated wire copy); secondary wobble: "disclosed Friday" vs `event_date` 07-11 | The article's own text supports all entry facts (cameras compromised, default passwords/outdated firmware, AIVD/MIVD) — the quote's wording came from elsewhere |
| 2 | `2026-07-14/eset-forgotten-uefi-shims-secure-boot-bypass` | `sourcing_note` claims "the two CVE identifiers resolve on cve.org" — CVE-2026-10797 returns CVE_RECORD_DNE/404 there and is absent from NVD | Both ids are valid and correctly attributed (ESET Research + CERT/CC VU#616257 both cite them); only the note's database-resolution claim is wrong |
| 3 | `2026-07-12/weekly-w28-exploited-edge-enterprise-software` | "Langflow … now KEV-listed" cited solely to a Sysdig post published 06-26 (not 07-08 as cited) that itself says the IDOR is *not* in KEV | The KEV addition (07-07/08) is real but carried by BleepingComputer — an in-store source this entry does not cite |
| 4 | `2026-07-12/weekly-w28-government-public-admin-targeting` | "a service left unpatched for roughly two years" attributed to CERT.LV's bulletin, which never says it; Kaspersky citation dated 8 days late | The "two years" detail is The Record's (2026-07-09), uncited |
| 5 | `2026-07-12/weekly-w28-the-gentlemen-status` | "580 victims … through 3 July" — spliced Unit 42's headline count onto the as-of date of an adjacent chart | Unit 42: "Through July 7, one reputable source had counted a total of 580 victims … across 77 countries"; the "through July 3" date belongs to a different figure comparing RaaS programs (main-agent spot-check confirmed both passages on the live page) |
| 6 | `2026-07-12/weekly-w28-third-party-cloud-account-exposure` | "claimed by 'The Syndicate'" cited to Nayax's SEC 6-K, which never names the group | The attribution is DataBreaches.net's (2026-07-08), uncited |
| 7 | Eight further W28 entries + `looking-ahead-2026-w28` | Citation dates 1–8 days later than the sources' actual publication dates (JSON-LD/meta-verified); all substance accurate | Pattern: the pipeline's discovery/processing date was reused as the citation date at synthesis time |

**Root cause (findings 3–7, one mechanism).** The weekly composes by re-framing operational entries and findings files rather than fresh fetches, and nothing required it to (a) carry the source's own publication date into inline citations or (b) re-verify that the *specific* co-cited source carries each specific fact. The operational entries citing the same sources got the dates right — the defect enters exactly at the synthesis step. Fix shipped: prompts **v3.26** adds a mechanical citation-date + per-fact-attribution duty to `prompts/weekly-summary.md` Phase 4 (see § Fixes). A caveat recorded for fairness: a subset of the 1-day drifts may be timezone-rendering artifacts of late-UTC publication; the 2-, 3- and 8-day drifts and all four attribution errors are not explainable that way.

**Root cause (findings 1–2, pre-existing classes).** #1 is the known citation-locality class (quote sourced from one syndication, cited to another) — the v3.26 weekly duty does not cover it because it arose in an operational entry; it remains rare (1 of 47) and the verifier already flags it (F4/F3) — no new mechanism shipped. #2 is a sourcing-note overclaim (asserting database resolution that was never checked); rare (1 of 47), documented.

**Resolved false alarm (B2).** The 07-15 Patch Tuesday follow-up's claim that CVE-2026-58644 was "not confirmed exploited" was accurate at compose time; MSRC revised the flag later that day and CISA KEV-listed it 07-16 — and the pipeline shipped the correct delta as `2026-07-17/cve-2026-58644-sharepoint-confirmed-exploited-kev` (`update_of` the 07-15 entry) on the next fire. Entry immutability + update discipline worked exactly as designed; no action.

## Findings — missing or incomplete coverage

Re-sweep results: G1 checked ~45 CVE/advisory items (3 candidate gaps), G2 checked 8 incident items (**zero gaps** — all matched store coverage), G3 enumerated in-window posts across 22 research publishers (3 candidate gaps, 4 borderlines). Store-diff by the main agent resolved the candidates as follows.

**Genuine misses, recovered and published by this audit (through dedup, `check_run.py`, and the verifier loop):**

1. **WordPress core "WP2Shell" pre-auth RCE chain (CVE-2026-63030 + CVE-2026-60137)** → `entries/2026-07-18/wordpress-core-wp2shell-preauth-rce-chain-cve-2026-63030` (`high`). Out-of-band core security release 2026-07-17T18:45Z; public checker + detection-only PoC; NCSC-NL expects short-term exploitation. Everything was public ~9 h before the 07-18T0409Z fire. **Root cause, two-part:** (a) S1 used ENISA EUVD as a per-CVE *lookup* service that morning, not as a discovery *listing sweep* — the EUVD recent/criticals listings carried the records (published 19:14Z 07-17) and G1 found them exactly there; (b) no curated source carries WordPress core releases (the discoverer, `searchlight-cyber`, is a sporadic candidate source that was not in the day's rotation slice; the NCSC-NL advisory landed 11:01Z 07-18, after the fire). Fixes shipped: an `enisa-euvd` recipe note making the listing sweep an explicit duty, and `wordpress-org-news` added as this run's one new candidate source. Contributing factor: the cadence drop to one fire/day (see systemic finding 3) removed the two later fires that would have caught the NCSC-NL echo and media pickup.
2. **Kaspersky GReAT "GoSerpent" evolution (2026-07-16)** → `entries/2026-07-18/goserpent-backdoor-evolution-sea-government-diplomatic` (`notable`). Government/diplomatic-targeting espionage tradecraft with a staged collect-then-return intrusion model — squarely the profile's government lens (same class as the 07-11 audit's Armored Likho recovery). **Root cause:** the securelist.com listing renders several post titles with no visible date to a plain fetch, so the 07-16→07-18 fires' S3 sweeps read the new post as old and never drilled it; G3 surfaced it only via targeted WebSearch. Fix shipped: `kaspersky-securelist` recipe note (sweep the RSS feed, which carries dates; never assume an undated listing title is old).
3. **Moodle local_o365 JWT-forgery auth bypass (CVE-2026-54733)** → `entries/2026-07-18/moodle-local-o365-jwt-forgery-admin-takeover-cve-2026-54733` (`notable`). Pre-auth impersonation-to-admin on the Teams SSO endpoint of the official Microsoft 365 plugin. Dates (corrected during this run's verifier loop — iteration 2 caught the entry initially misdating the primary): the fix shipped in April 2026 plugin releases (tags `v20260423`), the GitHub advisory published **2026-07-06** (its own page date; the OSV mirror records a 07-16 ingestion timestamp for the same GHSA), and BSI CERT-Bund's advisory WID-SEC-2026-2400 (severity "hoch") followed **2026-07-16/17** — the last of these is the in-window anchor and the recency basis for the recovery. `event_date` records the 07-06 disclosure so the entry makes no false freshness claim. G1 flagged the include as borderline on exactly this recency ground; the audit's call is **include at `notable`** under the national-CERT-surfaces-a-vendor-advisory-fresh carve-out: BSI (an essential source the 07-17 and 07-18 fires both swept) issued an in-window advisory, the outcome is full-admin from a trivially-forgeable token on an internet-facing identity bridge, and the education/research estate is in the constituency's wider public-sector remit. **Root cause:** plugin-level CVEs surfacing through the WID-SEC roundup feed pattern-match to routine patch-cycle items, so the fires swept BSI but did not drill the advisory to see the trust-forgery mechanics. No mechanism fix ships — this is a PD-11 margin judgment-call, and one marginal miss in a week does not evidence a systemic filter defect; recorded for the next audit to watch the class (vulnerabilities whose *mechanics* justify out-of-band response despite no exploitation).

**Correctly-droppable borderlines (documented so the completeness judgment is auditable):**

- **FortiSandbox CVE-2026-25089/-39808 KEV addition (07-16)** — G1 flagged as a gap, resolved as *not one*: the CVEs and campaign were covered three times in June (06-12/06-17/06-22, exploitation included); the 07-17 and 07-18 run records each documented the KEV-flag-only delta as a borderline drop with correct policy reasoning ("a KEV listing never opens an update entry"). G1's 14-day dedup context simply predated the June coverage — the store-wide CVE index catches it.
- **CVE-2008-4128 KEV addition (07-13)** — named with correct EOL context in the Static Tundra entry's prose; a `cves[]`-enrichment update for an 18-year-old, patchless, EOL-only bug is marginal and stays dropped.
- **Kaspersky OkoBot (07-15)** — crypto-wallet-theft framework; the trojanized-SSMS-repo lure is a transferable developer-endpoint lesson, but the objective and victim class sit outside the constituency; relevance doubt resolves toward drop.
- **Unit 42 TuxBot v3 (07-15)** — LLM-assisted IoT botnet; research-curiosity value (LLM artifacts left in code) exceeds its detection-changing value for this constituency; the adjacent AI-tooling angle was already carried in-window (`2026-07-14/patriot-bait-jailbroken-gemini-cli-autonomous-c2`).
- **Mandiant Cloud Run/Functions hardening guidance, Unit 42 AI-IR recap, Varonis Dialogflow "Rogue Agent"** — respectively: product-hardening guidance with no campaign, recycled February content, and a primary that predates the window.

**Resolved false alarm:** none in the coverage domain beyond the FortiSandbox item above.

## Findings — systemic / operational

1. **Double-CLEAN gate churns to the iteration cap (the week's main machinery finding).** Since v3.23 landed (07-13), 7 of 10 scheduled runs used 4–5 verification iterations and **5 of 10 published on a `confirmation_waived` fail-open** (07-13T2009Z, 07-14T0409Z, 07-14T1210Z, 07-15T0409Z, 07-17T0409Z) — the two-model confirmed CLEAN the gate exists to produce was achieved in a minority of runs. The mechanism is arithmetic: any NEEDS_FIXES at iteration 4 makes a confirmed CLEAN impossible within the cap of 5 (it would need CLEANs at 5 *and* 6), and the alternating models reliably each surface one further minor finding per pass (the waiver notes show every finding was genuine and remediated — the rotation is *working*; the cap just cuts it off). The cap is named in hard invariant #11, so the audit does not touch it — **operator recommendation 2**.
2. **Publish follow-through: clean.** 16/16 window records carry a final `publish_status` (all `ok`); the 07-11 finding class (amendment never landing, invisibly) did not recur.
3. **Scheduler cadence silently halved.** Through 07-14 the pipeline fired 3×/day (0409/1210/2009Z); from 07-15 only the 0409Z slot fires (`gap_hours: 24` exactly, three consecutive days). The runs self-healed correctly (window 26 h, no coverage hole *between* fires) — but nothing anywhere records whether this was an intended operator change or two silently dead scheduler slots, and it materially contributed to the WP2Shell miss (disclosure at 18:45Z waits ~9.5 h for the next fire; under the old cadence the 1210Z/2009Z fires would have caught the NCSC-NL echo same-day). This is scheduler-side (outside § META authority) — **operator recommendation 1**, and exactly the class the 07-11 audit's still-unadopted scheduler-watchdog recommendation targets.
4. **Reader-pool outage and recovery.** The jina reader key pool was exhausted from 07-13 (HTTP 402; the 07-18T0409Z run additionally observed HTTP 401 on the anonymous tier) — the 07-13 run flagged it, v3.25 (07-18, pre-audit) demoted the reader to last-resort transport, and the operator added four fresh keys on 2026-07-18 ~10:01Z (audit-verified via `jina-usage`: 4 live keys, ~39.9 M tokens). **Resolved; recommendation adopted.** Residual note: the outage window (07-13 → 07-18) cost no verified content — the affected leads were out-of-window or dropped on merits — but ico-uk and industrialcyber article surfaces were unreachable during it (disclosed per-run).
5. **Dark-but-green check.** All 15 essential sources fetched successfully through the window. `ncsc-uk` — the 07-11 audit's dark-but-green case — **contributed a published citation on 07-13** (FSB Centre 16 joint advisory) via the recipe recorded then: fix effective. BSI/CERT-EU/CERT-AT show zero window citations but their sweeps are documented in run notes with content simply pre-dating windows — quiet, not dark. One new suspect found by G2: `ncsc-ch-incidents` fetches 200 but serves stale consumer-phishing content (through Oct 2025), not incident bulletins — recipe-review note shipped on the source record.
6. **Securelist listing renders without dates** (root cause of the GoSerpent miss) — recipe note shipped; see coverage findings.
7. **Discipline drift: none found.** `actions[]` across the window's 62 entries: 40 empty / 15 with one / 7 with two, none above two — the v3.19 empty-is-normal shape fully restored (pre-fix baseline: uniformly 2–3 per entry, zero empty). All 62 entries carry a valid Admiralty rating; no empty `techniques[]` anywhere; 12 proper `update_of` deltas; every borderline drop in the window's run records carries a reason line. Store-wide gate (`check_run.py --all`): 0 FAIL / 8 WARN (three known pre-fix runaway-duration records, five documented confirmation waivers — all accounted for above). ATT&CK pin current (v19.1 = upstream latest).
8. **Fix effectiveness (2026-07-11 audit).** All five shipped fixes verified effective in this window's output: **CVE provenance rule (v3.21)** — truth passes re-derived every operational `cves[]` id/CVSS from per-CVE authorities with zero mismatches (the 07-16 record even documents its per-CVE cross-check inline); **wall-clock watchdog** — zero runs over 3 h (max 2.9 h; the two runaway records predate the fix); **outage-backfill duty** — not exercised (no gap > 24 h in-window); dormant, not failed; **verifier per-CVE cross-check (F4 scope)** — visible in iteration findings (e.g. the 07-14 CrashStealer catches); **check_run additions** — duration WARN and publish-status WARN both firing on the intended records, dead-ATT&CK-id FAIL active. **ncsc-uk recipe** — contributing (see 5).
9. **Audit-artifact hygiene note (this run).** The B2 truth pass, finding earlier ledger rows without timestamps, normalized the whole `url-liveness.tsv` timestamp column instead of leaving the missing fields — the audit's own forensic ledger therefore carries reconstructed (window-accurate but not per-fetch-accurate) timestamps for batch-B1 rows. No content impact; recorded for transparency.

## Priority calibration

First calibration section for July (Phase 3b duty — none earlier this month; the monthly cadence is now current).

| Scope | critical | high | notable | routine | high share |
|---|---|---|---|---|---|
| Store-wide (977 entries) | 15 | 352 | 608 | 2 | **36.0 %** |
| Trailing month (06-18 → 07-18, 350) | 2 | 122 | 224 | 2 | **34.9 %** |
| Audit window (62) | 0 | 17 | 44 | 1 | **27.4 %** |

- **F16 signal:** zero priority-calibration findings in any of the month's verification iterations, and zero priority-adjustment remediations — the loop consistently judged windows calibrated, including several explicit per-run calibration notes.
- **`critical` bar:** the month's two criticals (PTC Windchill unauth deserialization RCE 06-20; SimpleHelp RMM actively-exploited auth bypass 06-30) each clear the stop-and-act bar. The 18-day critical-free streak since 06-30 was checked against the window's real exploitation pressure (SharePoint CVE-2026-58644 KEV, SonicWall SMA1000 exploited pair, Oracle EBS KEV, ShareFile exploitation): each shipped at `high` with either patch-plus-hunt posture already public for days, or (SonicWall) an `update_of` chain on coverage that had carried the urgency at first disclosure — none demanded the to-the-hour immediate-action page. The streak reflects the bar, not under-alerting.
- **`high` share:** trending the right way — 37 % at the 07-11 baseline, 34.9 % over the trailing month, 27.4 % in this window. The 07-11 verdict ("stable but on the generous side") stands; no drift toward inflation, movement is deflationary.
- **Verdict: no calibration edit warranted.** The bars are holding under a verifier loop that actively checks them (rec 3 of the 07-11 audit is hereby discharged for July; the duty recurs monthly via the audit prompt's Phase 3b self-check).

## Fixes shipped in this commit

- **Prompts v3.26** (weekly-summary Phase 4 + CHANGELOG + lockstep banners): mechanical citation-date + per-fact-attribution duty at weekly synthesis time — inline citation dates come from the source's own publication metadata (or the referenced operational entry's `sources[]` record), never from pipeline bookkeeping; every number/as-of-date/status claim cites the specific source that states it.
- **Three audit-recovered entries** (each through dedup, the mechanical gate, and this run's verifier loop): WP2Shell (`high`), GoSerpent (`notable`), Moodle local_o365 (`notable`) — with two registry additions (`malware:goserpent`, `actor:tetrisphantom`, linked by a sourced hedged `overlaps-with` edge) and three `state/cves_seen.json` records.
- **`sources/sources.json`:** recipe notes on `enisa-euvd` (listing sweep is a discovery duty, not just per-CVE lookups), `kaspersky-securelist` (undated-listing trap — sweep the dated RSS feed), `ncsc-ch-incidents` (reachable-but-stale content suspect); contribution bookkeeping on `searchlight-cyber`; **one new candidate source** `wordpress-org-news` (WordPress core security releases had no curated discovery path).
- **This report** + run record + `work/2026-07-18T1208Z-audit/` forensic artifacts (truth-pass YAMLs, sweep findings, deep-read findings, audit plan, url-liveness ledger).

## Recommendations (operator decisions, not shipped)

1. **Confirm (or restore) the scheduler cadence.** The 1210Z and 2009Z daily slots stopped firing after 2026-07-14 — silently. If intended, record it (a one-line operator note in the repo suffices); if not, restore the slots. Either way, adopt the still-open scheduler-side watchdog from the 07-11 audit (alert on a slot that produces no run record within an hour): this is the second audit in a row where scheduler behavior changed and only a trailing audit noticed. Single-daily cadence measurably raises disclosure-to-publication latency (WP2Shell: ~9.5 h to the next fire, then missed).
2. **Give the double-CLEAN gate room to confirm.** Half the post-v3.23 runs publish on `confirmation_waived` at the 5-iteration cap — the fail-open valve has become the common path. Options, in increasing order of change: (a) exempt the confirmation pass from the cap (cap 5 fix-iterations, allow iteration 6 only as an other-model confirmation of a CLEAN at 5); (b) raise the cap to 6; (c) accept the current behavior and re-label expectations. The cap is fixed in hard invariant #11, so this is the operator's call, not the audit's.
3. **Populate the org-profile watchlists** (carried from 07-11, still open, unchanged rationale — the `bd.zh.ch` watch item below remains exactly the signal a victim-side watchlist would prioritize).

Adopted since last audit (retired from this list): jina reader key pool regenerated (4 live keys added 2026-07-18 ~10:01Z; v3.25 additionally made the reader last-resort). Monthly priority-calibration review: institutionalized in the audit prompt and discharged for July above.

## Watch items

| Item | Status | Resolution condition |
|---|---|---|
| `bd.zh.ch` (Kanton Zürich Baudirektion) MedusaLocker leak-site listing (2026-07-01) | **Still uncorroborated** after G2's 12-query DE/FR/EN re-check (Swiss press, zh.ch, NCSC-CH, trackers); listing confirmed still live on the leak site as of 2026-07-18 — not paid/retracted | Victim statement, official zh.ch/NCSC-CH communication, or Admiralty A/B journalism → publish; listing removed without corroboration → document and close |
| Roundcube 1.6.17/1.7.2 patch fold-in (from 07-11) | Open, dormant — no in-window Roundcube delta to fold it into | Next Roundcube entry (any delta) references the patched versions |
| **NEW:** KELA "ByteToBreach" claim naming Romania's ANCPI (national land registry) as a victim | Single-source criminal-marketplace claim surfaced by G3; EU government registry = constituency-relevant if true; fails the fake-news gate today | Corroboration from ANCPI/Romanian authorities/A-B journalism → publish; next audit re-checks |
| **NEW:** PD-11 margin-class — unexploited vulnerabilities whose *mechanics* justify out-of-band response (the Moodle local_o365 miss class) | Watch — one judgment-call miss this window, not yet a pattern | Next audit checks whether the class recurs; two consecutive audits finding misses of this shape ⇒ a PD-11(b) clarification ships |
| Weekly citation-date discipline (v3.26 fix) | New fix — effectiveness unverified until the next weekly runs | Next audit truth-passes the first post-v3.26 weekly batch: citation dates match source publication dates |

## Operator response (2026-07-18, post-publication addendum)

Recorded verbatim-in-substance from the operator's same-day review of this audit; these dispositions are final and the next audit honors them as such.

1. **Recommendation 1 (scheduler cadence) — resolved: the single daily fire is intended.** The cadence drop to 1×/day was a deliberate operator change, not a dead slot. Standing policy going forward: the scheduler cadence is operator-owned and intentionally variable — it may be raised or lowered at will, and the pipeline must simply work off the gap to the last run (which it did: the gap-derived windows self-healed with no coverage hole between fires). A cadence change is therefore not an audit finding and not an operator recommendation; audits verify the gap-window mechanism held, nothing more. Codified in `prompts/quality-audit.md` Phase 3 (v3.27); the scheduler-side watchdog suggestion is superseded by the same decision for cadence changes (a slot that changes is policy; only a *record-less* gap on a schedule that was supposed to fire remains an availability signal).
2. **Recommendation 2 (double-CLEAN gate room) — adopted, variant (b) extended: verifier iteration cap raised 5 → 8.** The double-CLEAN publish gate stays required exactly as specified in v3.23; the Phase 5.7 loop cap moves from 5 to 8 iterations so the CLEAN chain has room to confirm instead of churning into the `confirmation_waived` fail-open. Shipped as prompts v3.27 (both master prompts, verification policy, both verifier definitions in lockstep, `tools/check_run.py` cap constant — version-aware so pre-v3.27 records keep their 5-cap semantics).
3. **Watch item `bd.zh.ch` (Kanton Zürich Baudirektion MedusaLocker listing) — CLOSED by operator decision.** The incident is no longer tracked as a dedicated watch item; no re-check duty carries forward. Any genuinely new information (victim statement, official communication, corroborated reporting) surfaces through the normal intel-run research sweeps and would publish as an `update_of` the 2026-07-02 entry — the run pipeline, not audit tracking, owns that story from here. The registry record `incident:medusalocker-canton-zurich-baudirektion-2026` and the published entry remain as-is (immutable history).

4. **Zero-warning discipline instituted (follow-up directive, same day).** The operator further directed that the repo be held *perfectly clean*: all 8 standing `check_run.py --all` warnings and both `site/build.py` self-check warnings existing at the time were resolved (renderer emphasis-leak fixes at root cause; the 8 immutable-history warnings acknowledged with reasons in the new `state/warning_acknowledgments.json` ledger), and the discipline was codified as prompts v3.28: every run fixes its own fixable warnings before commit, and the weekly audit is the pipeline's weekly cleanup — it sweeps the store to **0 warn · 0 fail** (fix the cause, or acknowledge genuinely unfixable settled history in the ledger) and a warning-free build before it publishes, fixing everything else fixable it finds along the way.

5. **`ncsc-ch-incidents` reachable-but-stale suspect (G2) — RESOLVED, false alarm (operator-directed re-verification, same day).** The documented bridge fetch returns the full accordion — 10 dated entries newest-first, latest **01.07.2026 13:22** (SwissNovaChat/SwissNovaCare fake-subscription warning), operator-confirmed as the page's actual latest content. G2's "Oct-2025 only" observation was a truncated/summarized read: 4 of 10 accordion entries cluster in Oct 2025, so a partial fetch that misses the top of the page reads as stale. The page stays consumer-fraud-only by design with a genuinely slow cadence; operational/sector incidents live on the Cyber Security Hub / Im Fokus, as the source record has long noted. Recipe unchanged, no demotion; the read-the-full-accordion rule is recorded in the source's notes and `.claude/memory/source-fetch-blocks.md`, so the next audit's fix-effectiveness check should verify freshness from the raw bridge body, not re-flag the suspect.

Watch-item ledger after this response: **3 open** (Roundcube patch fold-in; KELA/ANCPI corroboration; PD-11 margin-class) + 1 fix-effectiveness check (weekly citation-date discipline, v3.26) — `bd.zh.ch` closed.
